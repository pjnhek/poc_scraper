---
phase: 07-public-repo-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_check_public_discipline.py
autonomous: true
requirements:
  - REPO-04

must_haves:
  truths:
    - "The pre-commit guard at scripts/check_public_discipline.py has CI-runnable unit-test coverage of both its match branches (D-05)."
    - "The content-match branch (denylist term inside file body) returns 1 (D-06)."
    - "The path-match branch (denylist term inside file path) returns 1 (D-06)."
    - "The test patches DENYLIST to a tmp file containing only a fake non-sensitive term so the real hiring company name never enters the test source or the CI cache (D-05, THR-02)."
    - "Test runs in CI as part of the offline test suite (no smoke marker, no external services)."
  artifacts:
    - path: "tests/unit/test_check_public_discipline.py"
      provides: "Parametrized coverage of scripts/check_public_discipline.py::main content-match + path-match branches"
      contains: "def test_main_flags_violations"
  key_links:
    - from: "tests/unit/test_check_public_discipline.py"
      to: "scripts.check_public_discipline"
      via: "monkeypatch.setattr(mod, \"DENYLIST\", fake_denylist) + monkeypatch.setattr(mod, \"_staged_content\", lambda p: body)"
      pattern: "monkeypatch.setattr"
---

<objective>
Add `tests/unit/test_check_public_discipline.py` to close the test gap on the existing pre-commit guard at `scripts/check_public_discipline.py`. Two parametrized cases cover the content-match and path-match branches of the `if pat.search(content) or pat.search(path):` line per D-06. The test patches `DENYLIST` to a tmp file with a publishable fake term ("fake-denylisted-term-for-test") per D-05 and THR-02 so the real hiring company name never appears in a test source file.

Purpose: REPO-04's success criterion is "pre-commit hook blocks the hiring company name in staged content or paths." The guard is already in place since 2026-05-14; this plan adds the CI-runnable proof that it works. CONTEXT.md D-05 explicitly rejects `pre-commit run --all-files` in CI (no-ops without `.secrets-denylist`) and manual smoke tests (cost per operator-touch); a parametrized pytest is the only durable way to lock the behavior.

Output: One new test file. No source code changes; the guard script is the artifact under test, unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-public-repo-audit/07-CONTEXT.md
@.planning/phases/07-public-repo-audit/07-PATTERNS.md
@scripts/check_public_discipline.py
@tests/unit/test_score_math.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from scripts/check_public_discipline.py and PATTERNS.md. -->

From scripts/check_public_discipline.py (the module under test):
- Module path: `scripts.check_public_discipline` (importable as `scripts.check_public_discipline` because there is no `scripts/__init__.py` but `python -m scripts.check_public_discipline` works; importing as `import scripts.check_public_discipline` from a pytest run also works because the project's pytest discovery adds the repo root to sys.path).
- `DENYLIST: Path` (module constant at line 18) — the path the test patches.
- `_load_patterns() -> list[re.Pattern[str]]` — reads DENYLIST, returns case-insensitive compiled regexes.
- `_staged_content(path: str) -> str` — shells out to `git show :<path>`. The test patches THIS function because the fixture is NOT git-staged in a unit test, so the real `_staged_content` returns `""` and the content-match case would false-pass otherwise.
- `main(argv: list[str]) -> int` — return 0 when patterns empty OR no matches; return 1 when any pattern matches content or path.

Parametrize shape per D-06 and PATTERNS.md:
- Case ids: `"content-match"`, `"path-match"`.
- Case parameters: `body` (str), `path_suffix` (str), `expected` (int).
- Content-match case: body contains the fake term, path is harmless (e.g. `"ok.txt"`), expected = 1.
- Path-match case: body is harmless, path contains the fake term (e.g. `"fake-denylisted-term-for-test.txt"`), expected = 1.

Critical safety constraint (THR-02, D-05, CONTEXT.md `<specifics>`):
- The hiring company name MUST NOT appear in this test file.
- The placeholder term `"fake-denylisted-term-for-test"` is the recommended publishable fake. Use this verbatim.
- The acceptance-criteria safety grep is denylist-driven (loops over `.secrets-denylist` patterns) rather than typing the term literally; this keeps the live term out of operator shell history and out of this PLAN.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write tests/unit/test_check_public_discipline.py with content-match + path-match parametrized cases</name>
  <files>tests/unit/test_check_public_discipline.py</files>
  <read_first>
    - scripts/check_public_discipline.py (the module under test; specifically lines 18, 21-30, 33-41, 44-62)
    - tests/unit/test_score_math.py (analog for parametrize-style + small-helper + `from __future__ import annotations` import order; the new test file matches this shape)
    - .planning/phases/07-public-repo-audit/07-CONTEXT.md (D-05, D-06 govern the test contract; `<specifics>` block locks the fake-term placeholder)
    - .planning/phases/07-public-repo-audit/07-PATTERNS.md ("tests/unit/test_check_public_discipline.py" pattern assignment section; the composite tmp-file + monkeypatch fixture pattern is the implementation guide)
  </read_first>
  <action>
Create `tests/unit/test_check_public_discipline.py` with the following structure:

1. File header: `from __future__ import annotations` as line 1, then a blank line, then `import pytest` and `from pathlib import Path`. Then `import scripts.check_public_discipline as mod` (aliasing as `mod` matches the `monkeypatch.setattr(mod, "CONST", value)` pattern used in `tests/unit/test_calibration_runner.py`).

2. One module-level constant: `FAKE_TERM = "fake-denylisted-term-for-test"`. This is the publishable placeholder per CONTEXT.md `<specifics>` and D-05. The hiring company name MUST NOT appear anywhere in this file.

3. A `pytest.mark.parametrize` decorated test function `test_main_flags_violations`. The parametrize names string is `"case_id, body, path_suffix, expected"`. The case list has exactly two entries:
   - `("content-match", f"this file mentions {FAKE_TERM}\n", "ok.txt", 1)`
   - `("path-match", "harmless body\n", f"{FAKE_TERM}.txt", 1)`

4. Test function signature: `def test_main_flags_violations(case_id: str, body: str, path_suffix: str, expected: int, tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:`. All parameters annotated (project convention; matches `test_retry.py` shape and CONTEXT.md PATTERNS.md note about strict-mypy spirit even in tests).

5. Test body:
   - Build a tmp denylist: `fake_denylist = tmp_path / ".secrets-denylist"`, then `fake_denylist.write_text(FAKE_TERM + "\n", encoding="utf-8")`.
   - Patch the module constant: `monkeypatch.setattr(mod, "DENYLIST", fake_denylist)`.
   - Build the fixture file: `fixture = tmp_path / path_suffix`, then `fixture.write_text(body, encoding="utf-8")`.
   - CRITICAL: per PATTERNS.md note, `mod.main()` calls `_staged_content()` which shells `git show :<path>`. In a unit test the file is not staged so the real call returns `""` and the content-match case would false-pass. Patch this function: `monkeypatch.setattr(mod, "_staged_content", lambda p: body if p == str(fixture) else "")`.
   - Invoke: `result = mod.main([str(fixture)])`.
   - Assert: `assert result == expected`.
   - Optional but valuable: include a docstring on the test function naming D-06 ("Covers both branches of `if pat.search(content) or pat.search(path)` at scripts/check_public_discipline.py:53.") so a future reader knows which lines the test is locking.

6. Add a second test function `test_main_returns_zero_when_no_denylist_terms_match` to lock the happy path (no match -> 0). Same fixture build pattern but with body = `"clean content\n"` and path_suffix = `"clean.txt"`. Assert `mod.main([str(fixture)]) == 0`. This guards against a future regression where a code change accidentally always-flags. (Strictly D-06 says the two parametrized cases are the test gap; this third case is cheap insurance against false-positive regressions and matches the project convention of "err on the side of too many tests rather than too few" per CLAUDE.md.)

7. Do NOT test the missing-denylist no-op branch. Per D-06, that branch is "acceptable as untested (it returns 0 silently by design)." Adding a test would re-open a debate that CONTEXT.md already closed.

Constraints:
- Every test parameter and helper annotated.
- No emojis. No em-dashes.
- The fake term `fake-denylisted-term-for-test` is the ONLY denylist-like string in the file. Verify by reading the final file before commit. The denylist-driven acceptance grep below is the programmatic gate.
- File is independent of `.secrets-denylist`; runs identically in a fresh clone with no `.secrets-denylist` present (CI is exactly this environment).
- `tests/` is exempt from `ARG` ruff rule per `pyproject.toml:53-54`, so an unused `case_id` parameter is acceptable; keep it as a readable test-id rather than an `_` placeholder.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_check_public_discipline.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: file `tests/unit/test_check_public_discipline.py` exists.
    - Source: file starts with `from __future__ import annotations` on line 1.
    - Source: file contains `import scripts.check_public_discipline as mod`.
    - Source: file contains the literal `FAKE_TERM = "fake-denylisted-term-for-test"`.
    - Source: file contains `@pytest.mark.parametrize` decorator with the case-id string `"case_id, body, path_suffix, expected"`.
    - Source: file contains exactly the two parametrized case tuples `"content-match"` and `"path-match"` (D-06 lock).
    - Source: file contains `monkeypatch.setattr(mod, "DENYLIST"` (DENYLIST patch present).
    - Source: file contains `monkeypatch.setattr(mod, "_staged_content"` (the load-bearing _staged_content patch flagged in PATTERNS.md).
    - Source: file contains a happy-path test asserting `mod.main([...]) == 0`.
    - Source assertion (safety, denylist-driven per W3): the following shell loop must exit 0 (no denylist term found in the test source file):
      ```bash
      while IFS= read -r pat; do
        [ -z "$pat" ] && continue
        case "$pat" in \#*) continue ;; esac
        grep -i -- "$pat" tests/unit/test_check_public_discipline.py && exit 1
      done < .secrets-denylist
      ```
      This loop reads patterns from `.secrets-denylist` rather than typing the live term in the acceptance criteria, keeping the live term out of operator shell history and PLAN.md. If `.secrets-denylist` is missing locally the check is a silent no-op pass; defense in depth lives in the pre-commit hook + the in-test FAKE_TERM constraint.
    - Test command: `uv run pytest tests/unit/test_check_public_discipline.py -v` exits 0; pytest reports 3 passing tests (2 parametrized cases + 1 happy-path test).
    - Test command: `uv run pytest tests/unit/test_check_public_discipline.py -v --collect-only` shows the test ids include `[content-match]` and `[path-match]`.
    - Test command: `uv run pytest -m "not smoke"` (the full offline suite) exits 0 with the new tests included.
    - Source assertion: file contains no fenced code blocks (it is a .py file; this is implicit; the check is that the file is valid Python — `uv run python -c "import tests.unit.test_check_public_discipline"` runs without SyntaxError).
  </acceptance_criteria>
  <done>
The two parametrized cases lock the content-match and path-match branches of the pre-commit guard. The happy-path test locks the zero-violation behavior. The file contains no reference to the hiring company name. The full offline test suite remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test source -> public repo | The test file is committed to the public repository. Any string literal in it becomes searchable in the published repo. The hiring company name MUST therefore never appear in this file, including in comments, docstrings, parametrize ids, or test-name strings. |
| test runtime -> filesystem | The test creates a tmp `.secrets-denylist` under `tmp_path` (pytest-managed; auto-cleaned). It does NOT read or write the real local `.secrets-denylist`. |
| test runtime -> git subprocess | `mod.main()` would normally subprocess to `git show :<path>`. The test patches `_staged_content` to bypass the subprocess entirely (the fixture is not git-staged in a unit test). No real git operations occur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 (THR-02 in CONTEXT.md) | Information Disclosure | test source file | mitigate | The fake term `"fake-denylisted-term-for-test"` is the only denylist-like string in the file. Acceptance criterion loops over `.secrets-denylist` and greps each pattern against the test file (denylist-driven per W3 in the revision context); zero hits required. The pre-commit guard itself would also block this — defense in depth. |
| T-07-06 | Tampering | DENYLIST patching | mitigate | The test patches `mod.DENYLIST` to `tmp_path / ".secrets-denylist"`. The patch is scoped to the test (monkeypatch auto-undo) and never touches the real local denylist or anything outside `tmp_path`. |
| T-07-07 | Repudiation | test results vs reality | accept | The test exercises the in-process `main()` function rather than the actual pre-commit invocation. The pre-commit hook itself shells `python scripts/check_public_discipline.py`, which calls the same `main(sys.argv[1:])` the test exercises. The equivalence is good enough; a full end-to-end pre-commit-run test would need `.secrets-denylist` in CI, which CONTEXT.md D-05 explicitly rejects. |

</threat_model>

<verification>
- `uv run pytest tests/unit/test_check_public_discipline.py -v` reports 3 passing tests.
- `uv run pytest -m "not smoke"` (full offline suite) exits 0.
- Denylist-driven safety grep loop (per W3) exits 0 against `tests/unit/test_check_public_discipline.py`.
- `uv run ruff check tests/unit/test_check_public_discipline.py` exits 0.
- `uv run black --check tests/unit/test_check_public_discipline.py` exits 0.
</verification>

<success_criteria>
The pre-commit guard now has CI-runnable proof that its content-match and path-match branches both fire. The test is independent of the real local `.secrets-denylist` (CI environment safe). The hiring company name appears nowhere in the test file. REPO-04's "proof the guard works" half is closed.
</success_criteria>

<output>
Create `.planning/phases/07-public-repo-audit/07-02-SUMMARY.md` when done, recording: the test file path, the three test names, the parametrize ids, and confirmation that the full offline suite remains green.
</output>

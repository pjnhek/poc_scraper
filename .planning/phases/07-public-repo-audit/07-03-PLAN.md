---
phase: 07-public-repo-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - .planning/phases/07-public-repo-audit/07-FINDINGS.md
  - .planning/REQUIREMENTS.md
  - README.md
autonomous: true
requirements:
  - REPO-01
  - REPO-03
  - REPO-04

must_haves:
  truths:
    - "Phase 7 ships a verdict artifact at .planning/phases/07-public-repo-audit/07-FINDINGS.md (D-10)."
    - "FINDINGS.md records summary COUNTS only, never raw match text (D-11, THR-01)."
    - "FINDINGS.md opens with a per-requirement verdict table (REPO-01 / REPO-03 / REPO-04) with evidence links (D-12)."
    - "REPO-01, REPO-03, REPO-04 traceability table rows flip from Pending to Complete IN THE SAME COMMIT as FINDINGS.md (D-12 atomicity)."
    - "REPO-01, REPO-03, REPO-04 checkbox lines in REQUIREMENTS.md flip from `- [ ]` to `- [x]` in the same commit."
    - "FINDINGS.md documents the ../poc_scraper-FULL-BACKUP.bundle retention policy (D-14, D-15) and a brief re-scrub procedure pointer (D-16)."
    - "FINDINGS.md cites Phase 1 findings.md lines 288-294 (OQ2 history-rewrite rationale) and PROJECT.md Key Decisions 2026-05-14 rows."
    - "README.md gains a 'Local setup' subsection at the END of the Run it section (immediately before `### Picking models`) explaining how to recreate .secrets-denylist on a fresh clone (D-09, THR-03)."
    - "Neither FINDINGS.md, README.md, nor the new commit message contains the hiring company name (THR-01, THR-03)."
    - "Phase 7 stays internal to .planning/; Phase 8 README adds no link to FINDINGS.md (D-13). The Local setup subsection is the only README change in this phase."
  artifacts:
    - path: ".planning/phases/07-public-repo-audit/07-FINDINGS.md"
      provides: "Phase 7 verdict artifact: per-requirement table, grep summary, guard confirmation, bundle policy, re-scrub pointer"
      contains: "Per-Requirement Verdict Table"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability flips: REPO-01/03/04 -> Complete"
      contains: "| REPO-01 | Phase 7 | Complete |"
    - path: "README.md"
      provides: "Local setup section documenting .secrets-denylist recreation"
      contains: "Local setup"
  key_links:
    - from: ".planning/phases/07-public-repo-audit/07-FINDINGS.md"
      to: "scripts/verify_public_repo.py"
      via: "REPO-01 evidence row links to the script + summary line + commit SHA"
      pattern: "scripts/verify_public_repo.py"
    - from: ".planning/phases/07-public-repo-audit/07-FINDINGS.md"
      to: "tests/unit/test_check_public_discipline.py"
      via: "REPO-04 evidence row links to the unit test"
      pattern: "tests/unit/test_check_public_discipline.py"
    - from: ".planning/phases/07-public-repo-audit/07-FINDINGS.md"
      to: ".planning/phases/audit/findings.md"
      via: "REPO-03 evidence row cites OQ2 rationale at lines 288-294"
      pattern: "findings.md"
    - from: ".planning/phases/07-public-repo-audit/07-FINDINGS.md"
      to: ".planning/PROJECT.md"
      via: "REPO-03 evidence row cites the 2026-05-14 Key Decisions rows"
      pattern: "PROJECT.md"
---

<objective>
Close Phase 7 atomically. Author `07-FINDINGS.md` (the phase verdict per D-10/D-11/D-12), flip REPO-01/03/04 from Pending to Complete in `.planning/REQUIREMENTS.md` (both the checkbox lines at 54-56 AND the traceability table rows at 125-128), and add a "Local setup" subsection to `README.md` documenting how to recreate `.secrets-denylist` on a fresh clone (D-09). All three file edits land in a SINGLE commit per D-12 atomicity.

This plan depends on Plans 01 and 02 because:
- FINDINGS.md's REPO-01 evidence row cites the output of `make verify-public-repo` (the script created in 07-01) — the operator runs the script first and records the count + SHA.
- FINDINGS.md's REPO-04 evidence row cites the unit-test path `tests/unit/test_check_public_discipline.py` (created in 07-02).

Purpose: Make the audit auditable. A reader of the public repo can open FINDINGS.md and see, at a glance, what was verified, by what evidence, against which requirements — without ever seeing the hiring company name. The atomic commit ensures the requirements table never lies about Phase 7 status.

Output: Three file edits (one new, two modified) committed as one unit per D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-public-repo-audit/07-CONTEXT.md
@.planning/phases/07-public-repo-audit/07-PATTERNS.md
@.planning/phases/audit/findings.md
@.planning/REQUIREMENTS.md
@README.md
@scripts/verify_public_repo.py
@tests/unit/test_check_public_discipline.py

<interfaces>
<!-- Key contracts extracted from PATTERNS.md and the analog Phase 1 findings.md. -->

FINDINGS.md required sections (per D-10/D-11/D-12/D-14/D-15/D-16, planner discretion on ordering — recommended in this order):

1. Header block (4 lines): title `# Phase 7 Public-Repo Audit Findings`, **Audit date:** 2026-05-23, **Auditor:** Claude, **Phase context:** Phase 7 of 8 (Public-Repo Audit), **Requirements:** REPO-01 (no hiring company name in content, paths, or reachable history), REPO-03 (deny-list grep with rewrite-vs-document decision), REPO-04 (pre-commit hook blocks the name).

2. `## 1. Per-Requirement Verdict Table (D-12)` — three rows, one per requirement, with Verdict = Complete and an Evidence cell that links to the proof. Locked by D-12 at the top of the file.

3. `## 2. Grep-Pass Summary (D-11)` — summary line of the `make verify-public-repo` run: counts + SHA. No raw matches. Operator runs `make verify-public-repo` at execution time and pastes the single stdout line.

4. `## 3. Pre-Commit Guard Confirmation (REPO-04)` — pointer to `.pre-commit-config.yaml:21-27` (the local-repo public-repo-discipline hook block, confirmed against the file as of 2026-05-23), to `scripts/check_public_discipline.py`, and to `tests/unit/test_check_public_discipline.py` as the CI-runnable proof.

5. `## 4. Forensic Bundle Retention Policy (D-14, D-15)` — names `../poc_scraper-FULL-BACKUP.bundle`, its purpose (pre-rewrite history preserved 2026-05-14), retention rule ("safe to delete after the Phase 8 Loom is recorded; never push to a remote; offline-only by design").

6. `## 5. Re-Scrub Procedure Pointer (D-16)` — one paragraph: future-leak procedure is `git filter-repo --replace-text <patterns>`, force-push, reset working repo, delete stale branches, take a fresh `../poc_scraper-FULL-BACKUP.bundle`. The original 2026-05-14 procedure followed this pattern; see PROJECT.md Key Decisions for the timeline.

7. `## 6. Timeline Pointer to PROJECT.md (2026-05-14 rows)` — names the three load-bearing Key Decisions rows: "Phase 7 narrowed from broad vertical/vendor/synthetic scrub to a hiring-company-name-only audit", "History rewritten and force-pushed to purge the hiring company name", "Pre-commit company-name guard added in lieu of detect-secrets/gitleaks".

8. `## Audit Sign-off` — closing section mirroring `.planning/phases/audit/findings.md:370-378`. Lists the four ROADMAP Phase 7 success criteria (lines 204-209) and maps each to the FINDINGS.md evidence that satisfies it.

Per-requirement verdict table shape (locked by D-12; planner authors the exact links):

| Requirement | Verdict | Evidence |
|-------------|---------|----------|
| REPO-01 | Complete | `make verify-public-repo` run 2026-05-23 at commit `<SHA>`: 0 hits in tracked content, 0 hits in history reachable from any ref. Source: `scripts/verify_public_repo.py`. |
| REPO-03 | Complete | History rewritten via `git filter-repo` on 2026-05-14, force-pushed. Pre-rewrite preserved offline at `../poc_scraper-FULL-BACKUP.bundle`. Decision recorded at `.planning/PROJECT.md` Key Decisions row 2026-05-14. Rationale at `.planning/phases/audit/findings.md` lines 288-294 (OQ2). |
| REPO-04 | Complete | Pre-commit hook at `.pre-commit-config.yaml:21-27` invokes `scripts/check_public_discipline.py`. Content-match and path-match branches covered by `tests/unit/test_check_public_discipline.py`. |

REQUIREMENTS.md edits (per D-12, in the SAME commit as FINDINGS.md):
- Lines 54-56: flip `- [ ] **REPO-01**`, `- [ ] **REPO-03**`, `- [ ] **REPO-04**` to `- [x] **REPO-01**`, `- [x] **REPO-03**`, `- [x] **REPO-04**`.
- Lines 125, 127, 128: flip `| REPO-01 | Phase 7 | Pending |`, `| REPO-03 | Phase 7 | Pending |`, `| REPO-04 | Phase 7 | Pending |` to `| REPO-01 | Phase 7 | Complete |`, `| REPO-03 | Phase 7 | Complete |`, `| REPO-04 | Phase 7 | Complete |`.
- Line 126 (REPO-02) is UNCHANGED: stays `| REPO-02 | Phase 7 | Withdrawn (scope narrowed 2026-05-14) |`.

README.md edit (per D-09 + W2 from the revision context):
- Add a new H3 `### Local setup` subsection at the END of the `## Run it` content, AFTER the `RUN_LIMIT` fenced bash block (currently lines 104-106) and immediately BEFORE the existing `### Picking models` heading (currently line 108). The new heading therefore sits at the SAME outline level as `### Picking models`, both as siblings under `## Run it`. This placement preserves the markdown outline: the conversational `make run` paragraph (currently line 100) and the `RUN_LIMIT` block stay as direct children of `## Run it`, NOT as children of `### Local setup`.
- Content: a short prose paragraph naming `.secrets-denylist`, noting it is gitignored by design, explaining the one-regex-per-line format, naming both consumers (the pre-commit hook `scripts/check_public_discipline.py` and the verification script `make verify-public-repo`).
- The sensitive term itself is NEVER quoted. The README refers to "denylist patterns" or "the local denylist file" generically.
- No em-dashes (CLAUDE.md rule; README is published markdown). Use commas, parentheses, or rewrite.

Constraints applied to ALL files in this plan:
- No emojis anywhere.
- No em-dashes in README.md (published markdown). FINDINGS.md is internal but planner discretion: stay em-dash-free for consistency.
- The hiring company name MUST NOT appear in FINDINGS.md, README.md, the commit message, or any new artifact (THR-01, THR-03, D-11). Safety greps are denylist-driven (loop over `.secrets-denylist` patterns) so the live term never appears in PLAN.md or operator shell history.
- The atomic commit for D-12 includes ALL THREE files. Commit message in conventional-commits format with scope `(07)` per recent git log.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run verify-public-repo and capture the summary line for FINDINGS.md</name>
  <files>(no file edit; this task captures runtime evidence for Task 2)</files>
  <read_first>
    - scripts/verify_public_repo.py (the script created in Plan 07-01; confirm it exists and is importable before running)
    - .planning/phases/07-public-repo-audit/07-CONTEXT.md (D-11 — only counts + SHA may be quoted; raw matches stay local)
  </read_first>
  <action>
Run `make verify-public-repo` from the repo root. Capture the single stdout summary line (format: `verify-public-repo: <N> hits in tracked content, <M> hits in history reachable from any ref (commit <SHA>).`).

Also capture the current short SHA via `git rev-parse --short HEAD` for the FINDINGS.md REPO-01 evidence row.

If the script exits 0 (clean): record the literal stdout line for use in Task 2.
If the script exits 1 (hits found): STOP. Do not write FINDINGS.md claiming clean state. Report the non-zero exit to the operator with the message "verify-public-repo found hits; Phase 7 cannot close until the repo is re-scrubbed. Consult D-16 re-scrub procedure pointer." Then halt this plan and surface the issue.
If the script exits 2 (denylist missing): STOP. Report "verify-public-repo: .secrets-denylist missing locally; recreate it (one regex per line) before re-running. See README Local setup (will be added in Task 4)." Halt this plan.

No file is created in this task; the captured summary line and SHA are inputs to Task 2.

Note for executor: per D-11 and THR-01, do NOT paste any raw match text into FINDINGS.md or any planning artifact. The script is designed to not emit any in the first place; this task respects that contract.
  </action>
  <verify>
    <automated>make verify-public-repo</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `make verify-public-repo` exits 0 (clean state required to proceed). If non-zero, the plan halts here.
    - CLI: stdout contains exactly one line matching the regex `verify-public-repo: [0-9]+ hits in tracked content, [0-9]+ hits in history reachable from any ref \(commit [0-9a-f]+\)\.`.
    - The captured summary line is recorded in the executor's working notes (NOT in any committed file) for use in Task 2.
    - CLI: `git rev-parse --short HEAD` returns a non-empty SHA which will be quoted in FINDINGS.md REPO-01 evidence row.
  </acceptance_criteria>
  <done>
The summary line and SHA are captured. The exit code is 0 (clean). Task 2 can proceed to author FINDINGS.md citing this evidence.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author .planning/phases/07-public-repo-audit/07-FINDINGS.md</name>
  <files>.planning/phases/07-public-repo-audit/07-FINDINGS.md</files>
  <read_first>
    - .planning/phases/audit/findings.md (the binding analog per D-10; specifically lines 1-6 header, lines 8/22/31 section headers, lines 35-89 evidence rows, lines 288-294 OQ2 history-rewrite rationale to cite, lines 370-378 closing sign-off)
    - .planning/PROJECT.md (Key Decisions section; the three 2026-05-14 rows to cite in section 6)
    - .planning/ROADMAP.md (Phase 7 section, lines 199-211; the four success criteria to map in the sign-off section)
    - .planning/REQUIREMENTS.md (REPO-01 / REPO-03 / REPO-04 wording; the table rows being flipped in Task 3)
    - .planning/phases/07-public-repo-audit/07-CONTEXT.md (D-10 through D-17)
    - .planning/phases/07-public-repo-audit/07-PATTERNS.md ("07-FINDINGS.md" pattern assignment section; the section ordering template and the per-requirement verdict-table shape)
    - .pre-commit-config.yaml (confirm the local public-repo-discipline hook block sits at lines 21-27 as expected; per W1 in revision context, prior PLAN drafts said lines 17-23 in error)
  </read_first>
  <action>
Create `.planning/phases/07-public-repo-audit/07-FINDINGS.md` with the section structure named in `<interfaces>` above. Specific authoring rules:

1. Header block: exactly 5 lines (title + 4 bold-key metadata lines). Match the Phase 1 analog at `.planning/phases/audit/findings.md:1-6` shape but with Phase 7 substituted.

2. Section 1 (Per-Requirement Verdict Table): table with columns `Requirement | Verdict | Evidence` and three data rows (REPO-01, REPO-03, REPO-04). Use the exact evidence-cell wording from `<interfaces>` above, substituting the captured short SHA from Task 1 for `<SHA>` in the REPO-01 row. The REPO-04 evidence cell quotes the pre-commit hook line range as `.pre-commit-config.yaml:21-27` (NOT `:17-23`; per W1 the local public-repo-discipline block is the fifth `repos:` entry, sitting at lines 21-27 in the current file; lines 16-20 are the ruff-pre-commit block).

3. Section 2 (Grep-Pass Summary): quote the literal stdout line captured in Task 1 inside a single backtick-fenced block. Add one sentence of prose framing: "The above line is the output of `make verify-public-repo`, defined at `scripts/verify_public_repo.py`. Per D-11, only summary counts are recorded here. If a future re-run yields a non-zero count, the script's stderr will name the count; the matching content stays local."

4. Section 3 (Pre-Commit Guard Confirmation): three sub-bullets:
   - `Hook wired at .pre-commit-config.yaml:21-27 (local repo, language: system).` (Per W1: lines 21-27, NOT 17-23.)
   - `Guard script at scripts/check_public_discipline.py; reads patterns from .secrets-denylist (gitignored).`
   - `Branch coverage at tests/unit/test_check_public_discipline.py (content-match and path-match parametrized cases per D-06).`

5. Section 4 (Forensic Bundle Retention Policy): one paragraph naming `../poc_scraper-FULL-BACKUP.bundle`, its purpose (pre-rewrite history forensic preservation, captured 2026-05-14), its location (sibling directory to the repo root, NOT inside the repo), and the retention rule from D-15 verbatim: "safe to delete after the Phase 8 Loom is recorded; never push to a remote; offline-only by design." Per D-17, also state that `verify_public_repo.py` does NOT scan the bundle; its scan boundary is the current repo + its git history only.

6. Section 5 (Re-Scrub Procedure Pointer): one paragraph per D-16. Verbatim: "If a future leak is discovered, the re-scrub procedure is `git filter-repo --replace-text <patterns>`, force-push, reset the working repo, delete stale branches, and take a fresh `../poc_scraper-FULL-BACKUP.bundle`. The original 2026-05-14 procedure followed this pattern; see `.planning/PROJECT.md` Key Decisions for the timeline."

7. Section 6 (Timeline Pointer): bullet list of the three load-bearing PROJECT.md 2026-05-14 Key Decisions rows (verbatim row labels from `<interfaces>`).

8. Audit Sign-off section: closing section dated 2026-05-23. Map each of the four Phase 7 success criteria (from `.planning/ROADMAP.md` lines 204-209) to the evidence in this document. Mirror the Phase 1 analog shape at `.planning/phases/audit/findings.md:370-378`.

Constraints:
- No emojis.
- Stay em-dash-free for consistency with the README change landing in the same commit (planner choice per PATTERNS.md: "Recommendation: stay em-dash-free for consistency").
- The hiring company name MUST NOT appear in this file. The pre-commit guard will block it; the denylist-driven safety grep in acceptance is the programmatic gate.
- File should be 60-120 lines (estimate; Phase 1 findings.md is 378 lines but covered far more ground — Phase 7 is verification, not gap enumeration).
- Every claim has a `file:line` or path link per the Phase 1 analog convention.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-public-repo-audit/07-FINDINGS.md && wc -l .planning/phases/07-public-repo-audit/07-FINDINGS.md | awk '{print $1 >= 40}'</automated>
  </verify>
  <acceptance_criteria>
    - Source: file `.planning/phases/07-public-repo-audit/07-FINDINGS.md` exists.
    - Source: file is at least 40 lines (lower bound; planner estimate is 60-120 lines).
    - Source: line 1 is `# Phase 7 Public-Repo Audit Findings` (exact).
    - Source: file contains all six section headers: `## 1. Per-Requirement Verdict Table`, `## 2. Grep-Pass Summary`, `## 3. Pre-Commit Guard Confirmation`, `## 4. Forensic Bundle Retention Policy`, `## 5. Re-Scrub Procedure Pointer`, `## 6. Timeline Pointer`.
    - Source: file contains an `## Audit Sign-off` closing section.
    - Source: file contains the literal `| REPO-01 | Complete |` (table row in section 1).
    - Source: file contains the literal `| REPO-03 | Complete |` (table row in section 1).
    - Source: file contains the literal `| REPO-04 | Complete |` (table row in section 1).
    - Source: file contains the substring `scripts/verify_public_repo.py` (REPO-01 evidence link).
    - Source: file contains the substring `tests/unit/test_check_public_discipline.py` (REPO-04 evidence link).
    - Source: file contains the substring `.pre-commit-config.yaml:21-27` (per W1; the correct line range for the local public-repo-discipline hook).
    - Source: file contains the substring `findings.md` and references the OQ2 line range (e.g., `lines 288-294`) for the REPO-03 rationale link.
    - Source: file contains the substring `poc_scraper-FULL-BACKUP.bundle` (section 4 bundle policy).
    - Source: file contains the literal phrase `never push to a remote` (D-15 retention rule).
    - Source: file contains the substring `git filter-repo` (section 5 re-scrub pointer).
    - Source: file does NOT contain any emoji (visual grep; check the executor's diff).
    - Source assertion (em-dash check, per filtered out comments): `grep -v '^#' .planning/phases/07-public-repo-audit/07-FINDINGS.md | grep -c '—'` returns 0. (`.md` has no `#` comment syntax — `#` is a heading marker — but the filter is a no-op cost and matches the planner-antipatterns header-hygiene rule.)
    - Source assertion (hiring-name safety, THR-01, denylist-driven per W3): the following shell loop must exit 0:
      ```bash
      while IFS= read -r pat; do
        [ -z "$pat" ] && continue
        case "$pat" in \#*) continue ;; esac
        grep -i -- "$pat" .planning/phases/07-public-repo-audit/07-FINDINGS.md && exit 1
      done < .secrets-denylist
      ```
    - Behavior: the verify summary line quoted in section 2 matches the regex `verify-public-repo: [0-9]+ hits in tracked content, [0-9]+ hits in history reachable from any ref \(commit [0-9a-f]+\)\.`.
  </acceptance_criteria>
  <done>
FINDINGS.md exists with all six locked sections and the closing sign-off. The per-requirement table opens the file. No emoji, no em-dash, no hiring company name. Evidence rows link to live files.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Flip REPO-01/03/04 to Complete in .planning/REQUIREMENTS.md (checkboxes + traceability table)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (specifically lines 54-56 checkboxes and lines 125-128 traceability table; the analog Complete rows at lines 110-124 confirm the exact target shape)
    - .planning/phases/07-public-repo-audit/07-PATTERNS.md (REQUIREMENTS.md pattern assignment section locks both the table-row shape and the checkbox-flip shape with the EVAL-/NARR-/HARD-/POLISH- precedent)
  </read_first>
  <action>
Two specific edits in `.planning/REQUIREMENTS.md`:

1. Checkbox flips (lines 54-56):
   - Change `- [ ] **REPO-01**:` to `- [x] **REPO-01**:` (rest of the line unchanged).
   - Change `- [ ] **REPO-03**:` to `- [x] **REPO-03**:` (rest of the line unchanged).
   - Change `- [ ] **REPO-04**:` to `- [x] **REPO-04**:` (rest of the line unchanged).

2. Traceability table flips:
   - Line 125: change `| REPO-01 | Phase 7 | Pending |` to `| REPO-01 | Phase 7 | Complete |`.
   - Line 127: change `| REPO-03 | Phase 7 | Pending |` to `| REPO-03 | Phase 7 | Complete |`.
   - Line 128: change `| REPO-04 | Phase 7 | Pending |` to `| REPO-04 | Phase 7 | Complete |`.

Do NOT touch:
- Line 126: `| REPO-02 | Phase 7 | Withdrawn (scope narrowed 2026-05-14) |` is UNCHANGED. This row stays Withdrawn per the 2026-05-14 scope narrowing recorded in PROJECT.md.
- Any DEMO-* row at lines 129-131. Phase 8 owns those.
- The rest of the file.

Per D-12 atomicity, these edits land in the SAME commit as the FINDINGS.md creation (Task 2) and the README edit (Task 4). Do NOT commit separately. The commit is created in Task 5.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*REPO-0[134]\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/REQUIREMENTS.md` line 54 (or wherever REPO-01 checkbox lives) starts with `- [x] **REPO-01**:`.
    - Source: `.planning/REQUIREMENTS.md` line 55 (or wherever REPO-03 checkbox lives) starts with `- [x] **REPO-03**:`.
    - Source: `.planning/REQUIREMENTS.md` line 56 (or wherever REPO-04 checkbox lives) starts with `- [x] **REPO-04**:`.
    - Source: `grep -c '^- \[x\] \*\*REPO-0[134]\*\*' .planning/REQUIREMENTS.md` returns 3.
    - Source: file contains the literal line `| REPO-01 | Phase 7 | Complete |`.
    - Source: file contains the literal line `| REPO-03 | Phase 7 | Complete |`.
    - Source: file contains the literal line `| REPO-04 | Phase 7 | Complete |`.
    - Source: file contains the literal line `| REPO-02 | Phase 7 | Withdrawn (scope narrowed 2026-05-14) |` (UNCHANGED — REPO-02 stays Withdrawn).
    - Source: file does NOT contain any line matching `| REPO-0[134] | Phase 7 | Pending |` (all three Pending rows are gone).
  </acceptance_criteria>
  <done>
Both the checkbox section and the traceability table reflect REPO-01/03/04 as Complete. REPO-02 stays Withdrawn. No other row is touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add the "Local setup" subsection to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 82-115 (the existing "Run it" section and surrounding prose; specifically the install bash code block at lines 84-98, the conversational paragraph at line 100, the `RUN_LIMIT` bash block at lines 104-106, and the existing `### Picking models` heading at line 108)
    - .planning/phases/07-public-repo-audit/07-PATTERNS.md (README.md pattern assignment section; the recommended placement is a new H3 nested under `## Run it`; the published-markdown em-dash rule is locked)
    - .planning/phases/07-public-repo-audit/07-CONTEXT.md (D-09 wording constraints: discoverable, brief, no exposure of the sensitive term)
  </read_first>
  <action>
Add a new H3 subsection `### Local setup` to `README.md`, placed at the END of the `## Run it` content. Specifically: AFTER the `RUN_LIMIT=5 make run` fenced bash block (currently lines 104-106) AND its blank-line separator, and IMMEDIATELY BEFORE the existing `### Picking models` heading (currently line 108). The new `### Local setup` is therefore a SIBLING subsection of `### Picking models` under `## Run it`, NOT a parent of the conversational `make run` paragraph or the `RUN_LIMIT` block.

Why this placement (per W2 in the revision context): if `### Local setup` were inserted between the install bash block (line 98) and the conversational `make run` paragraph (line 100), markdown's outline semantics would make the conversational paragraph AND the `RUN_LIMIT` block read as children of `### Local setup`, which is wrong (they belong to `## Run it`). Placing `### Local setup` after the `RUN_LIMIT` block keeps the outline correct: `## Run it` directly owns the install steps, the conversational paragraph, and the `RUN_LIMIT` block, then `### Local setup` and `### Picking models` are two sibling subsections at the end.

Subsection content (planner's recommended wording; the executor may refine but must keep all constraints):

```
### Local setup

If you want the public-repo guard active locally (recommended for any contributor pushing to the public repo), create `.secrets-denylist` at the repo root. The file is gitignored by design so the sensitive terms never enter version control. Add one regex per line; lines beginning with `#` are comments. Both the pre-commit hook (`scripts/check_public_discipline.py`) and the verification script (`make verify-public-repo`) read from this file. Without `.secrets-denylist`, the pre-commit hook is silent by design (so a fresh clone can commit), but `make verify-public-repo` exits non-zero with a setup prompt.
```

Constraints:
- No em-dashes (CLAUDE.md rule for published markdown). The draft above uses commas and parentheses only. Verify the executor's final text honors this. Per I4, the em-dash check is programmatic: compare em-dash count before and after the edit (must be equal — no new em-dashes introduced).
- No emojis.
- The sensitive term itself is NEVER named. The README refers to "the sensitive terms" and "denylist patterns" generically.
- No mention of the hiring company name, the bundle, or PROJECT.md (this is operator-facing public docs, not the audit trail).
- One paragraph; do NOT add example regex content, do NOT add a code block (per CONTEXT.md D-09 "discoverable, brief, no exposure of the sensitive term").
- Section heading is exactly `### Local setup` (three hashes, single space, title-case-first-word).

Per CONTEXT.md `<deferred>`: this is the ONLY README change Phase 7 makes. Do NOT touch the README "What this is" / "What's next" / "Picking models" sections. Phase 8 owns the broader README refresh per DEMO-02 and DEMO-03.
  </action>
  <verify>
    <automated>grep -A 1 '^### Local setup' README.md | grep -q '.secrets-denylist' && before=$(git show HEAD:README.md | grep -c '—'); after=$(grep -c '—' README.md); test "$before" -eq "$after"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `README.md` contains the literal heading line `### Local setup`.
    - Source: the paragraph immediately after `### Local setup` contains the substring `.secrets-denylist`.
    - Source: the paragraph contains the substring `scripts/check_public_discipline.py`.
    - Source: the paragraph contains the substring `make verify-public-repo`.
    - Source: the paragraph contains the word `gitignored` (D-09 framing).
    - Source assertion (em-dash conservation, per I4 in revision context): the README must not introduce new em-dashes. Verify with:
      ```bash
      before=$(git show HEAD:README.md | grep -c '—')
      after=$(grep -c '—' README.md)
      test "$before" -eq "$after"
      ```
      Exits 0 only when the diff added zero em-dashes (pre-existing em-dashes are tolerated; Phase 8 may scrub them, per D-13 narrow framing).
    - Source assertion (sensitive-term safety, THR-03, denylist-driven per W3): the following shell loop must exit 0 against `README.md`:
      ```bash
      while IFS= read -r pat; do
        [ -z "$pat" ] && continue
        case "$pat" in \#*) continue ;; esac
        grep -i -- "$pat" README.md && exit 1
      done < .secrets-denylist
      ```
    - Source (placement, per W2): the `### Local setup` heading appears AFTER the `RUN_LIMIT` fenced bash block (which closes around line 106 currently) AND IMMEDIATELY BEFORE `### Picking models` (currently line 108). Verify with `awk` or a quick line-number check: the line number of `### Local setup` is GREATER THAN the line number of `RUN_LIMIT=5 make run`, AND LESS THAN the line number of `### Picking models`. (One-liner: `awk '/^### Local setup/ {ls=NR} /^### Picking models/ {pm=NR} /RUN_LIMIT=5 make run/ {rl=NR} END {exit !(rl < ls && ls < pm)}' README.md`.)
    - Source: no other section of README.md was modified (the diff is one addition: the H3 heading + one paragraph).
  </acceptance_criteria>
  <done>
README.md has a `### Local setup` subsection at the end of `## Run it`, sibling-positioned with `### Picking models`. The conversational `make run` paragraph and the `RUN_LIMIT` block remain direct children of `## Run it`. No em-dashes added. No other README section touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Atomic commit of FINDINGS.md + REQUIREMENTS.md + README.md (D-12)</name>
  <files>
    .planning/phases/07-public-repo-audit/07-FINDINGS.md,
    .planning/REQUIREMENTS.md,
    README.md
  </files>
  <read_first>
    - .planning/phases/07-public-repo-audit/07-CONTEXT.md (D-12 locks atomicity: FINDINGS.md and the REQUIREMENTS.md status flips MUST land in ONE commit; conventional-commit shape per CLAUDE.md and recent git log)
    - .planning/phases/07-public-repo-audit/07-PATTERNS.md ("Conventional Commits (No Emojis)" shared pattern section; commit-message template lives there)
  </read_first>
  <action>
Stage exactly three files and commit them as one unit:

1. `.planning/phases/07-public-repo-audit/07-FINDINGS.md` (new, from Task 2)
2. `.planning/REQUIREMENTS.md` (modified, from Task 3)
3. `README.md` (modified, from Task 4)

Do NOT stage:
- `scripts/verify_public_repo.py` or the `Makefile` (those belong to Plan 07-01 and are committed there).
- `tests/unit/test_check_public_discipline.py` (belongs to Plan 07-02).
- `.planning/STATE.md` or any other planning bookkeeping (separate commit).

Critical: `.planning/` is gitignored at `.gitignore:16`, BUT select files inside (`REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, prior `*-SUMMARY.md` artifacts) are tracked via the historical force-add precedent. FINDINGS.md MUST follow that precedent because D-11 explicitly rejected a gitignored FINDINGS.md ("artifact no longer demonstrates Phase 7 completion to a public-repo reader"). Use `git add -f` for the FINDINGS.md path; regular `git add` for `.planning/REQUIREMENTS.md` (already tracked) and `README.md` (always tracked). Verify after staging with `git diff --cached --name-only`.

Concretely:
- `git add -f .planning/phases/07-public-repo-audit/07-FINDINGS.md`
- `git add .planning/REQUIREMENTS.md`
- `git add README.md`

Do NOT use `git add -A` or `git add .` (would pull in untracked PLAN.md / CONTEXT.md / PATTERNS.md / DISCUSSION-LOG.md files, contradicting D-13 "Phase 7 stays internal"). Stage by explicit path only, per CLAUDE.md safety rule.

Commit message in conventional-commits shape with `(07)` scope per recent git log (`feat(06-04): ...`, `docs(06-04): ...`):

```
docs(07): close REPO-01/03/04 with audit findings and traceability flip

Adds .planning/phases/07-public-repo-audit/07-FINDINGS.md recording the
Phase 7 verdict: per-requirement table, grep-pass summary (counts only,
no raw matches per D-11), pre-commit guard confirmation, bundle
retention policy, re-scrub procedure pointer, and timeline pointer to
PROJECT.md 2026-05-14 Key Decisions.

Flips REPO-01, REPO-03, REPO-04 from Pending to Complete in
.planning/REQUIREMENTS.md (both checkbox and traceability table) per
D-12 atomicity.

Adds a Local setup subsection to README.md documenting how to recreate
.secrets-denylist on a fresh clone (D-09).
```

The pre-commit hook (`scripts/check_public_discipline.py`) will run on the staged content. The hiring company name appears nowhere in any of the three files; the hook should pass. If it fails, STOP and investigate (likely a slip in FINDINGS.md or the README).

Per CLAUDE.md commit safety protocol: never `--no-verify`; if the hook fails, fix and create a NEW commit (do NOT amend). Use a heredoc for the commit message to preserve newlines (per the global guidance in this codebase).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^docs(07):' && test "$(git log -1 --name-only --pretty= | grep -v '^$' | sort)" = "$(printf '.planning/REQUIREMENTS.md\n.planning/phases/07-public-repo-audit/07-FINDINGS.md\nREADME.md\n' | sort)"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `git log -1 --pretty=%s` returns a string starting with `docs(07):`. Programmatic check: `git log -1 --pretty=%s | grep -q '^docs(07):'` exits 0.
    - CLI (per W5 — stronger gate than the prior `git log -1 --pretty=%s` smoke check): the HEAD commit touches EXACTLY the three expected files, no more, no fewer. Programmatic check:
      ```bash
      test "$(git log -1 --name-only --pretty= | grep -v '^$' | sort)" = "$(printf '.planning/REQUIREMENTS.md\n.planning/phases/07-public-repo-audit/07-FINDINGS.md\nREADME.md\n' | sort)"
      ```
      Exits 0 only when the HEAD commit's file list is exactly these three paths (sorted comparison; `grep -v '^$'` strips the trailing blank line `git log --name-only` emits).
    - CLI: `git status` reports a clean working tree on these three files (no unstaged changes leftover).
    - CLI: pre-commit hook ran and passed (the commit landed; if it failed, the commit would not have created).
    - Source: HEAD commit includes the FINDINGS.md creation, both REQUIREMENTS.md edits (checkboxes + table), and the README.md "Local setup" addition — verified via `git show --stat HEAD`.
    - Behavior (denylist-driven safety check per W3): the following shell loop must exit 0 across all three committed files:
      ```bash
      for f in .planning/phases/07-public-repo-audit/07-FINDINGS.md README.md .planning/REQUIREMENTS.md; do
        while IFS= read -r pat; do
          [ -z "$pat" ] && continue
          case "$pat" in \#*) continue ;; esac
          grep -i -- "$pat" "$f" && exit 1
        done < .secrets-denylist
      done
      ```
      Confirms THR-01 / THR-03: zero denylist hits across the three staged files.
  </acceptance_criteria>
  <done>
The three Phase 7 closing artifacts are committed atomically in a single conventional-commits-shaped commit. The traceability table now reflects REPO-01/03/04 as Complete. README has the new Local setup section. The pre-commit guard passed. The W5 stronger gate (`git log -1 --pretty=%s | grep -q '^docs(07):'` AND a sorted-name-list equality check) is satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FINDINGS.md -> public repo | FINDINGS.md is committed and visible to anyone reading the public repository. Per D-11 it records counts and links only; raw match text never enters this artifact. |
| README "Local setup" -> public repo | README is the most-read public artifact in the repo. The sensitive term itself is never named per D-09 and THR-03. The subsection explains the mechanism without exposing the data. |
| atomic commit -> requirements truth | Per D-12, the same commit that records FINDINGS.md must flip REQUIREMENTS.md. A non-atomic commit would create a temporary lie where REQUIREMENTS.md said Pending but FINDINGS.md claimed Complete (or vice versa). |
| verify-public-repo runtime -> FINDINGS.md content | The script's stdout summary line is quoted in FINDINGS.md section 2. The script is designed to never emit raw matches; FINDINGS.md inherits that property by quoting only the script's safe summary line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 (THR-01 in CONTEXT.md) | Information Disclosure | 07-FINDINGS.md content | mitigate | Section 2 quotes ONLY the verify-script's safe summary line (counts + SHA). No section quotes raw match text. Acceptance criterion uses a denylist-driven grep loop (per W3) to programmatically gate zero hits without typing the live term in the PLAN.md. The pre-commit hook is a second line of defense. |
| T-07-09 (THR-03 in CONTEXT.md) | Information Disclosure | README "Local setup" subsection | mitigate | The README subsection refers to "the sensitive terms" and "denylist patterns" generically; the hiring company name appears nowhere. Acceptance criterion gates via the same denylist-driven grep loop (per W3) and the em-dash conservation check (per I4). The pre-commit hook also blocks. |
| T-07-10 | Tampering | REQUIREMENTS.md atomic flip (D-12) | mitigate | All three files are staged via explicit `git add` paths in Task 5 and committed as one unit. A non-atomic commit (e.g., FINDINGS first, REQUIREMENTS later) would leave the repo in a state where the traceability table contradicts the findings artifact for any window between the two commits. The single-commit constraint closes that window. Per W5, the Task 5 `<automated>` block now ASSERTS the subject prefix AND the exact three-file set, not just runs `git log`. |
| T-07-11 | Tampering | accidental staging of out-of-plan files | mitigate | Per CLAUDE.md safety protocol, Task 5 uses explicit `git add <path>` for exactly three files (never `git add -A` or `git add .`). The verify script + Makefile (Plan 07-01) and the test file (Plan 07-02) belong to their own commits and must not be co-staged. The sorted-name-list equality check in Task 5 acceptance is the programmatic gate. |
| T-07-12 | Repudiation | future re-scrub decision | accept | FINDINGS.md section 5 records the re-scrub procedure pointer (D-16) so a future operator (or future-Claude) knows where to start if a leak surfaces post-Phase-8. Without this pointer, the institutional memory of the 2026-05-14 rewrite would have to be reconstructed from PROJECT.md alone. |

</threat_model>

<verification>
- `cat .planning/REQUIREMENTS.md | grep -E '^- \[[ x]\] \*\*REPO-0[134]\*\*'` shows three `- [x]` lines.
- `cat .planning/REQUIREMENTS.md | grep -E '^\| REPO-0[134] \| Phase 7 \|'` shows three `| Complete |` rows.
- `cat .planning/REQUIREMENTS.md | grep '^| REPO-02'` shows the unchanged Withdrawn row.
- `wc -l .planning/phases/07-public-repo-audit/07-FINDINGS.md` returns at least 40.
- `grep -c '^## ' .planning/phases/07-public-repo-audit/07-FINDINGS.md` returns at least 7 (six numbered sections + sign-off).
- `grep -n '### Local setup' README.md` returns exactly one match.
- Placement gate per W2: `awk '/^### Local setup/ {ls=NR} /^### Picking models/ {pm=NR} /RUN_LIMIT=5 make run/ {rl=NR} END {exit !(rl < ls && ls < pm)}' README.md` exits 0.
- Em-dash conservation per I4: `before=$(git show HEAD~1:README.md 2>/dev/null | grep -c '—'); after=$(grep -c '—' README.md); test "$before" -eq "$after"` exits 0 against the pre-commit baseline.
- W5 commit-shape gate: `git log -1 --pretty=%s | grep -q '^docs(07):'` AND the sorted-name-list equality check in Task 5 acceptance both exit 0.
</verification>

<success_criteria>
Phase 7 is closed atomically. A reader of the public repo can:
1. Open `.planning/REQUIREMENTS.md` and see REPO-01/03/04 marked Complete in both the checklist and the traceability table.
2. Open `.planning/phases/07-public-repo-audit/07-FINDINGS.md` and see the per-requirement evidence table, the grep-pass summary (counts only), the guard confirmation (pointing at `.pre-commit-config.yaml:21-27`), the bundle retention policy, the re-scrub pointer, and the timeline pointer.
3. Open `README.md` and find a discoverable "Local setup" subsection at the end of `## Run it`, sibling-positioned with `### Picking models`.

The hiring company name appears in none of these artifacts. The pre-commit guard passed on the commit. The repo is now provably ready for Phase 8 to record the Loom against a clean SHA, fulfilling the ROADMAP "Phase 7 -> Phase 8 (hard precedence; non-negotiable)" gating.
</success_criteria>

<output>
Create `.planning/phases/07-public-repo-audit/07-03-SUMMARY.md` when done, recording: the atomic commit SHA, the captured grep summary line (counts + SHA only — no raw matches), the three modified file paths, and confirmation that the pre-commit hook passed.
</output>

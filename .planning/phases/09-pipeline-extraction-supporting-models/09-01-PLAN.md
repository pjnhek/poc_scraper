---
phase: 09-pipeline-extraction-supporting-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline.py
  - tests/functional/test_pipeline_open_deps.py
autonomous: true
requirements: [INTEG-01]

must_haves:
  truths:
    - "open_deps(settings) is an async context manager yielding the existing Deps bundle (D-01); no new bundle type introduced"
    - "main() contains no inline httpx.AsyncClient / Replay* / Recording* construction block; it only opens `async with open_deps(settings) as deps:` (D-01..D-04)"
    - "The shared httpx.AsyncClient's lifetime is fully owned inside open_deps and is closed automatically when the async with block exits (D-02)"
    - "Pre-existing pipeline tests (test_pipeline.py, test_pipeline_failures.py, test_replay_pipeline.py) pass unmodified since build_deps/process_account/run_pipeline call sites are untouched"
  artifacts:
    - "src/pipeline.py (open_deps added, main() rewired)"
    - "tests/functional/test_pipeline_open_deps.py (new, 3 tests)"
  key_links:
    - "main() -> open_deps(settings) -> build_deps(...) -> Deps"
    - "settings.demo_bundle / settings.record_bundle branching moved verbatim into open_deps (D-03)"
---

<objective>
Extract the client-construction block currently inline in `pipeline.main()` into a reusable `open_deps(settings)` async context manager (INTEG-01), so both the CLI and the future MCP server tiers (Phase 10+) share exactly one wiring seam. `main()`'s observable behavior and the existing pipeline test suite must be unchanged.

Purpose: gives Phase 10's `src/mcp_server/wiring.py` a ready-made lifespan seam without duplicating replay/record/live client construction logic.
Output: `open_deps()` in `src/pipeline.py`, plus a new functional test proving all three construction modes (replay, record, live) and httpx client teardown.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md
@.planning/phases/09-pipeline-extraction-supporting-models/09-PATTERNS.md
@src/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract open_deps() async context manager from main()</name>
  <files>src/pipeline.py</files>
  <read_first>
    src/pipeline.py (current file: Deps/build_deps at lines 43-64, main()'s inline construction block at lines 295-327)
    .planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md (D-01, D-02, D-03, D-04)
    .planning/phases/09-pipeline-extraction-supporting-models/09-PATTERNS.md (open_deps target-shape section)
  </read_first>
  <action>
    Add `from contextlib import asynccontextmanager` and `from collections.abc import AsyncIterator` to the stdlib import block at the top of src/pipeline.py, alongside the existing `import asyncio` / `import logging` lines. Define a new function `async def open_deps(settings: Settings) -> AsyncIterator[Deps]:` decorated with `@asynccontextmanager`, placed immediately after `build_deps` (currently ending at line 64) and before `process_account` (currently starting at line 67), per D-01: open_deps yields the existing frozen `Deps` dataclass, no new bundle type.

    Move the entire client-construction block currently inside `main()` verbatim into `open_deps`'s body: the `async with httpx.AsyncClient(timeout=60.0) as http:` block (currently lines 295-327), including the `exa`/`bb`/`writer`/`judge` type declarations, the `if settings.demo_bundle is not None: ... else: ...` branch (Replay*/Recording* wiring, D-03), and the trailing `deps = build_deps(writer=writer, judge=judge, exa=exa, browserbase=bb)` line. Preserve the existing inline comments citing D-15/D-17 verbatim during the move (those reference the original v1.0 replay/record feature phase, a different CONTEXT.md's decision numbering than this phase's D-01..D-12 — do not delete or renumber them).

    Replace the trailing `deps = build_deps(...)` assignment with `yield build_deps(writer=writer, judge=judge, exa=exa, browserbase=bb)` so `open_deps` yields the `Deps` bundle instead of assigning it. Per D-02, `open_deps` owns the httpx.AsyncClient lifetime entirely inside its own `async with` block; add no separate `try`/`finally` and expose no `http` parameter or return value outside the function. Per D-04, `open_deps` performs no key validation and takes no tier/mode parameter; it constructs clients from `settings` exactly as `main()` does today — key validation stays in `settings.require_for_pipeline()`, called by `main()` before entering `open_deps`.

    Rewrite `main()`'s body: replace the whole moved block (including the removed `deps = build_deps(...)` line) with `async with open_deps(settings) as deps:` followed by the existing `scored = await run_pipeline(accounts, deps, concurrency=settings.pipeline_concurrency)` line, indented one level under the new `async with`. `main()` keeps calling `settings.require_for_pipeline()` immediately before this block, exactly as today.
  </action>
  <verify>
    <automated>uv run mypy src/pipeline.py</automated>
  </verify>
  <done>
    open_deps is a module-level @asynccontextmanager-decorated async generator function directly below build_deps; main()'s body no longer contains an inline httpx.AsyncClient/Replay*/Recording* construction block, only `async with open_deps(settings) as deps:`; mypy strict passes on src/pipeline.py.
  </done>
  <acceptance_criteria>
    - `grep -n "async def open_deps" src/pipeline.py` returns exactly one match, positioned after `def build_deps` and before `async def process_account`.
    - `grep -c "async with httpx.AsyncClient" src/pipeline.py` equals 1 (the block now lives only inside open_deps, not duplicated in main()).
    - `grep -n "async with open_deps(settings) as deps:" src/pipeline.py` returns exactly one match inside main().
    - `uv run mypy src/pipeline.py` exits 0.
    - `uv run pytest tests/integration/test_pipeline.py tests/integration/test_pipeline_failures.py tests/functional/test_replay_pipeline.py -q` passes with 0 failures (build_deps/process_account/run_pipeline call sites are untouched by this task, so these keep passing unmodified).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Functional test proving open_deps wiring across replay/record/live modes (D-11)</name>
  <files>tests/functional/test_pipeline_open_deps.py</files>
  <read_first>
    src/pipeline.py (post-Task-1 open_deps, to confirm signature and placement)
    src/config.py (Settings fields: demo_bundle, record_bundle, deepseek_api_key, exa_api_key, browserbase_api_key, browserbase_project_id)
    src/clients/replay.py (ReplayExa, ReplayBrowserbase, RecordingExa, RecordingBrowserbase class names)
    src/clients/exa_client.py (ExaClient, `_client` attribute holding the shared httpx.AsyncClient)
    src/clients/browserbase_client.py (BrowserbaseClient, `_client` attribute)
    src/enrich.py (Enricher.__init__ stores self._exa / self._browserbase, so Deps.enricher._exa is the reach-in point)
    tests/unit/test_config.py (existing `Settings(_env_file=None, ...)  # type: ignore[call-arg]` convention for hermetic test construction)
    tests/functional/test_replay_pipeline.py (existing @pytest.mark.asyncio + tmp_path conventions in this test suite)
  </read_first>
  <action>
    Create tests/functional/test_pipeline_open_deps.py with a module-level helper `_base_settings(**overrides: object) -> Settings` that returns `Settings(_env_file=None, deepseek_api_key="test-deepseek-key", exa_api_key="test-exa-key", browserbase_api_key="test-bb-key", browserbase_project_id="test-project", **overrides)  # type: ignore[call-arg]`, mirroring tests/unit/test_config.py's hermetic-construction convention so a developer's local .env cannot influence the test. Fake non-empty key strings are sufficient because only client construction is exercised, never `.synthesize()` / `.search_about()` / `.render()` (no live network calls occur in this test).

    Write three `@pytest.mark.asyncio` test functions per D-11:
    1. `test_open_deps_replay_settings_yield_replay_clients(tmp_path: Path) -> None` — build `_base_settings(demo_bundle=tmp_path)`, enter `async with open_deps(settings) as deps:`, assert `isinstance(deps.enricher._exa, ReplayExa)` and `isinstance(deps.enricher._browserbase, ReplayBrowserbase)`.
    2. `test_open_deps_record_settings_yield_recording_wrappers(tmp_path: Path) -> None` — build `_base_settings(record_bundle=tmp_path)`, assert `isinstance(deps.enricher._exa, RecordingExa)` and `isinstance(deps.enricher._browserbase, RecordingBrowserbase)`.
    3. `test_open_deps_live_settings_yield_real_clients_and_closes_http_on_exit() -> None` — build `_base_settings()` (neither demo_bundle nor record_bundle set), inside the `async with` block assert `isinstance(deps.enricher._exa, ExaClient)` and `isinstance(deps.enricher._browserbase, BrowserbaseClient)`, capture `http_client = deps.enricher._exa._client` and assert `http_client.is_closed is False` while still inside the block; after the `async with` block exits, assert `http_client.is_closed is True` (proves D-02's internally-owned httpx.AsyncClient is closed on context exit).

    Import `open_deps` from `src.pipeline`; `Settings` from `src.config`; `ReplayExa`, `ReplayBrowserbase`, `RecordingExa`, `RecordingBrowserbase` from `src.clients.replay`; `ExaClient` from `src.clients.exa_client`; `BrowserbaseClient` from `src.clients.browserbase_client`; `Path` from `pathlib`.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_pipeline_open_deps.py -q</automated>
  </verify>
  <done>
    All three tests pass: replay settings yield Replay* client types, record settings yield Recording* wrapper types, live settings yield real ExaClient/BrowserbaseClient types, and the shared httpx.AsyncClient is closed after the `async with open_deps(...)` block exits.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_pipeline_open_deps.py -q` reports 3 passed, 0 failed.
    - `uv run ruff check tests/functional/test_pipeline_open_deps.py` exits 0.
    - `uv run black --check tests/functional/test_pipeline_open_deps.py` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

Symbols created across all Phase 9 plans (09-01 through 09-04), for source-grounding drift checks:

- `src/pipeline.py::open_deps` — new `@asynccontextmanager`-decorated async function, `async def open_deps(settings: Settings) -> AsyncIterator[Deps]`
- `src/enrich.py::RawContext` — promoted public frozen dataclass (was `_RawContext`)
- `src/enrich.py::collect_context` — new module-level async function, `async def collect_context(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike) -> RawContext`
- `src/enrich.py::_gather_about_text` — promoted module-level private helper (was `Enricher._gather_about_text`)
- `src/clients/browserbase_client.py::NullBrowserbase` — new no-op `BrowserbaseLike` client
- `src/models.py::RetrievalStatus` — new `Literal["ok", "thin", "empty"]` type alias
- `src/models.py::EvidencePack` — new frozen pydantic model
- `src/models.py::EvidencePack.from_context` — new factory classmethod
- `tests/functional/test_pipeline_open_deps.py` — new test file (3 tests)
- `tests/functional/test_browserbase_client.py::test_null_browserbase_render_returns_none` — new test
- `tests/functional/test_enrich.py::test_collect_context_threads_null_browserbase_through_exa_only_continue` — new test
- `tests/unit/test_models.py::TestEvidencePack` — new test class (4 tests)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings env vars -> client constructors (open_deps) | API keys/config flow unchanged from process env into ExaClient/BrowserbaseClient/NvidiaClient construction; relocated, not modified |
| Exa/Browserbase HTTP responses -> collect_context/Enricher parsing | Untrusted external API content parsed into RawContext/Firmographics; parsing logic unchanged, only the entry point (module function vs private method) changes |
| EvidencePack model -> future MCP tool boundary (Phase 10+) | New frozen aggregate model; not yet exposed to any external caller this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|------------------|
| T-09-01 | Tampering | src/pipeline.py::open_deps (httpx.AsyncClient lifecycle) | low | accept | Client construction/teardown logic moved verbatim (D-02/D-03); this plan's Task 2 functional test asserts `is_closed` False-then-True across the context boundary, so lifecycle regressions are caught by CI, not by inspection |
| T-09-02 | Information Disclosure | src/clients/browserbase_client.py::NullBrowserbase.render logging | low | accept | Logs only the target URL (already Exa-sourced, not raw user input) at INFO, consistent with BrowserbaseClient's existing WARNING-level URL logging; no keys/secrets in the log line |
| T-09-03 | Denial of Service | src/models.py::EvidencePack (justifications/news tuples, about_text) | medium | accept | No size cap on retrieved text in this phase (D-10 defers snippet capping to the Phase 10 MCP boundary); acceptable now because EvidencePack has no external consumer yet in Phase 9 — tracked forward, Phase 10 must cap sizes before wiring get_account_evidence |

No new package-manager installs in this phase (no pyproject.toml changes); the `mcp` SDK dependency is deferred to Phase 10 per ROADMAP, so the package-legitimacy gate does not apply here.
</threat_model>

<verification>
Run `uv run pytest tests/functional/test_pipeline_open_deps.py tests/integration/test_pipeline.py tests/integration/test_pipeline_failures.py tests/functional/test_replay_pipeline.py -q` and `uv run mypy src/pipeline.py` — all must pass with zero failures/errors before this plan is considered complete.
</verification>

<success_criteria>
- `open_deps(settings)` exists as an async context manager next to `build_deps`, yielding `Deps`.
- `main()` uses `async with open_deps(settings) as deps:` with no inline client-construction block remaining.
- New functional test proves replay/record/live construction modes and httpx client teardown (D-11).
- Pre-existing pipeline tests pass unmodified.
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-extraction-supporting-models/09-01-SUMMARY.md` when done
</output>

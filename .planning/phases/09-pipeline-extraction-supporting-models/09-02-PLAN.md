---
phase: 09-pipeline-extraction-supporting-models
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enrich.py
  - src/clients/browserbase_client.py
  - tests/functional/test_enrich.py
  - tests/functional/test_browserbase_client.py
autonomous: true
requirements: [INTEG-02]

must_haves:
  truths:
    - "collect_context() is importable at module level from src.enrich without instantiating Enricher or supplying an llm argument (D-05)"
    - "RawContext is a public frozen dataclass, promoted from _RawContext (D-06)"
    - "NullBrowserbase.render() returns None (not raising BrowserbaseError), deliberately deviating from the design spec per D-07, and threads through collect_context's existing 'if rendered is not None' continue-with-Exa-only branch without introducing a new exception path"
    - "Enricher.enrich() still delegates to collect_context internally; all pre-existing test_enrich.py behavior is unchanged"
  artifacts:
    - "src/enrich.py (RawContext, collect_context, _gather_about_text promoted to module level)"
    - "src/clients/browserbase_client.py (NullBrowserbase added)"
    - "tests/functional/test_enrich.py (extended with a NullBrowserbase-threading test)"
    - "tests/functional/test_browserbase_client.py (extended with a NullBrowserbase render test)"
  key_links:
    - "Enricher.enrich() -> collect_context(account, exa=self._exa, browserbase=self._browserbase) -> RawContext"
    - "NullBrowserbase.render() -> collect_context's existing 'rendered is not None' branch -> Exa-only RawContext, no new exception type reaches process_account's narrow except clauses"
---

<objective>
Promote `Enricher._collect_context`/`_RawContext` to a public, module-level `collect_context()`/`RawContext` seam in `src/enrich.py`, and add a `NullBrowserbase` no-op client to `src/clients/browserbase_client.py` (INTEG-02). This gives Phase 10's thin MCP tier an Exa-only retrieval path that requires no `Enricher` instance and no LLM key.

Purpose: the thin MCP tier (Phase 10) must call retrieval with zero LLM involvement; `Enricher.__init__` currently requires an `llm` argument, which is the trap D-05 avoids.
Output: public `collect_context()` + `RawContext` in `src/enrich.py`, `NullBrowserbase` in `src/clients/browserbase_client.py`, plus tests proving the D-07 deviation (no-op, not raising) threads harmlessly through the existing catch-and-continue path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md
@.planning/phases/09-pipeline-extraction-supporting-models/09-PATTERNS.md
@src/enrich.py
@src/clients/browserbase_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote collect_context() and RawContext to module level (D-05, D-06)</name>
  <files>src/enrich.py</files>
  <read_first>
    src/enrich.py (current file: `_RawContext` at lines 27-31, `Enricher._collect_context` at lines 63-85, `Enricher._gather_about_text` at lines 87-96, `Enricher._extract_firmographics` at lines 98-124, `_build_context_block` at lines 190-195)
    src/clients/protocols.py (ExaLike, BrowserbaseLike Protocol contracts collect_context's params must satisfy)
    .planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md (D-05, D-06)
    .planning/phases/09-pipeline-extraction-supporting-models/09-PATTERNS.md (collect_context target-shape section)
    tests/functional/test_enrich.py (confirms no existing test reaches into the private methods being promoted, only `Enricher(...).enrich(...)`)
  </read_first>
  <action>
    Rename the frozen dataclass `_RawContext` (current lines 27-31) to public `RawContext`, keeping `@dataclass(frozen=True)` and its three existing fields (`about_text: str`, `about_citations: list[Citation]`, `news_items: list[NewsItem]`) unchanged.

    Extract the body of `Enricher._collect_context` (current lines 63-85) into a new module-level function `async def collect_context(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike) -> RawContext:` placed above the `Enricher` class (after the module constants and `FIRMOGRAPHICS_SYSTEM`, before `class Enricher`), per D-05: keyword-only `exa`/`browserbase` params so a caller with no `Enricher` instance and no `llm` argument can retrieve context.

    Move `Enricher._gather_about_text` (current lines 87-96) to a module-level private function `_gather_about_text(results: list[ExaResult]) -> tuple[str, list[Citation]]` next to `collect_context` (it has no `self` dependency in its current body). Update `collect_context`'s body to call the module-level `_gather_about_text(about_results)` instead of `self._gather_about_text(about_results)`.

    Update `Enricher.enrich()` (current line 46) to call `ctx = await collect_context(account, exa=self._exa, browserbase=self._browserbase)` instead of `await self._collect_context(account)`.

    Update the parameter type on `Enricher._extract_firmographics` (current line 98, `ctx: _RawContext`) to `ctx: RawContext`. Update the parameter type on `_build_context_block` (current line 190, `ctx: _RawContext`) to `ctx: RawContext`.

    Remove `Enricher._collect_context` and `Enricher._gather_about_text` entirely once their logic has moved to module level; `Enricher` retains only `__init__`, `enrich`, and `_extract_firmographics`.
  </action>
  <verify>
    <automated>uv run mypy src/enrich.py && uv run pytest tests/functional/test_enrich.py -q</automated>
  </verify>
  <done>
    collect_context and RawContext are importable at module level from src.enrich; Enricher no longer defines _collect_context or _gather_about_text; all pre-existing tests in tests/functional/test_enrich.py pass unmodified (they only call `Enricher(...).enrich(...)`, never the promoted internals directly).
  </done>
  <acceptance_criteria>
    - `grep -n "class RawContext" src/enrich.py` returns exactly one match.
    - `grep -n "async def collect_context" src/enrich.py` returns exactly one match at module-level indentation (not nested under `class Enricher`).
    - `grep -c "def _collect_context\|def _gather_about_text" src/enrich.py` equals 0 (both promoted out of Enricher).
    - `uv run mypy src/enrich.py` exits 0.
    - `uv run pytest tests/functional/test_enrich.py -q` passes with 0 failures.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add NullBrowserbase and prove it threads through collect_context's continue path (D-07)</name>
  <files>src/clients/browserbase_client.py, tests/functional/test_browserbase_client.py, tests/functional/test_enrich.py</files>
  <read_first>
    src/clients/browserbase_client.py (current file: BrowserbaseClient.render at lines 56-61, RenderedPage at lines 26-30, `log` at line 19)
    src/clients/protocols.py (BrowserbaseLike.render(url) -> RenderedPage | None contract)
    src/enrich.py (post-Task-1 collect_context signature and the `if rendered is not None:` continue branch it contains)
    .planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md (D-07's deliberate spec deviation)
    tests/functional/test_replay_pipeline.py (existing test-local `_NullBrowserbase` stub, the near-verbatim shape reference, lines 123-127)
    tests/functional/test_browserbase_client.py (current tests, for style)
    tests/functional/test_enrich.py (current FakeExa stub and `_exa_about` helper, for style)
  </read_first>
  <action>
    Append a new class `NullBrowserbase` to the bottom of src/clients/browserbase_client.py, after `BrowserbaseClient`, per Claude's Discretion in 09-CONTEXT.md (same file as the real client, since `RenderedPage` and `log` are already defined there and no repo convention calls for a dedicated `null_*.py` file). Give it a docstring stating it satisfies `BrowserbaseLike` unconditionally, returning `None` — and explicitly note the D-07 spec deviation: it does NOT raise `BrowserbaseError`, unlike the original design spec's wording, because the catch-and-continue path lives inside `BrowserbaseClient.render`'s own try/except (lines 56-61), not in `collect_context` itself; a raising null object would propagate into `process_account`'s enrich `except` clause and mark the whole account `unscoreable`, the opposite of the intended Exa-only continue.

    Implement one method: `async def render(self, url: str) -> RenderedPage | None:` that calls `log.info("browserbase disabled: skipping render for %s", url)` (reuse the existing module-level `log = logging.getLogger(__name__)`, no new import) and returns `None`. Add no explicit `BrowserbaseLike` inheritance declaration — the protocol is structural, matching `BrowserbaseClient`'s own existing convention of zero explicit protocol inheritance.

    Add a test `test_null_browserbase_render_returns_none` to tests/functional/test_browserbase_client.py asserting `await NullBrowserbase().render("https://x.com/about") is None`.

    Add a test `test_collect_context_threads_null_browserbase_through_exa_only_continue` to tests/functional/test_enrich.py that imports `collect_context` and `NullBrowserbase`, then calls the module-level `collect_context(Account(domain="x.com"), exa=FakeExa(about=[_exa_about(text="short")], news=[]), browserbase=NullBrowserbase())` directly (no `Enricher` instance). Assert the call completes without raising, the returned value is a `RawContext` instance, and `about_text == "short"` (the exact short Exa snippet, unchanged), proving Browserbase-disabled continues with Exa-only results via the existing `if rendered is not None:` branch even though the about text is below `ABOUT_TEXT_MIN_CHARS`.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_browserbase_client.py tests/functional/test_enrich.py -q</automated>
  </verify>
  <done>
    NullBrowserbase.render always returns None and logs at INFO; collect_context called with NullBrowserbase completes without exception and returns Exa-only RawContext even when about_text is below ABOUT_TEXT_MIN_CHARS. ROADMAP Phase 9 success criterion 3's "raises BrowserbaseError" wording is superseded by D-07's continue-path behavior, which this test proves instead.
  </done>
  <acceptance_criteria>
    - `grep -n "class NullBrowserbase" src/clients/browserbase_client.py` returns exactly one match.
    - `uv run mypy src/clients/browserbase_client.py` exits 0 (return type `RenderedPage | None` satisfies `BrowserbaseLike` structurally).
    - `uv run pytest tests/functional/test_browserbase_client.py::test_null_browserbase_render_returns_none tests/functional/test_enrich.py::test_collect_context_threads_null_browserbase_through_exa_only_continue -q` reports 2 passed, 0 failed.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

Symbols created across all Phase 9 plans (09-01 through 09-04), for source-grounding drift checks:

- `src/pipeline.py::open_deps` — new `@asynccontextmanager`-decorated async function, `async def open_deps(settings: Settings) -> AsyncIterator[Deps]`
- `src/enrich.py::RawContext` — promoted public frozen dataclass (was `_RawContext`)
- `src/enrich.py::collect_context` — new module-level async function, `async def collect_context(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike) -> RawContext`
- `src/enrich.py::_gather_about_text` — promoted module-level private helper (was `Enricher._gather_about_text`)
- `src/clients/browserbase_client.py::NullBrowserbase` — new no-op `BrowserbaseLike` client
- `src/models.py::RetrievalStatus` — new `Literal["ok", "thin", "empty"]` type alias
- `src/models.py::EvidencePack` — new frozen pydantic model
- `src/models.py::EvidencePack.from_context` — new factory classmethod
- `tests/functional/test_pipeline_open_deps.py` — new test file (3 tests)
- `tests/functional/test_browserbase_client.py::test_null_browserbase_render_returns_none` — new test
- `tests/functional/test_enrich.py::test_collect_context_threads_null_browserbase_through_exa_only_continue` — new test
- `tests/unit/test_models.py::TestEvidencePack` — new test class (4 tests)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings env vars -> client constructors (open_deps) | API keys/config flow unchanged from process env into ExaClient/BrowserbaseClient/NvidiaClient construction; relocated, not modified |
| Exa/Browserbase HTTP responses -> collect_context/Enricher parsing | Untrusted external API content parsed into RawContext/Firmographics; parsing logic unchanged, only the entry point (module function vs private method) changes |
| EvidencePack model -> future MCP tool boundary (Phase 10+) | New frozen aggregate model; not yet exposed to any external caller this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|------------------|
| T-09-01 | Tampering | src/pipeline.py::open_deps (httpx.AsyncClient lifecycle) | low | accept | Client construction/teardown logic moved verbatim (D-02/D-03); Plan 09-01's functional test asserts `is_closed` False-then-True across the context boundary, so lifecycle regressions are caught by CI, not by inspection |
| T-09-02 | Information Disclosure | src/clients/browserbase_client.py::NullBrowserbase.render logging | low | accept | Logs only the target URL (already Exa-sourced, not raw user input) at INFO, consistent with BrowserbaseClient's existing WARNING-level URL logging; no keys/secrets in the log line |
| T-09-03 | Denial of Service | src/models.py::EvidencePack (justifications/news tuples, about_text) | medium | accept | No size cap on retrieved text in this phase (D-10 defers snippet capping to the Phase 10 MCP boundary); acceptable now because EvidencePack has no external consumer yet in Phase 9 — tracked forward, Phase 10 must cap sizes before wiring get_account_evidence |

No new package-manager installs in this phase (no pyproject.toml changes); the `mcp` SDK dependency is deferred to Phase 10 per ROADMAP, so the package-legitimacy gate does not apply here.
</threat_model>

<verification>
Run `uv run pytest tests/functional/test_enrich.py tests/functional/test_browserbase_client.py -q` and `uv run mypy src/enrich.py src/clients/browserbase_client.py` — all must pass with zero failures/errors before this plan is considered complete.
</verification>

<success_criteria>
- `collect_context()` and `RawContext` are importable at module level from `src.enrich`.
- `NullBrowserbase` satisfies `BrowserbaseLike`, returns `None` (D-07 deviation, not raising), and threads through `collect_context`'s existing continue path with no new exception type.
- Pre-existing `Enricher` behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-extraction-supporting-models/09-02-SUMMARY.md` when done
</output>

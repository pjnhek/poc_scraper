---
phase: 09-pipeline-extraction-supporting-models
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models.py
  - tests/unit/test_models.py
autonomous: true
requirements: [INTEG-03]

must_haves:
  truths:
    - "EvidencePack is a frozen, extra='forbid' pydantic model in src/models.py using tuple collections for justifications and news (D-10)"
    - "EvidencePack.from_context computes retrieval_status as empty/thin/ok using the same 'no about text AND no news' semantics Enrichment.is_empty already uses, and the same about_text_min_chars threshold value ABOUT_TEXT_MIN_CHARS already defines in src/enrich.py, without src/models.py importing anything from src/enrich.py (avoids the models.py<->enrich.py circular import flagged in 09-PATTERNS.md, per D-09's 'no new tuning knobs')"
  artifacts:
    - "src/models.py (RetrievalStatus, EvidencePack, EvidencePack.from_context added)"
    - "tests/unit/test_models.py (TestEvidencePack class, 4 tests)"
  key_links:
    - "EvidencePack.from_context(about_text, news_items, justifications, about_text_min_chars=...) -> retrieval_status"
    - "Phase 10's future evidence.py wrapper will call from_context(ctx.about_text, ctx.news_items, justifications, about_text_min_chars=ABOUT_TEXT_MIN_CHARS)"
---

<objective>
Add a frozen `EvidencePack` model to `src/models.py` (INTEG-03) with a `from_context` factory classmethod that computes a `retrieval_status` ("ok"/"thin"/"empty") honesty field, following the codebase's existing frozen-model and factory-classmethod conventions.

Purpose: Phase 10's `get_account_evidence` MCP tool needs a JSON-serializable evidence aggregate with an honesty signal for weak retrieval, mirroring the `unscoreable` rule already used for the full pipeline.
Output: `RetrievalStatus`, `EvidencePack`, and `EvidencePack.from_context` in `src/models.py`, plus unit tests covering empty/thin/ok classification and the frozen+extra-forbid contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md
@.planning/phases/09-pipeline-extraction-supporting-models/09-PATTERNS.md
@src/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RetrievalStatus and EvidencePack.from_context (D-08, D-09, D-10)</name>
  <files>src/models.py</files>
  <read_first>
    src/models.py (current file: `_Frozen` base at lines 17-18, tuple-collection convention in `Firmographics`/`Enrichment`, factory-classmethod convention in `Citation.make` lines 44-62 and `ScoredAccount.unscoreable` lines 164-172, `Justification`/`NewsItem` at lines 65-83)
    src/enrich.py (read-only reference: `ABOUT_TEXT_MIN_CHARS = 200` constant and `RawContext.about_text`/`news_items` field shape — src/models.py must NOT import from src/enrich.py)
    .planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md (D-08, D-09, D-10)
    .planning/phases/09-pipeline-extraction-supporting-models/09-PATTERNS.md (EvidencePack target-shape section and its circular-import discussion)
  </read_first>
  <behavior>
    - `EvidencePack.from_context("", [], (), about_text_min_chars=200)` -> `retrieval_status == "empty"` (no about text AND no news, mirrors `Enrichment.is_empty`)
    - `EvidencePack.from_context("short text", [], (), about_text_min_chars=200)` -> `retrieval_status == "thin"` (about text shorter than the threshold)
    - `EvidencePack.from_context("x" * 250, [], (), about_text_min_chars=200)` -> `retrieval_status == "ok"` (about text meets the threshold)
    - `EvidencePack(retrieval_status="ok")` is frozen (mutating raises `ValidationError`) and rejects unknown keys (`extra="forbid"`)
  </behavior>
  <action>
    Add a module-level type alias `RetrievalStatus = Literal["ok", "thin", "empty"]` directly above the new `EvidencePack` class, near the bottom of src/models.py after `ScoredAccount`, matching the existing `Literal`-alias convention (`LLMProvider` in src/config.py, `AxisName` in src/icp_config.py).

    Define `class EvidencePack(_Frozen):` with fields, required field first per the codebase's convention seen in `ScoredAccount`/`EvalScore`: `retrieval_status: RetrievalStatus` (required), `justifications: tuple[Justification, ...] = ()`, `news: tuple[NewsItem, ...] = ()` (D-10: reuses the exact nested frozen models and field names `Enrichment.justifications`/`Enrichment.news` already use, so `EvidencePack` slots in as a peer of `Enrichment` and `model_dump()` gives JSON for free).

    Add a classmethod with signature `def from_context(cls, about_text: str, news_items: list[NewsItem], justifications: tuple[Justification, ...], *, about_text_min_chars: int) -> EvidencePack:`. This deviates from CONTEXT.md D-08's illustrative `from_context(ctx, justifications)` shorthand by accepting `about_text`/`news_items` as plain primitives instead of a `RawContext` instance, and by taking `about_text_min_chars` as an explicit keyword argument instead of importing `ABOUT_TEXT_MIN_CHARS` — specifically to avoid a circular import: `src/enrich.py` already imports `Account, Citation, Enrichment, Firmographics, Justification, NewsItem` from `src.models` at module level, so `src/models.py` cannot import `RawContext` or `ABOUT_TEXT_MIN_CHARS` back from `src.enrich` without creating an import cycle. The caller (this task's own unit test now, and Phase 10's future `evidence.py` wrapper later) passes `ctx.about_text`, `ctx.news_items`, and `enrich.ABOUT_TEXT_MIN_CHARS` through as plain values — this keeps `ABOUT_TEXT_MIN_CHARS` a single source of truth in `src/enrich.py` per D-09 (no new tuning knob, no duplicated literal `200` in `src/models.py`).

    Inside `from_context`, compute `status: RetrievalStatus = "empty"` when `not about_text and not news_items` (mirrors `Enrichment.is_empty`'s exact "no about text AND no news" semantics per D-09), else `"thin"` when `len(about_text) < about_text_min_chars`, else `"ok"`. Return `cls(retrieval_status=status, justifications=justifications, news=tuple(news_items))`.
  </behavior>
  <verify>
    <automated>uv run mypy src/models.py</automated>
  </verify>
  <done>
    RetrievalStatus and EvidencePack are importable from src.models; EvidencePack is frozen with extra="forbid" (inherited via _Frozen); EvidencePack.from_context returns "empty"/"thin"/"ok" correctly for crafted inputs; mypy strict passes with no new ignore comments.
  </done>
  <acceptance_criteria>
    - `grep -n "class EvidencePack" src/models.py` returns exactly one match.
    - `grep -n "RetrievalStatus = Literal" src/models.py` returns exactly one match.
    - `uv run mypy src/models.py` exits 0.
    - `uv run python -c "from src.models import EvidencePack; e = EvidencePack(retrieval_status='ok'); print(e.model_dump()['retrieval_status'])"` prints `ok` and exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Unit tests for EvidencePack classification and frozen contract</name>
  <files>tests/unit/test_models.py</files>
  <read_first>
    tests/unit/test_models.py (current file: existing `TestEnrichmentEmpty`, `TestJustification`, `TestScoredAccountUnscoreable` classes for per-model-class style and the top-of-file `from src.models import (...)` block)
    src/models.py (post-Task-1 EvidencePack)
  </read_first>
  <action>
    Add `EvidencePack` to the existing `from src.models import (...)` block at the top of tests/unit/test_models.py. Add a new `class TestEvidencePack:` following the file's existing per-model-class convention, with four test methods:

    `test_empty_when_no_about_text_and_no_news` — call `EvidencePack.from_context("", [], (), about_text_min_chars=200)`, assert `retrieval_status == "empty"`.

    `test_thin_when_about_text_below_threshold` — call `EvidencePack.from_context("short text", [], (), about_text_min_chars=200)`, assert `retrieval_status == "thin"`.

    `test_ok_when_about_text_meets_threshold` — call `EvidencePack.from_context("x" * 250, [], (), about_text_min_chars=200)`, assert `retrieval_status == "ok"`.

    `test_is_frozen_and_rejects_extra_fields` — construct `pack = EvidencePack(retrieval_status="ok")`, assert `pytest.raises(ValidationError)` when setting `pack.retrieval_status = "thin"`, and assert `pytest.raises(ValidationError)` when constructing `EvidencePack(retrieval_status="ok", unexpected_field=1)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_models.py::TestEvidencePack -q</automated>
  </verify>
  <done>
    TestEvidencePack has 4 passing tests covering empty/thin/ok classification and the frozen+extra-forbid contract; no regression to the other existing test classes in the file.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_models.py::TestEvidencePack -q` reports 4 passed, 0 failed.
    - `uv run pytest tests/unit/test_models.py -q` reports 0 failed (no regression to the file's other pre-existing test classes).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

Symbols created across all Phase 9 plans (09-01 through 09-04), for source-grounding drift checks:

- `src/pipeline.py::open_deps` — new `@asynccontextmanager`-decorated async function, `async def open_deps(settings: Settings) -> AsyncIterator[Deps]`
- `src/enrich.py::RawContext` — promoted public frozen dataclass (was `_RawContext`)
- `src/enrich.py::collect_context` — new module-level async function, `async def collect_context(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike) -> RawContext`
- `src/enrich.py::_gather_about_text` — promoted module-level private helper (was `Enricher._gather_about_text`)
- `src/clients/browserbase_client.py::NullBrowserbase` — new no-op `BrowserbaseLike` client
- `src/models.py::RetrievalStatus` — new `Literal["ok", "thin", "empty"]` type alias
- `src/models.py::EvidencePack` — new frozen pydantic model
- `src/models.py::EvidencePack.from_context` — new factory classmethod
- `tests/functional/test_pipeline_open_deps.py` — new test file (3 tests)
- `tests/functional/test_browserbase_client.py::test_null_browserbase_render_returns_none` — new test
- `tests/functional/test_enrich.py::test_collect_context_threads_null_browserbase_through_exa_only_continue` — new test
- `tests/unit/test_models.py::TestEvidencePack` — new test class (4 tests)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings env vars -> client constructors (open_deps) | API keys/config flow unchanged from process env into ExaClient/BrowserbaseClient/NvidiaClient construction; relocated, not modified |
| Exa/Browserbase HTTP responses -> collect_context/Enricher parsing | Untrusted external API content parsed into RawContext/Firmographics; parsing logic unchanged, only the entry point (module function vs private method) changes |
| EvidencePack model -> future MCP tool boundary (Phase 10+) | New frozen aggregate model; not yet exposed to any external caller this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|------------------|
| T-09-01 | Tampering | src/pipeline.py::open_deps (httpx.AsyncClient lifecycle) | low | accept | Client construction/teardown logic moved verbatim (D-02/D-03); Plan 09-01's functional test asserts `is_closed` False-then-True across the context boundary, so lifecycle regressions are caught by CI, not by inspection |
| T-09-02 | Information Disclosure | src/clients/browserbase_client.py::NullBrowserbase.render logging | low | accept | Logs only the target URL (already Exa-sourced, not raw user input) at INFO, consistent with BrowserbaseClient's existing WARNING-level URL logging; no keys/secrets in the log line |
| T-09-03 | Denial of Service | src/models.py::EvidencePack (justifications/news tuples, about_text) | medium | accept | No size cap on retrieved text in this phase (D-10 defers snippet capping to the Phase 10 MCP boundary); acceptable now because EvidencePack has no external consumer yet in Phase 9 — tracked forward, Phase 10 must cap sizes before wiring get_account_evidence |

No new package-manager installs in this phase (no pyproject.toml changes); the `mcp` SDK dependency is deferred to Phase 10 per ROADMAP, so the package-legitimacy gate does not apply here.
</threat_model>

<verification>
Run `uv run pytest tests/unit/test_models.py -q` and `uv run mypy src/models.py` — all must pass with zero failures/errors before this plan is considered complete.
</verification>

<success_criteria>
- `EvidencePack` exists as a frozen, extra="forbid" model with tuple collections, matching existing conventions.
- `EvidencePack.from_context` correctly classifies empty/thin/ok without src/models.py importing from src/enrich.py.
- Unit tests cover all three classification branches plus the frozen+extra-forbid contract.
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-extraction-supporting-models/09-03-SUMMARY.md` when done
</output>

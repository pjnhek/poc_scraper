---
phase: 09-pipeline-extraction-supporting-models
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified: []
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03]

must_haves:
  truths:
    - "make test (offline suite, no smoke) passes with 0 failures against the fully merged Phase 9 changeset (D-11)"
    - "make typecheck (mypy src evals, strict) exits 0 with no new ignore_missing_imports overrides added (D-12)"
    - "make lint (ruff + black --check) exits 0 across src, tests, evals (D-12)"
  artifacts: []
  key_links:
    - "All four Wave-1 touch points (open_deps, collect_context/RawContext, NullBrowserbase, EvidencePack) coexist without regressing tests/integration/test_pipeline.py, tests/integration/test_pipeline_failures.py, or tests/functional/test_replay_pipeline.py"
---

<objective>
Close Phase 9 with the offline regression safety net demanded by D-11/D-12: run the full non-smoke test suite, strict mypy, and lint across the merged state of all three Wave-1 plans (open_deps, collect_context/RawContext/NullBrowserbase, EvidencePack). No live `make run` or `make smoke` is required.

Purpose: prove ROADMAP Phase 9 success criterion 1 ("make run behavior and the existing pipeline test suite are unchanged") holds once all four touch points exist together, not just individually within each Wave-1 plan.
Output: a clean `make test` + `make typecheck` + `make lint` run; no code changes expected unless a genuine cross-plan regression surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full offline regression gate (D-11, D-12)</name>
  <files></files>
  <read_first>
    .planning/phases/09-pipeline-extraction-supporting-models/09-CONTEXT.md (D-11, D-12)
    src/pipeline.py (post-09-01: open_deps)
    src/enrich.py (post-09-02: collect_context, RawContext)
    src/clients/browserbase_client.py (post-09-02: NullBrowserbase)
    src/models.py (post-09-03: EvidencePack)
  </read_first>
  <action>
    Run the full offline regression gate against the merged state of all three Wave-1 plans: `make test` (== `uv run pytest -m "not smoke"`, the entire unit+functional+integration suite excluding the opt-in `make smoke` marker), `make typecheck` (== `uv run mypy src evals`), and `make lint` (== `uv run ruff check src tests evals` followed by `uv run black --check src tests evals`). Do not run `make smoke` or `make run` — D-12 explicitly does not require a live pipeline execution to close this phase.

    If any command fails, fix the regression in the offending file (src/pipeline.py, src/enrich.py, src/clients/browserbase_client.py, or src/models.py) before proceeding — this task is the phase's closing safety net, proving `make run`'s underlying wiring and the pre-existing pipeline test suite are unchanged (ROADMAP Phase 9 success criterion 1) now that `open_deps`, `collect_context`, `NullBrowserbase`, and `EvidencePack` all exist together.
  </action>
  <verify>
    <automated>make test && make typecheck && make lint</automated>
  </verify>
  <done>
    make test, make typecheck, and make lint all exit 0 with the full merged Phase 9 changeset (open_deps, collect_context/RawContext, NullBrowserbase, EvidencePack) in place; zero new mypy overrides added; zero pre-existing tests broken.
  </done>
  <acceptance_criteria>
    - `make test` exits 0 (all non-smoke tests pass, including the new tests/functional/test_pipeline_open_deps.py, the extended tests/functional/test_enrich.py and tests/functional/test_browserbase_client.py, and the extended tests/unit/test_models.py::TestEvidencePack).
    - `make typecheck` exits 0, and `grep -c "ignore_missing_imports = true" pyproject.toml` is unchanged from its pre-phase-9 value of 1 (no new override entries added).
    - `make lint` exits 0 (ruff and black both clean across src, tests, evals).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

Symbols created across all Phase 9 plans (09-01 through 09-04), for source-grounding drift checks. This plan produces no new symbols itself; it verifies the four touch points below coexist cleanly:

- `src/pipeline.py::open_deps` — new `@asynccontextmanager`-decorated async function, `async def open_deps(settings: Settings) -> AsyncIterator[Deps]`
- `src/enrich.py::RawContext` — promoted public frozen dataclass (was `_RawContext`)
- `src/enrich.py::collect_context` — new module-level async function, `async def collect_context(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike) -> RawContext`
- `src/enrich.py::_gather_about_text` — promoted module-level private helper (was `Enricher._gather_about_text`)
- `src/clients/browserbase_client.py::NullBrowserbase` — new no-op `BrowserbaseLike` client
- `src/models.py::RetrievalStatus` — new `Literal["ok", "thin", "empty"]` type alias
- `src/models.py::EvidencePack` — new frozen pydantic model
- `src/models.py::EvidencePack.from_context` — new factory classmethod
- `tests/functional/test_pipeline_open_deps.py` — new test file (3 tests)
- `tests/functional/test_browserbase_client.py::test_null_browserbase_render_returns_none` — new test
- `tests/functional/test_enrich.py::test_collect_context_threads_null_browserbase_through_exa_only_continue` — new test
- `tests/unit/test_models.py::TestEvidencePack` — new test class (4 tests)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings env vars -> client constructors (open_deps) | API keys/config flow unchanged from process env into ExaClient/BrowserbaseClient/NvidiaClient construction; relocated, not modified |
| Exa/Browserbase HTTP responses -> collect_context/Enricher parsing | Untrusted external API content parsed into RawContext/Firmographics; parsing logic unchanged, only the entry point (module function vs private method) changes |
| EvidencePack model -> future MCP tool boundary (Phase 10+) | New frozen aggregate model; not yet exposed to any external caller this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|------------------|
| T-09-01 | Tampering | src/pipeline.py::open_deps (httpx.AsyncClient lifecycle) | low | accept | Client construction/teardown logic moved verbatim (D-02/D-03); Plan 09-01's functional test asserts `is_closed` False-then-True across the context boundary, so lifecycle regressions are caught by CI, not by inspection |
| T-09-02 | Information Disclosure | src/clients/browserbase_client.py::NullBrowserbase.render logging | low | accept | Logs only the target URL (already Exa-sourced, not raw user input) at INFO, consistent with BrowserbaseClient's existing WARNING-level URL logging; no keys/secrets in the log line |
| T-09-03 | Denial of Service | src/models.py::EvidencePack (justifications/news tuples, about_text) | medium | accept | No size cap on retrieved text in this phase (D-10 defers snippet capping to the Phase 10 MCP boundary); acceptable now because EvidencePack has no external consumer yet in Phase 9 — tracked forward, Phase 10 must cap sizes before wiring get_account_evidence |

No new package-manager installs in this phase (no pyproject.toml changes); the `mcp` SDK dependency is deferred to Phase 10 per ROADMAP, so the package-legitimacy gate does not apply here.
</threat_model>

<verification>
Run `make test && make typecheck && make lint` — all three must exit 0 against the fully merged Phase 9 changeset before this plan (and the phase) is considered complete.
</verification>

<success_criteria>
- Full offline test suite (unit, functional, integration; smoke excluded) passes with 0 failures.
- Strict mypy across src and evals passes with 0 errors and no new overrides.
- ruff and black --check both pass across src, tests, evals.
- ROADMAP Phase 9 success criterion 1 ("make run behavior and the existing pipeline test suite are unchanged") is proven true for the merged changeset.
</success_criteria>

<output>
Create `.planning/phases/09-pipeline-extraction-supporting-models/09-04-SUMMARY.md` when done
</output>

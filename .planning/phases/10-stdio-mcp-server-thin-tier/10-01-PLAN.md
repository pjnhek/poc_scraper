---
phase: 10-stdio-mcp-server-thin-tier
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/models.py
  - tests/unit/test_models.py
  - src/config.py
  - tests/unit/test_config.py
autonomous: true
requirements: [MCP-01, MCP-06]

must_haves:
  truths:
    - "mcp>=1.28,<2.0 is an installed project dependency and the full existing suite is green on the resolved lockfile"
    - "EvidencePack carries an about_text field defaulting to empty string, and from_context stores the about_text it receives"
    - "Settings.mcp_tier() returns thin with only EXA_API_KEY, full with provider + Browserbase keys, thin when MCP_DEMO_MODE=1 regardless of keys, and raises listing EXA_API_KEY when it is missing"
  artifacts:
    - path: "pyproject.toml"
      provides: "mcp SDK dependency pin"
      contains: "mcp>=1.28,<2.0"
    - path: "src/models.py"
      provides: "EvidencePack.about_text wire-format field"
      contains: "about_text"
    - path: "src/config.py"
      provides: "Settings.mcp_demo_mode field and mcp_tier() tier resolver"
      contains: "def mcp_tier"
    - path: "tests/unit/test_config.py"
      provides: "mcp_tier() coverage across key-presence combinations"
      contains: "mcp_tier"
  key_links:
    - from: "src/models.py"
      to: "EvidencePack.from_context"
      via: "from_context passes its about_text parameter into the constructed instance"
      pattern: "about_text=about_text"
    - from: "src/config.py"
      to: "Settings.exa_api_key"
      via: "mcp_tier() fails fast on missing EXA key with the missing-keys listing message style"
      pattern: "EXA_API_KEY"
---

<objective>
Lay the Phase 10 foundation: add the `mcp` SDK as the project's first new production dependency of the milestone, extend the frozen `EvidencePack` model with the `about_text` wire-format field (D-01, D-02), and add `Settings.mcp_demo_mode` plus `Settings.mcp_tier()` so the server built in plan 10-02 can resolve its capability tier once at startup (MCP-06).

Purpose: everything in plans 10-02 and 10-03 imports `mcp`, serializes `EvidencePack`, or calls `mcp_tier()`. This plan makes those three seams exist and proves the existing suite stays green on the pydantic bump that `mcp` forces.
Output: updated pyproject.toml + uv.lock, extended src/models.py, extended src/config.py, extended unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-CONTEXT.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-RESEARCH.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 10-01 creates these symbols (the plan-review source-grounding pass should exclude them from drift verification):

- `pyproject.toml`: new dependency entry `mcp>=1.28,<2.0` in `[project] dependencies`
- `src/models.py`: new field `EvidencePack.about_text: str = ""`; extended `EvidencePack.from_context` return (now passes `about_text` through)
- `src/config.py`: new `Settings.mcp_demo_mode: bool = False` field (env `MCP_DEMO_MODE`); new method `Settings.mcp_tier() -> Literal["thin", "full"]`
- `tests/unit/test_models.py`: new tests for `about_text` default and passthrough
- `tests/unit/test_config.py`: new tests for `mcp_tier()` key-presence combinations

Phase-wide context: plans 10-02 and 10-03 additionally create the `src/mcp_server/` package (`build_server`, `get_account_evidence`, `resolve_and_log_tier`, `ThinDeps`, `make_thin_lifespan`, `build_evidence_pack`, `pack_from_context`, `ABOUT_TEXT_MCP_CAP`, `JUSTIFICATION_SUMMARY_MCP_CAP`, `NEWS_ITEM_MCP_CAP`), Makefile targets `mcp` and `smoke-mcp`, and test files `tests/unit/test_evidence.py`, `tests/functional/test_mcp_server.py`, `tests/smoke/test_mcp_e2e.py`.

<tasks>

<task type="auto">
  <name>Task 1: Add mcp>=1.28,<2.0 dependency and prove the suite survives the resolver bump</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (current `[project] dependencies` list, lines 7-20)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-RESEARCH.md sections "Standard Stack" and "Package Legitimacy Audit" (pin rationale, expected pydantic resolution, audit disposition)
  </read_first>
  <action>
    Add `"mcp>=1.28,<2.0"` to the `[project] dependencies` list in pyproject.toml (locked pin from milestone research; this is the official Model Context Protocol Python SDK bundling FastMCP, NOT the standalone `fastmcp` PyPI package). Do NOT pin any of mcp's transitive dependencies (anyio, starlette, uvicorn, httpx-sse, etc.) explicitly. No new dev dependencies and no new mypy overrides: `mcp` ships `py.typed`.

    Run `uv sync --extra dev` to regenerate uv.lock. Expect `pydantic` to resolve upward (mcp constrains `pydantic>=2.11.0,<3.0.0`), which is anticipated by 10-CONTEXT.md.

    Supply-chain confirmation (T-10-SC belt-and-suspenders on top of the RESEARCH.md legitimacy audit, which resolved the seam's raw SUS flag as a heuristic false positive and approved the package): run `uv pip show mcp` and confirm the reported version is >=1.28 and the project metadata points at the official `modelcontextprotocol/python-sdk` GitHub repository. If either check fails, STOP and surface the mismatch instead of proceeding.

    Then run the full offline gates to prove nothing in the existing suite regresses on the resolved pydantic bump: `make test`, `make typecheck`, `make lint`.
  </action>
  <verify>
    <automated>uv run python -c "from importlib.metadata import version; v = version('mcp'); assert v.split('.')[0] == '1' and int(v.split('.')[1]) >= 28, v; from mcp.server.fastmcp import FastMCP; from mcp.types import ToolAnnotations; print(v)" && make test && make typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mcp>=1.28,<2.0" pyproject.toml` returns 1
    - `uv run python -c "from mcp.server.fastmcp import FastMCP"` exits 0
    - `uv run python -c "from mcp.shared.memory import create_connected_server_and_client_session"` exits 0 (in-memory test client available for plan 10-02)
    - `make test` passes (smoke excluded as always)
    - `make typecheck` passes with zero new mypy override entries in pyproject.toml (the `[[tool.mypy.overrides]]` module list is unchanged)
    - `uv pip show mcp` output names version 1.28.x or later
  </acceptance_criteria>
  <done>mcp SDK importable at >=1.28, lockfile regenerated, full offline suite + strict mypy green on the resolved dependency set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add about_text field to EvidencePack and pass it through from_context (D-01, D-02)</name>
  <files>src/models.py, tests/unit/test_models.py</files>
  <read_first>
    - src/models.py (EvidencePack + RetrievalStatus, lines 175-201; _Frozen base at lines 17-18)
    - tests/unit/test_models.py (existing EvidencePack/from_context test style to extend, not replace)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-PATTERNS.md section "src/models.py::EvidencePack" (exact current shape and the one-line change)
  </read_first>
  <behavior>
    - Test 1: `EvidencePack(retrieval_status="ok")` still constructs (about_text defaults to "") so every Phase 9 call site and test stays green
    - Test 2: `EvidencePack.from_context(about_text="some capped text", news_items=[], justifications=(), about_text_min_chars=200)` returns a pack whose `about_text == "some capped text"`
    - Test 3: retrieval_status transitions are unchanged: empty about + no news -> "empty"; short about (< min_chars) + no news -> "thin"; long about -> "ok" (regression guard, may already exist -- extend, do not duplicate)
    - Test 4: `model_dump()` of a pack includes the `about_text` key (one model owns the wire format per D-02; no wrapper dict at the tool layer)
  </behavior>
  <action>
    Per D-01/D-02: add `about_text: str = ""` as a defaulted field on `EvidencePack` in src/models.py, declared immediately after `retrieval_status` so the serialized wire format leads with status then the cleaned about text. This is additive and defaulted, so existing constructions and Phase 9 tests remain valid against the frozen `extra="forbid"` base.

    In `EvidencePack.from_context`, add `about_text=about_text` to the `return cls(...)` call. `from_context` already receives `about_text` as its first parameter and already uses it for status computation; the only change is storing it. It stores the value verbatim: capping is the caller's job at the MCP boundary (Phase 9 D-10, implemented in plan 10-02's evidence.py per D-03), never the model's.

    Write the behavior tests above in tests/unit/test_models.py following its existing style (plain functions, crafted inputs, no mocks).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "about_text: str = \"\"" src/models.py` returns 1
    - `uv run python -c "from src.models import EvidencePack; p = EvidencePack(retrieval_status='empty'); assert p.about_text == ''"` exits 0
    - `uv run python -c "from src.models import EvidencePack; p = EvidencePack.from_context(about_text='x'*500, news_items=[], justifications=(), about_text_min_chars=200); assert p.about_text == 'x'*500 and p.retrieval_status == 'ok'"` exits 0
    - `uv run pytest tests/unit/test_models.py -q` passes with zero failures (Phase 9 tests untouched and green)
    - `make typecheck` passes
  </acceptance_criteria>
  <done>EvidencePack serializes about_text; from_context stores it; all pre-existing model tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Settings.mcp_demo_mode and Settings.mcp_tier() (MCP-06)</name>
  <files>src/config.py, tests/unit/test_config.py</files>
  <read_first>
    - src/config.py (Settings fields lines 20-27, resolved_provider property lines 119-125, require_for_pipeline lines 153-176 whose fail-fast message style mcp_tier mirrors)
    - tests/unit/test_config.py (monkeypatch + `Settings(_env_file=None)` test convention to replicate exactly)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-PATTERNS.md section "src/config.py::Settings.mcp_tier()"
  </read_first>
  <behavior>
    - Test 1: only EXA_API_KEY set -> `mcp_tier()` returns "thin"
    - Test 2: EXA + DEEPSEEK_API_KEY + BROWSERBASE_API_KEY + BROWSERBASE_PROJECT_ID set -> returns "full"
    - Test 3: all keys set + MCP_DEMO_MODE=1 -> returns "thin" (demo forces thin regardless of keys)
    - Test 4: EXA_API_KEY absent -> raises RuntimeError whose message matches "EXA_API_KEY" (fail fast even in demo mode, since no tier can retrieve without Exa)
    - Test 5: provider gating for full mirrors resolved_provider: LLM_PROVIDER=deepseek with only NVIDIA_API_KEY (no DeepSeek key) plus both Browserbase keys -> "thin", because the resolved provider's own key is missing
    - Test 6: EXA + provider key but missing one of the two Browserbase keys -> "thin"
    - Test 7: `mcp_demo_mode` defaults to False when MCP_DEMO_MODE is unset
  </behavior>
  <action>
    Add `mcp_demo_mode: bool = False` to `Settings` in src/config.py (pydantic-settings maps it to the `MCP_DEMO_MODE` env var automatically; "1" parses truthy). Scope note (Claude's Discretion resolved): add ONLY this field this phase. The design spec's remaining MCP fields (mcp_demo_ip_limit, mcp_demo_daily_cap, mcp_demo_exa_results, mcp_http_port) are Phase 11 scope; do not add them.

    Add a method `def mcp_tier(self) -> Literal["thin", "full"]` placed after `require_for_pipeline` (a raising method, not a property, matching require_for_pipeline's shape as locked by 10-CONTEXT.md). Logic, in this exact order:
    1. If `self.exa_api_key` is empty: raise `RuntimeError` using the same missing-keys listing message style as require_for_pipeline, e.g. `missing required env vars for MCP server: EXA_API_KEY. See .env.example.` The EXA key is non-optional for any MCP tier, so this check precedes the demo-mode early return.
    2. If `self.mcp_demo_mode`: return "thin" (MCP_DEMO_MODE=1 forces thin regardless of other keys, mirroring the demo_bundle early-return precedent).
    3. Full-tier gate: the resolved provider's own key must be present (deepseek -> deepseek_api_key, nvidia -> nvidia_api_key, reusing `self.resolved_provider` exactly like require_for_pipeline) AND both `browserbase_api_key` and `browserbase_project_id` must be truthy. If all hold, return "full"; otherwise return "thin".

    `require_for_pipeline()` itself is unchanged. Add a short why-docstring on mcp_tier noting thin = retrieval-only (Exa, optional Browserbase), full = adds writer/judge LLM tooling (registered in Phase 12).

    Write the behavior tests above in tests/unit/test_config.py, replicating the existing monkeypatch delenv/setenv + `Settings(_env_file=None)` pattern so local .env contents never leak into assertions.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config.py -k mcp -x -q && uv run pytest tests/unit/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def mcp_tier" src/config.py` returns 1
    - `grep -c "mcp_demo_mode: bool = False" src/config.py` returns 1
    - `uv run pytest tests/unit/test_config.py -k mcp -q` collects at least 6 tests and passes
    - `uv run pytest tests/unit/test_config.py -q` passes (pre-existing config tests untouched and green)
    - Behavior assertion: `EXA_API_KEY=x MCP_DEMO_MODE=1 DEEPSEEK_API_KEY=d BROWSERBASE_API_KEY=b BROWSERBASE_PROJECT_ID=p uv run python -c "from src.config import Settings; assert Settings(_env_file=None).mcp_tier() == 'thin'"` exits 0
    - `make typecheck` and `make lint` pass
  </acceptance_criteria>
  <done>mcp_tier() resolves thin/full/demo-forced-thin correctly, fails fast on missing EXA key with the listing-style message, and require_for_pipeline is byte-identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> dev machine | third-party package code enters the project via `uv add` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-SC | Tampering | `uv add mcp` install | high | mitigate | RESEARCH.md Package Legitimacy Audit approved `mcp` (seam's SUS flag resolved as false positive via direct PyPI JSON API + official modelcontextprotocol/python-sdk GitHub cross-check); Task 1 additionally verifies resolved version >=1.28 and official repo metadata via `uv pip show mcp` before proceeding |
| T-10-06 | Denial of Service | dependency resolver (pydantic bump) | low | accept | forced pydantic upgrade could break existing behavior silently; mitigated in practice by running the full offline suite + strict mypy in Task 1 before any new code lands; residual risk accepted because the suite is the project's regression gate |
</threat_model>

<verification>
- `make test`, `make typecheck`, `make lint` all green after each task
- `uv run python -c "from mcp.server.fastmcp import FastMCP"` exits 0
- EvidencePack about_text default + passthrough proven by unit tests
- mcp_tier() matrix (thin / full / demo-forced-thin / missing-EXA raise) proven by unit tests
</verification>

<success_criteria>
- mcp>=1.28,<2.0 in pyproject.toml, uv.lock regenerated, suite green on resolved deps
- EvidencePack.about_text exists, defaulted, stored by from_context, present in model_dump()
- Settings.mcp_tier() implements the locked tier matrix and fail-fast message; Settings.mcp_demo_mode reads MCP_DEMO_MODE
- Zero changes to require_for_pipeline, zero new mypy overrides
</success_criteria>

<output>
Create `.planning/phases/10-stdio-mcp-server-thin-tier/10-01-SUMMARY.md` when done.
</output>

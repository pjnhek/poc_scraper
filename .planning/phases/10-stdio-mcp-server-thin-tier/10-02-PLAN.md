---
phase: 10-stdio-mcp-server-thin-tier
plan: 2
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/mcp_server/__init__.py
  - src/mcp_server/evidence.py
  - src/mcp_server/wiring.py
  - src/mcp_server/server.py
  - src/mcp_server/__main__.py
  - Makefile
  - tests/unit/test_evidence.py
  - tests/functional/test_mcp_server.py
autonomous: true
requirements: [MCP-01, MCP-06, MCP-07, HOST-01]

must_haves:
  truths:
    - "An in-memory MCP client calling get_account_evidence(domain) receives structured JSON with retrieval_status, numbered justifications, capped about_text, and a capped news list"
    - "Invalid domains, provider failures, and unexpected exceptions all surface as isError: true tool results with sanitized human-readable text, never protocol-level errors"
    - "The tool carries readOnlyHint=True and destructiveHint=False annotations and its description states the citation-numbering contract"
    - "The server resolves and logs its capability tier once at startup and routes all logging to stderr"
    - "make mcp starts the stdio server via python -m src.mcp_server"
  artifacts:
    - path: "src/mcp_server/evidence.py"
      provides: "MCP-boundary caps and RawContext -> EvidencePack composition"
      contains: "ABOUT_TEXT_MCP_CAP"
    - path: "src/mcp_server/wiring.py"
      provides: "ThinDeps bundle and lifespan with key-aware Browserbase selection"
      contains: "class ThinDeps"
    - path: "src/mcp_server/server.py"
      provides: "FastMCP app factory, tool registration with annotations, sanitizing error wrapper, tier logging"
      contains: "def build_server"
    - path: "src/mcp_server/__main__.py"
      provides: "stdio entrypoint with stderr-first logging"
      contains: "stream=sys.stderr"
    - path: "tests/functional/test_mcp_server.py"
      provides: "in-memory MCP client coverage of MCP-01, MCP-06, MCP-07"
      contains: "create_connected_server_and_client_session"
    - path: "Makefile"
      provides: "make mcp target"
      contains: "python -m src.mcp_server"
  key_links:
    - from: "src/mcp_server/server.py"
      to: "src/mcp_server/evidence.py"
      via: "tool body delegates to build_evidence_pack"
      pattern: "build_evidence_pack"
    - from: "src/mcp_server/evidence.py"
      to: "src/enrich.py"
      via: "imports collect_context, _number_justifications, ABOUT_TEXT_MIN_CHARS so MCP status thresholds never drift from CLI"
      pattern: "ABOUT_TEXT_MIN_CHARS"
    - from: "src/mcp_server/wiring.py"
      to: "src/clients/browserbase_client.py"
      via: "key-aware construction of BrowserbaseClient vs NullBrowserbase"
      pattern: "NullBrowserbase"
    - from: "src/mcp_server/__main__.py"
      to: "src/mcp_server/server.py"
      via: "resolve_and_log_tier + build_server called after stderr logging config"
      pattern: "resolve_and_log_tier"
---

<objective>
Build the `src/mcp_server/` package: the thin-tier evidence composition with MCP-boundary size caps (MCP-01, D-03), the lifespan wiring with key-aware Browserbase selection (D-04), the FastMCP app with the `get_account_evidence` tool, read-only annotations, sanitizing error wrapper (MCP-07, D-07, D-08), startup tier logging (MCP-06), the stderr-first stdio entrypoint plus `make mcp` (HOST-01), and the in-memory functional test suite that proves all of it offline.

Purpose: this is the phase's core deliverable, a real MCP server exposing grounded, cited account evidence over stdio with the project's honesty guarantees intact.
Output: four-module package + __main__ entrypoint, Makefile `mcp` target, unit tests for capping, functional tests via `mcp.shared.memory`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-CONTEXT.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-RESEARCH.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-PATTERNS.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-01-SUMMARY.md
@docs/superpowers/specs/2026-07-15-mcp-server-design.md
@src/enrich.py
@src/pipeline.py
@src/clients/browserbase_client.py
@src/clients/protocols.py
</context>

## Interfaces from plan 10-01 (already on disk when this plan runs)

- `EvidencePack` now has `about_text: str = ""`; `from_context(about_text, news_items, justifications, *, about_text_min_chars)` stores about_text verbatim
- `Settings.mcp_tier() -> Literal["thin", "full"]` raises RuntimeError listing EXA_API_KEY when missing; `Settings.mcp_demo_mode: bool`
- `mcp==1.28.x` installed; `FastMCP`, `ToolAnnotations`, `mcp.shared.memory` all importable

## Artifacts this phase produces

Plan 10-02 creates these symbols (the plan-review source-grounding pass should exclude them from drift verification):

- `src/mcp_server/__init__.py`: empty package init (no re-export barrel, matching `src/clients/__init__.py`)
- `src/mcp_server/evidence.py`: constants `ABOUT_TEXT_MCP_CAP = 2000`, `JUSTIFICATION_SUMMARY_MCP_CAP = 300`, `NEWS_ITEM_MCP_CAP = 10`; helper `_truncate_words(text: str, cap: int) -> str`; pure function `pack_from_context(ctx: RawContext) -> EvidencePack`; async function `build_evidence_pack(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike) -> EvidencePack`
- `src/mcp_server/wiring.py`: frozen dataclass `ThinDeps` (fields `exa: ExaLike`, `browserbase: BrowserbaseLike`); factory `make_thin_lifespan(settings: Settings) -> Callable[[FastMCP], AbstractAsyncContextManager[ThinDeps]]`
- `src/mcp_server/server.py`: async tool function `get_account_evidence(domain: str, ctx: Context[ServerSession, ThinDeps]) -> EvidencePack`; factory `build_server(lifespan) -> FastMCP`; `resolve_and_log_tier(settings: Settings) -> Literal["thin", "full"]`
- `src/mcp_server/__main__.py`: stderr-first stdio entrypoint (`main() -> int`)
- `Makefile`: new `mcp` target
- `tests/unit/test_evidence.py`, `tests/functional/test_mcp_server.py`: new test files

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evidence.py with MCP-boundary caps and pure pack composition (MCP-01, D-01, D-03)</name>
  <files>src/mcp_server/__init__.py, src/mcp_server/evidence.py, tests/unit/test_evidence.py</files>
  <read_first>
    - src/enrich.py (collect_context lines 34-56, _number_justifications lines 129-167, _clean_summary lines 177-189 whose word-boundary + ellipsis mechanic _truncate_words copies, ABOUT_TEXT_MIN_CHARS line 14)
    - src/models.py (EvidencePack with the new about_text field; Justification model at lines 72-83)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-PATTERNS.md section "src/mcp_server/evidence.py" (imports and composition shape)
    - tests/unit/test_models.py (crafted-input unit test style)
  </read_first>
  <behavior>
    - Test 1: about_text longer than 2000 chars -> pack.about_text has length <= 2000, ends with the ellipsis character, and is cut at a word boundary (no trailing partial word before the ellipsis)
    - Test 2: about_text at or under 2000 chars passes through unmodified
    - Test 3: RawContext with 15 news items -> pack.news has exactly 10 entries and pack.justifications only numbers the capped 10 (plus about citations), so indices and the news field agree
    - Test 4: _truncate_words applied defensively to a Justification summary crafted above 300 chars returns a capped summary; summaries at the normal <=140 chars from _clean_summary pass through untouched (the 300 cap is a defensive ceiling per RESEARCH.md Open Question 1)
    - Test 5: status honesty preserved through capping: empty RawContext -> retrieval_status "empty"; about_text under ABOUT_TEXT_MIN_CHARS with no news -> "thin"; rich about_text -> "ok" (capping to 2000 can never push a >=200-char text below the 200-char threshold, assert "ok" survives a 5000-char input)
    - Test 6: pack.about_text is populated on the returned EvidencePack (D-01 payload contract)
  </behavior>
  <action>
    Create `src/mcp_server/__init__.py` empty (convention: no re-export barrels, matching src/clients/__init__.py).

    Create `src/mcp_server/evidence.py`. Use absolute imports (`from src.enrich import ...`, `from src.models import ...`, `from src.clients.protocols import ...`) since this subpackage sits one level down; match the RESEARCH.md code examples. Module-level SCREAMING_SNAKE constants per D-03 (values resolved from Claude's Discretion): `ABOUT_TEXT_MCP_CAP = 2000`, `JUSTIFICATION_SUMMARY_MCP_CAP = 300`, `NEWS_ITEM_MCP_CAP = 10`. No env knobs for these (Phase 9 D-09 principle); the Exa results clamp is Phase 11 scope.

    Implement `_truncate_words(text: str, cap: int) -> str`: return text unchanged when len <= cap; otherwise cut at `text[: cap - 1].rsplit(" ", 1)[0]` and append the ellipsis character, copying src/enrich.py's _clean_summary mechanic (word boundary + ellipsis, never a hard slice). Do not replicate _clean_summary's markdown-chrome stripping; about_text is already assembled text.

    Implement pure `pack_from_context(ctx: RawContext) -> EvidencePack`:
    1. Cap the news list FIRST: `news = list(ctx.news_items)[:NEWS_ITEM_MCP_CAP]`.
    2. Number justifications from `ctx.about_citations` plus the CAPPED news tuple via `_number_justifications`, so justification indices and the news field stay internally consistent (numbering uncapped news would emit indices for items absent from the payload).
    3. Defensively cap each justification summary at JUSTIFICATION_SUMMARY_MCP_CAP via `model_copy(update={"summary": ...})` only when over the cap (frozen models support model_copy; today _clean_summary already caps at 140 so this is a future-proof ceiling, applied anyway per RESEARCH.md's recommendation).
    4. Cap about_text: `_truncate_words(ctx.about_text, ABOUT_TEXT_MCP_CAP)`.
    5. Return `EvidencePack.from_context(about_text=..., news_items=news, justifications=..., about_text_min_chars=ABOUT_TEXT_MIN_CHARS)` where ABOUT_TEXT_MIN_CHARS is IMPORTED from src.enrich (RESEARCH.md Pitfall 6: never hardcode the thin threshold, so MCP retrieval_status and CLI behavior cannot drift). Capping happens before model construction, honoring Phase 9 D-10.

    Implement `async def build_evidence_pack(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike) -> EvidencePack`: await `collect_context(account, exa=exa, browserbase=browserbase)`, delegate to `pack_from_context`. evidence.py must NOT catch exceptions; stage modules raise and the caller (server.py's tool wrapper) catches, mirroring how Enricher.enrich propagates to process_account.

    Write tests/unit/test_evidence.py against the PURE surface only (pack_from_context and _truncate_words with crafted RawContext/Justification inputs, no stubs, no I/O), honoring the 5-layer strategy's unit-layer definition. Build RawContext instances directly (it is a plain frozen dataclass importable from src.enrich).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_evidence.py -x -q && uv run mypy src/mcp_server tests/unit/test_evidence.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^#" src/mcp_server/evidence.py | grep -c "ABOUT_TEXT_MCP_CAP = 2000"` returns 1; same for `JUSTIFICATION_SUMMARY_MCP_CAP = 300` and `NEWS_ITEM_MCP_CAP = 10`
    - `grep -c "from src.enrich import" src/mcp_server/evidence.py` returns 1 and the imported names include ABOUT_TEXT_MIN_CHARS, _number_justifications, collect_context, RawContext
    - Source assertion: evidence.py contains no try/except around collect_context or pack construction (error translation lives in server.py only)
    - `uv run pytest tests/unit/test_evidence.py -q` passes with at least 6 tests covering the behavior list
    - Behavior assertion: a 5000-char about_text input yields pack.about_text length <= 2000 AND retrieval_status == "ok"
  </acceptance_criteria>
  <done>Pure capping + composition proven by unit tests; caps are constants; thin threshold imported, not duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: wiring.py lifespan (D-04), server.py tool + error wrapper + tier logging (MCP-06, MCP-07, D-07, D-08), __main__.py stdio entrypoint and make mcp (HOST-01)</name>
  <files>src/mcp_server/wiring.py, src/mcp_server/server.py, src/mcp_server/__main__.py, Makefile</files>
  <read_first>
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-RESEARCH.md sections "Architecture Patterns" (Patterns 1-3, verified against installed mcp==1.28.1, including the confirmed lifespan-runs-once-per-process guarantee and the SDK's unconditional isError conversion) and "Common Pitfalls" (Pitfalls 1, 2, 4, 5)
    - src/pipeline.py (open_deps lines 69-128, the lifespan shape wiring.py mirrors; process_account error-wrapping style lines 131-148; the pattern source ONLY, wiring.py must not import from src.pipeline because open_deps constructs LLM clients unconditionally, wrong for the thin tier per Phase 9 D-04)
    - src/clients/browserbase_client.py (BrowserbaseClient constructor, NullBrowserbase lines 87-100)
    - src/clients/protocols.py (ExaLike, BrowserbaseLike)
    - src/models.py (Account validator lines 21-34; its ValueError message shape "invalid domain: ...")
    - Makefile (existing .PHONY line and target style)
  </read_first>
  <action>
    **wiring.py** (analog: open_deps, thin-tier subset). Define `@dataclass(frozen=True) class ThinDeps` with fields `exa: ExaLike` and `browserbase: BrowserbaseLike`. Define `make_thin_lifespan(settings: Settings) -> Callable[[FastMCP], AbstractAsyncContextManager[ThinDeps]]`: a factory returning an `@asynccontextmanager`-decorated inner `async def lifespan(app: FastMCP) -> AsyncIterator[ThinDeps]` that opens ONE `httpx.AsyncClient(timeout=60.0)` via `async with`, constructs `ExaClient(api_key=settings.exa_api_key, client=http)`, then key-aware Browserbase selection per D-04: construct `BrowserbaseClient(api_key=..., project_id=..., client=http)` when BOTH `settings.browserbase_api_key` and `settings.browserbase_project_id` are truthy, else `NullBrowserbase()`. Yield `ThinDeps(exa=exa, browserbase=bb)`. No teardown beyond the `async with` (no LLM clients exist in the thin tier, so open_deps' finally-block aclose logic has no counterpart here). The factory-closure shape exists so Settings is resolved once at startup by the entrypoint and tests can build lifespans around fakes. The lifespan is entered exactly once per stdio process (verified SDK guarantee, RESEARCH.md Pattern 1).

    **server.py**. Three symbols:

    1. `resolve_and_log_tier(settings: Settings) -> Literal["thin", "full"]`: calls `settings.mcp_tier()` (letting its RuntimeError propagate, its message already lists the missing EXA key), logs ONE INFO line with %-placeholders including the tier and the retrieval sub-mode ("exa+browserbase" when both Browserbase keys are set, "exa-only" otherwise, Claude's Discretion resolved to include it), and returns the tier. Kept as a small named function so functional tests can assert the log with caplog (MCP-06's "logs the resolved tier").

    2. Module-level `async def get_account_evidence(domain: str, ctx: Context[ServerSession, ThinDeps]) -> EvidencePack` (Context and ServerSession from `mcp.server.fastmcp` / `mcp.server.session`; the two-parameter Context generic follows the official SDK README convention, adjust parameterization only if strict mypy demands it against the installed SDK). The docstring IS the MCP tool description clients see (Pitfall 4), so front-load the citation contract, adapting RESEARCH.md Pattern 2's wording: retrieval of numbered, cited evidence (about page + last-90-day news); every downstream claim MUST cite a justification by its [N] index; retrieval_status semantics ok/thin/empty with empty meaning "cannot research this account". No em-dashes in the docstring.

       Body, per D-07/D-08 and RESEARCH.md Pattern 3 (the SDK already guarantees any raised exception becomes an isError: true tool result, so this wrapper's ONLY job is controlling message text):
       - Validate first: `Account(domain=domain)` inside try/except `pydantic.ValidationError`; on failure raise `ValueError` carrying the validator's message so the client sees the "invalid domain: ..." text (D-08 passthrough). Extract the human message from the ValidationError (first error's msg, stripping pydantic's "Value error, " prefix) rather than str(exc), which buries it in multi-line scaffolding.
       - Fetch deps: `deps = ctx.request_context.lifespan_context`.
       - Retrieval: `return await build_evidence_pack(account, exa=deps.exa, browserbase=deps.browserbase)` inside a try that catches `(httpx.HTTPError, BrowserbaseError)` -> log.warning with type(exc).__name__ and domain, then `raise ValueError("retrieval unavailable, try again") from None` (fixed message per D-08; from None so no internal detail chains into the message).
       - Sanitizing catch-all (D-07): final `except Exception` -> log.warning with exc_info=True (full traceback to stderr only), then `raise ValueError("internal error, try again") from None`. Never rely on the SDK's default str(exc), which leaks URLs/paths/provider bodies.
       - Empty/thin retrieval is NOT an error; it flows through as a successful result with retrieval_status (D-08).
       - Do NOT import ToolError (Pitfall 2); plain ValueError produces an identical isError result.

    3. `build_server(lifespan: Callable[[FastMCP], AbstractAsyncContextManager[ThinDeps]]) -> FastMCP`: constructs `FastMCP("poc-scraper", lifespan=lifespan)` and registers the tool via `server.tool(annotations=ToolAnnotations(readOnlyHint=True, destructiveHint=False))(get_account_evidence)` (MCP-07 annotations; ToolAnnotations from mcp.types). The `-> EvidencePack` return annotation triggers the SDK's auto-structured-output path; do NOT call model_dump() at the tool boundary (D-02: one model owns the wire format).

    **__main__.py** (entry point for `python -m src.mcp_server`, required by the locked `make mcp` contract; the locked four-file layout plus this module-runner shim). The LITERAL FIRST executable statements: `import logging`, `import sys`, then `logging.basicConfig(stream=sys.stderr, level=logging.INFO, format="%(asctime)s %(levelname)s %(name)s %(message)s")` BEFORE any project or third-party import that could write to stdout at import time (Pitfall 1: stdout is reserved exclusively for JSON-RPC frames). Follow with the remaining imports marked with a file-level ruff suppression for import-position (E402) plus a WHY comment explaining the deliberate ordering. Then `def main() -> int`: `settings = get_settings()`, `resolve_and_log_tier(settings)` (tier resolved and logged once at startup; a missing EXA key raises RuntimeError whose message lists it, acceptable fail-fast), `app = build_server(lifespan=make_thin_lifespan(settings))`, `asyncio.run(app.run_stdio_async())`, return 0. Guard with `if __name__ == "__main__": raise SystemExit(main())`. Do NOT call the synchronous `FastMCP.run()` (it starts its own anyio loop; RESEARCH.md anti-pattern). No stdout print calls anywhere in src/mcp_server/.

    **Makefile**: add `mcp` to the .PHONY line and a target `mcp:` running `uv run python -m src.mcp_server`, matching the existing target style.
  </action>
  <verify>
    <automated>uv run python -c "import asyncio; from src.mcp_server.server import build_server; from src.mcp_server.wiring import make_thin_lifespan; from src.config import Settings; app = build_server(lifespan=make_thin_lifespan(Settings(_env_file=None, exa_api_key='x'))); tools = asyncio.run(app.list_tools()); assert [t.name for t in tools] == ['get_account_evidence'], tools; ann = tools[0].annotations; assert ann is not None and ann.readOnlyHint is True and ann.destructiveHint is False" && uv run mypy src && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rh "print(" src/mcp_server/ | grep -v "^\s*#" | wc -l` outputs 0 (stdout purity, Pitfall 1)
    - Source assertion: src/mcp_server/wiring.py's import block contains no import from src.pipeline (thin tier wires itself per Phase 9 D-04; open_deps is full-tier only)
    - Source assertion: src/mcp_server/server.py contains no import of ToolError (plain exceptions suffice, Pitfall 2)
    - `grep -c "stream=sys.stderr" src/mcp_server/__main__.py` returns 1, and it appears before any `from src.` import in the file
    - `grep -c "readOnlyHint=True" src/mcp_server/server.py` returns 1 and `grep -c "destructiveHint=False" src/mcp_server/server.py` returns 1
    - `grep -c "NullBrowserbase" src/mcp_server/wiring.py` returns at least 1 (key-aware fallback branch exists, D-04 / Pitfall 5)
    - `grep -v "^#" Makefile | grep -c "python -m src.mcp_server"` returns 1
    - The verify one-liner exits 0 (tool registered with both annotation hints)
    - `uv run mypy src` passes with zero errors; `make lint` passes
  </acceptance_criteria>
  <done>Package imports cleanly, tool registered with read-only annotations, lifespan is thin-tier-only and key-aware, entrypoint logs to stderr before any project import, make mcp exists.</done>
</task>

<task type="auto">
  <name>Task 3: Functional tests via the in-memory MCP client (MCP-01, MCP-06, MCP-07)</name>
  <files>tests/functional/test_mcp_server.py</files>
  <read_first>
    - src/mcp_server/server.py and src/mcp_server/wiring.py (as built in Task 2)
    - tests/functional/test_enrich.py (FakeExa lines 14-32 and FakeBrowserbase, the stub pattern to reuse; import FakeExa from this module rather than duplicating it, tests are importable packages via __init__.py)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-RESEARCH.md section "Code Examples" (create_connected_server_and_client_session usage) and "Open Questions" item 2 (call-shape guidance)
    - src/clients/exa_client.py (ExaResult fields, for crafting fake results)
  </read_first>
  <action>
    Create tests/functional/test_mcp_server.py using `mcp.shared.memory.create_connected_server_and_client_session` (locked pattern: no subprocess, no network). Build a test lifespan factory that mirrors make_thin_lifespan's shape but yields `ThinDeps(exa=FakeExa(...), browserbase=NullBrowserbase())`; pass it to `build_server`. Per RESEARCH.md Open Question 2: try passing the FastMCP instance directly to create_connected_server_and_client_session first (its signature accepts Server or FastMCP); if tool calls misbehave, fall back to passing the instance's `._mcp_server` attribute. Call `await client.initialize()` after entering the session per the RESEARCH example; if the installed helper auto-initializes and a second initialize errors, drop the explicit call.

    Test cases (async, pytest-asyncio auto mode, no new markers):
    1. Happy path (MCP-01): FakeExa returning a rich about snippet (reuse test_enrich.py's _exa_about-style 30x-repeated text) plus one news result. call_tool("get_account_evidence", {"domain": "notion.so"}) -> `result.isError is False`; `result.structuredContent` is not None; `structuredContent["retrieval_status"] == "ok"`; `structuredContent["justifications"]` non-empty with each entry carrying an integer index >= 1, a summary, and a citation with a url; `structuredContent["about_text"]` is a non-empty string (D-01).
    2. Thin/empty honesty (MCP-01, D-08): FakeExa returning nothing -> isError is False (empty retrieval is NOT an error) and retrieval_status == "empty".
    3. Invalid domain (MCP-07, D-08): call with domain "not a domain" -> `result.isError is True` and the error text (result.content[0].text) contains "invalid domain"; assert it does NOT contain "Traceback".
    4. Provider failure sanitized (MCP-07, D-08): a FakeExa variant whose search_about raises `httpx.HTTPError("connect boom secret-host")` -> isError is True, text contains "retrieval unavailable, try again", and does NOT contain "secret-host" (proves the fixed-message path, not str(exc) passthrough). Note the SDK prefixes tool error text with "Error executing tool get_account_evidence:", so use substring assertions, never equality.
    5. Unexpected exception sanitized (MCP-07, D-07): a FakeExa variant raising `RuntimeError("/private/path/leak")` -> isError is True, text contains "internal error", does NOT contain "/private/path/leak"; additionally assert via caplog that a WARNING record was emitted (full detail goes to the server log, never the client).
    6. Annotations over the wire (MCP-07): `await client.list_tools()` -> the get_account_evidence tool's annotations have readOnlyHint True and destructiveHint False, and its description contains "[N]" (citation contract present, Pitfall 4).
    7. Tier logging (MCP-06): call `resolve_and_log_tier(Settings(_env_file=None, exa_api_key="x"))` with caplog at INFO -> returns "thin" and some record message contains "thin"; repeat with all keys set -> returns "full" and a record contains "full".

    Every test fully annotated (strict mypy). Close with the full offline gate.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -x -q && make test && make typecheck && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_mcp_server.py -q` passes with at least 7 tests
    - `uv run pytest tests/functional/test_mcp_server.py -k error -q` runs at least 3 tests (invalid domain, provider failure, unexpected exception) and passes (matches RESEARCH.md's MCP-07 validation command)
    - Behavior assertion: no test asserts on protocol-level JSON-RPC errors; all failure cases assert `result.isError is True` on a CallToolResult (MCP-07's never-protocol-errors contract)
    - `make test` passes (whole offline suite green with the new files)
    - `make typecheck` and `make lint` pass
  </acceptance_criteria>
  <done>MCP-01, MCP-06, MCP-07 all covered offline through a real in-memory MCP client; sanitization proven by leak-string absence assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> stdio server | untrusted tool arguments (domain string) enter via JSON-RPC |
| server -> Exa/Browserbase | untrusted third-party response bodies enter the process |
| server -> MCP client | everything returned or raised becomes client-visible text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | get_account_evidence error path (server.py) | high | mitigate | D-07 sanitizing wrapper: fixed messages for known failures, generic "internal error, try again" catch-all, full traceback to stderr only via log.warning(exc_info=True); functional tests 4-5 assert leak strings never reach the client |
| T-10-02 | Tampering | stdio JSON-RPC stream (protocol integrity) | high | mitigate | stderr-first logging.basicConfig as the literal first statement in __main__.py; zero stdout print calls in src/mcp_server/ (grep-gated in Task 2); real-transport verification lands in plan 10-03 |
| T-10-03 | Spoofing / Injection | domain argument -> Exa query | medium | mitigate | primary trust boundary, so mitigated at L1: existing Account field_validator is the single validation source (strips protocol/www, requires a dot, rejects spaces); the MCP wrapper only translates ValidationError, never re-implements validation |
| T-10-04 | Denial of Service | oversized EvidencePack JSON exhausting the calling agent's context | medium | mitigate | primary trust boundary: D-03 caps (about_text 2000, summaries 300, news 10) applied in evidence.py before model construction; unit-tested in Task 1 |
| T-10-05 | Repudiation | untraceable tool failures | low | accept | WARNING-level stderr logs with domain + exception type cover the solo-operator debugging need; structured audit logging is out of scope for a local read-only tool |
</threat_model>

<verification>
- `make test`, `make typecheck`, `make lint` all green
- In-memory client round-trip proves: structured EvidencePack JSON with retrieval_status + numbered justifications + capped about_text; isError-only failure surfacing; annotations on the wire; tier logged
- Tool listing one-liner proves registration + annotations without a client
- stdout purity grep gate clean across src/mcp_server/
</verification>

<success_criteria>
- src/mcp_server/{__init__,evidence,wiring,server,__main__}.py exist and pass strict mypy with zero new overrides
- get_account_evidence returns EvidencePack directly (auto-structured output), caps enforced at the boundary, thin threshold imported from src.enrich
- All three error categories produce sanitized isError results; empty/thin retrieval is a successful result
- make mcp runs the stdio server; all logging routed to stderr
- Functional + unit suites cover MCP-01, MCP-06, MCP-07 offline
</success_criteria>

<output>
Create `.planning/phases/10-stdio-mcp-server-thin-tier/10-02-SUMMARY.md` when done.
</output>

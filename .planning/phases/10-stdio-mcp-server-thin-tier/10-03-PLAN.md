---
phase: 10-stdio-mcp-server-thin-tier
plan: 3
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - tests/smoke/test_mcp_e2e.py
  - Makefile
autonomous: false
requirements: [TEST-02, HOST-01]

must_haves:
  truths:
    - "make smoke-mcp runs the stdio server as a real subprocess against one live domain and asserts non-empty numbered justifications plus a retrieval_status field"
    - "The MCP smoke test skips cleanly when EXA_API_KEY is absent and is excluded from make test (skipped in CI)"
    - "A real MCP client (Claude Code) connects to the server over stdio via claude mcp add and retrieves cited evidence in a live session"
  artifacts:
    - path: "tests/smoke/test_mcp_e2e.py"
      provides: "opt-in subprocess smoke over real stdio transport"
      contains: "stdio_client"
    - path: "Makefile"
      provides: "make smoke-mcp target"
      contains: "smoke-mcp"
  key_links:
    - from: "tests/smoke/test_mcp_e2e.py"
      to: "src/mcp_server/__main__.py"
      via: "spawns python -m src.mcp_server as a real subprocess over stdio pipes"
      pattern: "src.mcp_server"
    - from: "Makefile"
      to: "tests/smoke/test_mcp_e2e.py"
      via: "smoke-mcp target runs the file with pytest"
      pattern: "test_mcp_e2e"
---

<objective>
Close the phase with the two transport-reality gates: an opt-in `make smoke-mcp` that exercises the stdio server as a REAL subprocess against one live domain (TEST-02, D-06), and a human verification session connecting Claude Code as a real MCP client (HOST-01, D-05). The in-memory functional tests from plan 10-02 cannot catch stdout contamination by construction (they never touch stdin/stdout); only these two gates prove the actual transport.

Purpose: roadmap success criteria 1, 4, and 5 are only observable over a real stdio transport.
Output: tests/smoke/test_mcp_e2e.py, Makefile smoke-mcp target, human-verified real-client connection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-CONTEXT.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-RESEARCH.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-02-SUMMARY.md
@tests/smoke/test_e2e.py
@tests/smoke/fixtures.csv
</context>

## Artifacts this phase produces

Plan 10-03 creates these symbols (the plan-review source-grounding pass should exclude them from drift verification):

- `tests/smoke/test_mcp_e2e.py`: new smoke test module (`_skip_if_no_exa_key` fixture, `test_stdio_server_returns_live_evidence`)
- `Makefile`: new `smoke-mcp` target

(Plans 10-01 and 10-02 created the `src/mcp_server/` package, `EvidencePack.about_text`, `Settings.mcp_tier()`, `Settings.mcp_demo_mode`, the `mcp` dependency, the `make mcp` target, and the unit/functional test files; see those plans' artifact sections.)

<tasks>

<task type="auto">
  <name>Task 1: Subprocess smoke test over real stdio plus make smoke-mcp (TEST-02, D-06)</name>
  <files>tests/smoke/test_mcp_e2e.py, Makefile</files>
  <read_first>
    - tests/smoke/test_e2e.py (pytestmark = pytest.mark.smoke and the module-scoped autouse _skip_if_no_keys fixture pattern to mirror, lines 25-42; the MCP variant requires ONLY EXA_API_KEY since the thin tier has zero LLM involvement)
    - tests/smoke/fixtures.csv (the two smoke domains; D-06 locks reuse of one of them)
    - src/mcp_server/__main__.py (the entrypoint the subprocess runs)
    - Makefile (existing smoke target style and .PHONY line, including the mcp target added by plan 10-02)
  </read_first>
  <action>
    Create tests/smoke/test_mcp_e2e.py with `pytestmark = pytest.mark.smoke` (existing registered marker; no new marker, so `make test` with `-m "not smoke"` keeps excluding it and CI stays clean per TEST-02).

    Skip guard: module-scoped autouse fixture `_skip_if_no_exa_key` that calls `pytest.skip("mcp smoke requires EXA_API_KEY")` when `get_settings().exa_api_key` is empty, mirroring test_e2e.py's fixture shape but requiring ONLY the Exa key (thin tier).

    One test, `test_stdio_server_returns_live_evidence` (smoke-driver mechanics resolved from Claude's Discretion):
    - Spawn the server as a REAL subprocess with the mcp SDK's stdio client: `StdioServerParameters(command=sys.executable, args=["-m", "src.mcp_server"], cwd=<repo root resolved as Path(__file__).parents[2]>, env=dict(os.environ))`. sys.executable is the uv venv interpreter pytest already runs under, avoiding a nested uv invocation; passing the full current environment guarantees EXA_API_KEY reaches the child whether it lives in the shell env or .env (the child also reads .env from cwd via pydantic-settings). Verify the exact import paths against the installed SDK: stdio_client and StdioServerParameters live in mcp.client.stdio (StdioServerParameters is also re-exported from the mcp top level), ClientSession from mcp.
    - Wrap the whole interaction in `async with asyncio.timeout(120):` (Python 3.11 stdlib) so a hung handshake fails the test rather than the run.
    - `async with stdio_client(params) as (read, write):` then `async with ClientSession(read, write) as session:` then `await session.initialize()` and `result = await session.call_tool("get_account_evidence", {"domain": "notion.so"})` (notion.so chosen from fixtures.csv per D-06 so MCP smoke and pipeline smoke burn credits against the same well-understood account).
    - Assertions per the roadmap and D-06: `result.isError is False`; `result.structuredContent is not None`; `structuredContent["retrieval_status"] in ("ok", "thin", "empty")` (honesty field present); `structuredContent["justifications"]` is NON-EMPTY; every justification has an integer `index >= 1`, a non-empty `summary`, and a `citation` whose url is a non-empty string (numbered, cited evidence over a real transport). A successful initialize + structured tool result also proves stdout carried only clean JSON-RPC frames end to end (HOST-01's stderr-routing criterion, automated half).

    Makefile: add `smoke-mcp` to .PHONY and a target `smoke-mcp:` running `uv run pytest tests/smoke/test_mcp_e2e.py -v`, mirroring the existing smoke target style. Note `make smoke` (pytest -m smoke) will now also collect this file; that is correct, both are opt-in credit burners per CLAUDE.md.

    Verification runs BOTH halves: the offline skip proof (EXA_API_KEY forced empty, test reports skipped) and one live `make smoke-mcp` run (roadmap success criterion 5 requires the live assertion to actually pass once; the local .env carries the Exa key). If the live run fails on retrieval quality rather than transport (e.g. status thin), the justification assertions still gate; investigate rather than loosening assertions.
  </action>
  <verify>
    <automated>EXA_API_KEY="" uv run pytest tests/smoke/test_mcp_e2e.py -v -rs 2>&1 | grep -q "1 skipped" && make test && make smoke-mcp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pytest.mark.smoke" tests/smoke/test_mcp_e2e.py` returns 1
    - `EXA_API_KEY="" uv run pytest tests/smoke/test_mcp_e2e.py -v` reports 1 skipped, 0 failed (skip guard works; this is the CI behavior)
    - `make test` passes and its output shows no tests collected from test_mcp_e2e.py running (smoke excluded)
    - `grep -v "^#" Makefile | grep -c "test_mcp_e2e"` returns 1 and .PHONY includes smoke-mcp
    - One live `make smoke-mcp` run exits 0 with the justification and retrieval_status assertions passing against notion.so
    - `make typecheck` and `make lint` pass (new file fully annotated)
  </acceptance_criteria>
  <done>Real-subprocess stdio smoke passes live once, skips without the key, and is invisible to make test / CI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-client verification session with Claude Code (HOST-01, D-05)</name>
  <action>
    Automation is complete before this checkpoint (server built, smoke passed live). Pause and present the verification steps below to the developer. Claude Code is the gating client per D-05; Claude Desktop is optional bonus, not gating (its config snippet is Phase 13 README work). Do not proceed to phase close until the developer confirms.
  </action>
  <what-built>
    A stdio MCP server (`make mcp` / `uv run python -m src.mcp_server`) exposing get_account_evidence with grounded, cited, capped evidence; sanitized isError failure results; stderr-only logging; tier logged at startup. Automated coverage: unit caps tests, in-memory functional tests (MCP-01/06/07), and a live subprocess smoke test. What remains is D-05's gate: HOST-01 requires verification against a REAL client connection, which only a human driving Claude Code can confirm end to end.
  </what-built>
  <how-to-verify>
    From the repository root:
    1. Register the server with Claude Code: `claude mcp add poc-scraper -- uv run python -m src.mcp_server` (stdio is the default transport). Registering from the repo root matters so the server process inherits the right working directory for .env and module resolution.
    2. Start a new `claude` session in this repo and run `/mcp`. Expected: poc-scraper listed as connected (a hang or failed initialize here is the stdout-contamination signature; see 10-RESEARCH.md Pitfall 1 warning signs).
    3. Ask: "Use the get_account_evidence tool to research notion.so and summarize what you find, citing evidence." Expected: the tool call succeeds and returns numbered justifications with URLs, a retrieval_status field, and about_text; the assistant's summary can cite [N] indices.
    4. Ask it to call the tool with the domain "not a domain". Expected: a polite error mentioning an invalid domain; NO stack trace, file path, or env detail in the visible error; the server stays connected and a follow-up valid call still works.
    5. Optionally check the server side: `claude mcp list` still shows poc-scraper healthy after the error call.
    6. Cleanup is optional; if desired: `claude mcp remove poc-scraper`.
  </how-to-verify>
  <resume-signal>Type "approved" if the real-client session behaved as described, or describe what went wrong (which step, what you saw).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke test -> child server process | full parent environment (including API keys) passed to the spawned server |
| real MCP client (Claude Code) -> server | live client drives the same stdio boundary as production use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | stdio JSON-RPC stream over a real transport | high | mitigate | this plan IS the mitigation's verification: subprocess smoke (automated) and real Claude Code session (human) both fail loudly if any stray byte reaches stdout; complements plan 10-02's stderr-first construction |
| T-10-01 | Information Disclosure | error text shown inside a real client session | high | mitigate | checkpoint step 4 verifies the sanitized invalid-domain message with no stack trace/paths/env detail as rendered by an actual client (HOST-05 discipline front-loaded per D-07) |
| T-10-07 | Information Disclosure | API keys in the smoke child-process environment | low | accept | the child is our own server binary spawned locally by the developer's own test run; keys never appear in pytest output or assertions; equivalent exposure already exists in tests/smoke/test_e2e.py |
</threat_model>

<verification>
- Offline: skip guard proven with EXA_API_KEY forced empty; make test unaffected
- Live: make smoke-mcp passes once against notion.so with non-empty numbered justifications and retrieval_status present
- Human: Claude Code connects via claude mcp add, retrieves cited evidence, and sees a sanitized error for an invalid domain without losing the connection
</verification>

<success_criteria>
- Roadmap success criterion 5 satisfied: make smoke-mcp = real subprocess, one live domain, non-empty numbered justifications, skipped in CI
- Roadmap success criteria 1 and 4 satisfied via the checkpoint: real client connection works and stdio framing is never contaminated
- Phase 10 requirements MCP-01, MCP-06, MCP-07, HOST-01, TEST-02 all verifiable across plans 10-01..10-03
</success_criteria>

<output>
Create `.planning/phases/10-stdio-mcp-server-thin-tier/10-03-SUMMARY.md` when done.
</output>

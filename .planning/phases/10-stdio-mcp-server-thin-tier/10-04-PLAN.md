---
phase: 10-stdio-mcp-server-thin-tier
plan: 4
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - src/mcp_server/evidence.py
  - tests/unit/test_evidence.py
  - src/models.py
  - tests/unit/test_models.py
  - tests/functional/test_mcp_server.py
autonomous: true
gap_closure: true
requirements: [MCP-01, MCP-07]

must_haves:
  truths:
    - "Every client-visible evidence-text path is made MCP-safe before EvidencePack construction: about_text and summaries are bounded, headlines are bounded, and nested Citation title/snippet text cannot bypass the boundary"
    - "Every returned EvidencePack is runtime-enforced at or below EVIDENCE_PACK_MAX_BYTES after exact UTF-8 serialization, including adversarial multibyte text and long provenance URLs"
    - "Byte-budget reduction is deterministic: retained citation URLs stay valid and byte-for-byte unchanged, lower-priority evidence is removed rather than corrupting provenance, indices are rebuilt sequentially, and retrieval_status remains honest"
    - "Account accepts a bare hostname or root-only HTTP(S) URL, normalizes it to a lowercase hostname, and rejects malformed hostname input before any provider call"
    - "Malformed path, query, fragment, userinfo, port, control-character, invalid-label, and overlength inputs return sanitized isError tool results while the Exa fake records zero calls"
  artifacts:
    - path: "src/mcp_server/evidence.py"
      provides: "MCP-safe copies plus an exact serialized UTF-8 byte-budget reducer for Citation, NewsItem, Justification, and EvidencePack"
      contains: "EVIDENCE_PACK_MAX_BYTES"
    - path: "tests/unit/test_evidence.py"
      provides: "Nested text-path assertions and adversarial long-URL plus multibyte serialized-byte-budget coverage"
      contains: "model_dump_json"
    - path: "src/models.py"
      provides: "Strict Account hostname normalization and validation at the shared input boundary"
      contains: "urlsplit"
    - path: "tests/unit/test_models.py"
      provides: "Valid normalization and malformed-hostname rejection matrix"
      contains: "TestAccount"
    - path: "tests/functional/test_mcp_server.py"
      provides: "In-memory MCP proof that malformed input is rejected before Exa access"
      contains: "exa.calls"
  key_links:
    - from: "src/mcp_server/evidence.py"
      to: "src/models.py"
      via: "pack_from_context builds frozen MCP-safe copies, repeatedly measures model_dump_json UTF-8 bytes, and returns only a pack within the named hard boundary"
      pattern: "model_dump_json.*encode"
    - from: "src/mcp_server/server.py"
      to: "src/models.py"
      via: "get_account_evidence constructs Account before reading lifespan dependencies or calling build_evidence_pack"
      pattern: "Account\(domain=domain\)"
    - from: "tests/functional/test_mcp_server.py"
      to: "tests/functional/test_enrich.py"
      via: "FakeExa.calls records whether malformed MCP input crossed into the provider boundary"
      pattern: "exa\.calls"
---

<objective>
Close the two actionable Phase 10 verifier gaps: complete D-03's MCP payload caps across every nested evidence-text field, and harden D-08's invalid-domain path so malformed hostnames are rejected before provider access.

Purpose: Phase 10's stdio transport and real-client proof work, but MCP-01 remains blocked by raw nested evidence text and MCP-07 remains blocked by hostname-shaped invalid input reaching Exa. This plan fixes the trust boundaries rather than weakening the requirements.
Output: hardened evidence boundary composition, strict shared Account normalization/validation, and unit plus in-memory MCP regressions for both verifier gaps.

Root-cause classification:
- Payload gap: plan deficiency. Plan 10-02 capped top-level text and justification summaries but did not enumerate the nested Citation and NewsItem serialization paths, so its tests could not detect the 43,810-byte crafted payload.
- Domain gap: plan deficiency carried into execution. Plan 10-02 treated the pre-existing Account validator as a hostname validator, but its contract only checked spaces and the presence of a dot.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-CONTEXT.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-RESEARCH.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-PATTERNS.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-02-SUMMARY.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-03-SUMMARY.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-VERIFICATION.md
@.planning/phases/10-stdio-mcp-server-thin-tier/10-REVIEW.md
@src/mcp_server/evidence.py
@src/models.py
@src/mcp_server/server.py
@tests/unit/test_evidence.py
@tests/unit/test_models.py
@tests/functional/test_mcp_server.py

<interfaces>
Existing contracts to preserve and extend:

- `pack_from_context(ctx: RawContext) -> EvidencePack` is the MCP boundary and must remain the only RawContext-to-wire composition function.
- `EvidencePack.from_context(about_text: str, news_items: list[NewsItem], justifications: tuple[Justification, ...], *, about_text_min_chars: int) -> EvidencePack` computes retrieval_status and stores the already-safe values it receives.
- `Citation` fields are `url`, optional `title`, optional `snippet`, optional `retrieved_at`, and `source`; URL, source, and retrieval time remain useful provenance while title/snippet are redundant once a Justification summary or NewsItem headline/summary exists.
- `NewsItem` fields are `headline`, `summary`, `citation`, and optional `published_at`.
- `Account(domain: str)` is constructed at the start of `get_account_evidence`, before lifespan dependencies are read and before `build_evidence_pack` can call Exa.
- `FakeExa.calls: list[str]` records `about:{domain}` and `news:{domain}` and is the established provider-access assertion seam.
</interfaces>
</context>

## Artifacts this phase produces

Plan 10-04 modifies existing Phase 10 artifacts and adds no dependency or new subsystem:

- `src/mcp_server/evidence.py`: private MCP-copy helpers for `Citation` and `NewsItem`; completed nested-text sanitization; `CITATION_URL_MCP_CAP` for indivisible provenance; and `EVIDENCE_PACK_MAX_BYTES` enforced against the final `model_dump_json()` UTF-8 bytes by deterministic reduction.
- `tests/unit/test_evidence.py`: adversarial fixtures covering four-byte Unicode text, JSON-escaping overhead, long and over-limit URLs, nested evidence text, deterministic reduction, honest status, sequential numbering, citation integrity, and the hard serialized byte boundary.
- `src/models.py`: `Account._normalize_domain` upgraded from dot/space checks to HTTP(S)-aware hostname normalization and RFC-shaped ASCII label validation using only the standard library.
- `tests/unit/test_models.py`: expanded valid and invalid Account matrices.
- `tests/functional/test_mcp_server.py`: parameterized malformed-domain calls proving `isError is True`, sanitized text, and `FakeExa.calls == []`.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Local stdio client retrieves grounded, cited evidence | 10-01..10-04 | COVERED | Existing transport proof stands; 10-04 closes the two verifier-blocked boundary contracts. |
| REQ | MCP-01 | Structured numbered evidence with honest status and boundary caps | 10-01, 10-02, 10-04 | COVERED | 10-04 enforces the final serialized UTF-8 byte boundary at runtime, not only per-field character caps. |
| REQ | MCP-06 | Resolve and log tier once | 10-01, 10-02 | COVERED | Already verified; no gap action required. |
| REQ | MCP-07 | Read-only annotations and domain failures as tool errors | 10-02, 10-04 | COVERED | 10-04 rejects malformed hostname-shaped input before retrieval. |
| REQ | HOST-01 | Real stdio client with stderr-only logging | 10-02, 10-03 | COVERED | Already verified through the approved Codex client substitution. |
| REQ | TEST-02 | Opt-in live subprocess smoke | 10-03 | COVERED | Already verified; no paid live rerun is part of this gap plan. |
| RESEARCH | Pitfall 3 | Oversized EvidencePack must be capped at the MCP boundary | 10-02, 10-04 | COVERED | 10-04 measures every returned wire payload and reduces it until it is within a named byte constant. |
| RESEARCH | Security V5 | Validate untrusted domain input at the boundary | 10-04 | COVERED | The verifier supersedes the earlier assumption that dot/space checks were sufficient. |
| CONTEXT | D-01 | Include cleaned about_text | 10-01, 10-02 | COVERED | Preserved unchanged. |
| CONTEXT | D-02 | EvidencePack owns the wire format | 10-01, 10-02, 10-04 | COVERED | Safe copies are passed into EvidencePack; no wrapper dictionary is introduced. |
| CONTEXT | D-03 | Cap evidence in evidence.py before model construction | 10-02, 10-04 | COVERED | 10-04 completes every nested evidence-text path. |
| CONTEXT | D-04 | Key-aware Browserbase without open_deps | 10-02 | COVERED | Preserved unchanged. |
| CONTEXT | D-05 | Real-client gate | 10-03 | COVERED | Completed through user-approved Codex substitution. |
| CONTEXT | D-06 | Live smoke reuses a fixture domain | 10-03 | COVERED | Existing smoke remains unchanged. |
| CONTEXT | D-07 | Sanitizing catch-all | 10-02 | COVERED | Preserved unchanged. |
| CONTEXT | D-08 | Invalid domain gets a plain validator message; thin/empty is not an error | 10-02, 10-04 | COVERED | 10-04 strengthens what counts as invalid without changing error shape. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce a hard serialized EvidencePack byte budget without corrupting citations (MCP-01, D-02, D-03)</name>
  <files>src/mcp_server/evidence.py, tests/unit/test_evidence.py</files>
  <read_first>
    - src/mcp_server/evidence.py (`pack_from_context`, `_truncate_words`, and existing MCP caps)
    - src/models.py (`Citation`, `NewsItem`, `Justification`, and `EvidencePack` frozen model fields)
    - src/enrich.py (`_number_justifications` ordering/deduplication and upstream 5-about/10-news result shapes)
    - tests/unit/test_evidence.py (current pure RawContext fixture style)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-VERIFICATION.md (first gap, crafted 43,810-byte evidence)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-REVIEW.md (CR-01 field-level diagnosis)
  </read_first>
  <behavior>
    - Test 1: five about citations with long snippets plus ten news items with long citation snippets and long summaries produce a pack where every nested `citation.snippet` is `None` in both `justifications` and `news`.
    - Test 2: MCP citation copies also clear optional `title` because the bounded `Justification.summary` or `NewsItem.headline` is the canonical client-visible evidence text; URL, source, and retrieved_at remain unchanged.
    - Test 3: every `Justification.summary`, `NewsItem.headline`, and `NewsItem.summary` is at most `JUSTIFICATION_SUMMARY_MCP_CAP` characters, while `about_text` remains at most `ABOUT_TEXT_MCP_CAP`.
    - Test 4: news remains capped at `NEWS_ITEM_MCP_CAP`, numbering remains sequential, and every justification retains a non-empty citation URL.
    - Test 5: adversarial four-byte Unicode in every text field, quote/backslash JSON-escaping input, and long valid URLs still produce `len(pack.model_dump_json().encode("utf-8")) <= EVIDENCE_PACK_MAX_BYTES` because the returned payload is measured and reduced at runtime.
    - Test 6: a citation whose unmodified URL exceeds `CITATION_URL_MCP_CAP` is dropped with its evidence unit; it is never sliced. Every retained URL is non-empty, valid through the Citation model, and byte-for-byte equal to an input `Citation.url`.
    - Test 7: budget reduction is deterministic: keep source order, discard over-limit provenance units, remove tail news before company-about evidence, truncate about_text only on a valid UTF-8/code-point boundary, then remove tail about citations if required; rebuild justifications from the retained source units so deduplication and sequential indices stay correct.
    - Test 8: when no cited evidence unit can fit, the returned pack clears uncited content and reports `retrieval_status="empty"`; otherwise status is recomputed from the retained about/news fields and never copied from the oversized candidate.
    - Test 9: caller-owned RawContext/Citation/NewsItem instances are unchanged, proving frozen safe copies rather than mutation.
  </behavior>
  <action>
    Start with RED tests in `tests/unit/test_evidence.py`. Keep a representative upstream-shape case for the verifier's original nested-snippet regression, but do not call that fixture a maximum. Add adversarial cases that use four-byte Unicode, characters escaped by JSON, valid URLs near `CITATION_URL_MCP_CAP`, and a Pydantic-valid URL between 2,049 and 2,083 bytes so it exceeds the MCP provenance cap without exceeding `HttpUrl`'s accepted range. Assert the exact returned wire size with `len(pack.model_dump_json().encode("utf-8"))`, not character counts or a hand-derived estimate.

    In `src/mcp_server/evidence.py`, keep `EvidencePack` as the only wire model per D-02. Add `EVIDENCE_PACK_MAX_BYTES = 24_000` as the hard final-payload boundary and `CITATION_URL_MCP_CAP = 2_048` as the maximum indivisible URL component accepted into an evidence unit. These are module constants, not environment knobs, per D-03. The total byte constant is authoritative: per-field character caps remain readability limits, not proof of wire size.

    Import `Citation` and `NewsItem`, then add small fully typed helpers:

    - `_mcp_citation(citation: Citation) -> Citation` returns a frozen copy with optional `title` and `snippet` cleared. It must never alter `url`, `source`, or `retrieved_at`.
    - `_mcp_news(item: NewsItem) -> NewsItem` bounds headline and summary with the existing text cap and replaces the nested citation with `_mcp_citation`.
    - `_serialized_size(pack: EvidencePack) -> int` returns the exact UTF-8 byte length of `model_dump_json()`.
    - A byte-aware text helper returns the longest valid code-point/word-boundary prefix that fits a supplied byte allowance, including any ellipsis bytes; it must not slice encoded bytes and decode with loss.
    - A private candidate builder reruns `_number_justifications` against the currently retained about citations and news items, then applies the MCP-safe copies before calling `EvidencePack.from_context`. This preserves existing URL deduplication, including the case where an about citation and removed news item share a URL.
    - `_fit_pack_to_byte_budget(...) -> EvidencePack` owns deterministic reduction and asserts its returned serialization is at most `EVIDENCE_PACK_MAX_BYTES`.

    Build meaningful justification summaries from the raw capped inputs before clearing title/snippet, then operate only on frozen safe copies. Treat URL provenance as indivisible. If `len(str(citation.url).encode("utf-8"))` exceeds `CITATION_URL_MCP_CAP`, reject that complete about citation or news item; do not truncate, rewrite, hash, or replace the URL. Preserve source order among retained units.

    The reducer must use this order and rebuild `EvidencePack` through `EvidencePack.from_context` after every change so `retrieval_status` reflects retained fields:

    1. Serialize the safe candidate and return immediately when it fits.
    2. Remove tail news items first and rebuild justifications from the remaining about/news source units after each removal. Company-about evidence has higher priority than duplicated recent-news detail, and an about justification survives when a removed news item shared its URL.
    3. If still over budget, reduce `about_text` to the largest valid UTF-8/code-point and word-boundary prefix that lets the current pack fit. Measure the candidate serialization during the search because JSON escaping and multibyte characters make character arithmetic insufficient.
    4. If provenance alone is still too large, remove tail about citations and rebuild/renumber justifications until the pack fits. When no retained about citation remains because of budget rejection, clear about_text rather than return uncited content.
    5. If no cited evidence unit fits, return the schema-valid empty pack (`about_text=""`, no news, no justifications), whose status is `empty`. This is the guaranteed terminal case and keeps the hard boundary enforceable for every valid RawContext.

    Keep all retained citation URLs non-empty and unchanged. Keep the existing count and text caps, and add the final runtime byte check after reduction so future model fields or serializer overhead cannot silently bypass the boundary. Do not change `src/models.py`, add a wrapper dictionary, add dependencies, add runtime env knobs, mutate upstream models, or catch provider exceptions in evidence.py.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_evidence.py -x -q && uv run mypy src/mcp_server/evidence.py tests/unit/test_evidence.py</automated>
  </verify>
  <acceptance_criteria>
    - Every test fixture, including long valid URLs, an over-limit URL, four-byte Unicode, and JSON-escaping text, returns `len(pack.model_dump_json().encode("utf-8")) <= EVIDENCE_PACK_MAX_BYTES` because production code enforces the boundary after serialization.
    - `EVIDENCE_PACK_MAX_BYTES` is the named hard wire limit; no representative fixture is described as a mathematical maximum or used as the sole proof of size safety.
    - Every serialized `justifications[*].citation.snippet`, `justifications[*].citation.title`, `news[*].citation.snippet`, and `news[*].citation.title` is null.
    - Every serialized justification summary, news headline, and news summary is bounded by `JUSTIFICATION_SUMMARY_MCP_CAP`; about_text is bounded by `ABOUT_TEXT_MCP_CAP`.
    - Every retained citation URL remains non-empty, valid, and exactly equal to an input URL; over-limit URLs are absent rather than truncated, and justification indices are sequential after every drop.
    - Budget-driven removal follows the documented source-order priority and recomputes retrieval_status; a pack with no retained cited evidence contains no uncited text and reports `empty`.
    - `uv run pytest tests/unit/test_evidence.py -q` passes, including the pre-existing cap/status tests.
    - `uv run mypy src/mcp_server/evidence.py tests/unit/test_evidence.py` exits cleanly under strict configuration.
  </acceptance_criteria>
  <done>MCP-01's boundary contract covers every variable-length evidence path and enforces a true serialized UTF-8 maximum while preserving valid citation provenance and honest status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize HTTP(S) hostnames and reject malformed MCP input before provider access (MCP-07, D-08)</name>
  <files>src/models.py, tests/unit/test_models.py, tests/functional/test_mcp_server.py</files>
  <read_first>
    - src/models.py (`Account._normalize_domain`, shared by CSV/pipeline and MCP callers)
    - src/mcp_server/server.py (`Account(domain=domain)` ordering and ValidationError translation)
    - tests/unit/test_models.py (`TestAccount` current valid and invalid cases)
    - tests/functional/test_mcp_server.py (`_lifespan_factory`, FakeExa import, and invalid-domain MCP test)
    - tests/functional/test_enrich.py (`FakeExa.calls` recording contract)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-VERIFICATION.md (second gap and rejected input families)
    - .planning/phases/10-stdio-mcp-server-thin-tier/10-REVIEW.md (WR-01 trust-boundary diagnosis)
  </read_first>
  <behavior>
    - Test 1: bare `example.com`, mixed-case `HTTP://Linear.app`, and `https://www.Notion.so/` normalize to lowercase hostnames with the optional `www.` prefix removed.
    - Test 2: only `http` and `https` schemes are accepted; an HTTP(S) URL may have an empty path or the root slash only.
    - Test 3: paths, query strings, fragments, userinfo, explicit ports, unsupported schemes, and bare-host delimiter variants are rejected.
    - Test 4: newline, tab, other ASCII control characters, empty labels, labels with characters outside ASCII letters/digits/hyphen, leading/trailing hyphens, labels over 63 characters, hostnames over 253 characters, blank input, missing dots, and trailing-dot empty labels are rejected.
    - Test 5: valid punycode labels remain accepted because they use the allowed ASCII hostname alphabet; arbitrary Unicode labels are rejected rather than being silently transformed.
    - Test 6: each verifier-named malformed family called through the in-memory MCP client returns `isError is True`, contains `invalid domain`, contains no traceback, and leaves `FakeExa.calls == []`.
  </behavior>
  <action>
    Write the unit and in-memory MCP tests first. Expand `TestAccount` with parameterized valid normalization and invalid input matrices. Expand the functional invalid-domain test into a parameterized test over at least: path, query, fragment, userinfo, port, embedded newline, embedded tab, empty label, leading hyphen, trailing hyphen, 64-character label, and over-253-character hostname. Create a fresh `FakeExa` per case, then assert its `calls` list remains empty after the tool result so rejection-before-provider is proven directly rather than inferred from error text.

    Replace `Account._normalize_domain`'s prefix loop with standard-library parsing and explicit hostname validation. Use `urllib.parse.urlsplit` and a module-level compiled ASCII label regex. Keep the validator as the single shared source; do not add MCP-only duplicate validation in server.py.

    Required algorithm:

    1. Reject any ASCII control character (code points 0-31 or 127) in the original input before stripping, so newlines/tabs cannot be normalized away or enter logs/provider queries.
    2. Strip surrounding ordinary whitespace only after the control-character check; reject empty input.
    3. If the value contains a URI scheme delimiter, parse it directly and require case-insensitive `http` or `https`. Otherwise parse it as an authority by prefixing `//` for `urlsplit`.
    4. Reject parse failures, missing hostnames, username/password, explicit ports, query strings, fragments, and any path other than empty or `/`. Reject bare-host input containing URI delimiters rather than interpreting it as a path or query.
    5. Take `parsed.hostname`, lowercase it, remove one leading `www.`, and reject a trailing dot. Require total hostname length at most 253 and at least one dot.
    6. Split on dots and require every label to be 1-63 characters and match an ASCII DNS label: starts and ends with a lowercase letter or digit, with only lowercase letters, digits, or hyphens inside. This admits punycode (`xn--...`) without adding IDNA behavior or a dependency.
    7. Raise `ValueError(f"invalid domain: {original!r}")` for every invalid case so D-08's plain client-visible category remains stable. Return only the normalized hostname for valid input.

    Preserve the current valid cases and frozen pydantic behavior. Do not add dependencies, accept ports, resolve DNS, perform network I/O, or broaden the MCP error text. The existing `get_account_evidence` ordering must remain unchanged: Account construction happens before lifespan access and retrieval.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_models.py tests/functional/test_mcp_server.py -x -q && uv run mypy src/models.py tests/unit/test_models.py tests/functional/test_mcp_server.py</automated>
  </verify>
  <acceptance_criteria>
    - `Account(domain="https://www.Notion.so/").domain == "notion.so"` and `Account(domain="HTTP://Linear.app").domain == "linear.app"` remain true.
    - Every malformed family named by the verifier raises pydantic ValidationError in unit tests.
    - Every malformed family returns `isError is True` through the in-memory MCP client, exposes only sanitized invalid-domain text, and records `exa.calls == []`.
    - The validator uses only the Python standard library and a compiled regex; pyproject.toml and uv.lock remain untouched.
    - `uv run pytest tests/unit/test_models.py tests/functional/test_mcp_server.py -q` passes with all pre-existing tests.
    - Strict mypy passes for the modified source and tests without new overrides or ignores.
  </acceptance_criteria>
  <done>MCP-07's invalid-domain path rejects malformed untrusted input before Exa while preserving supported hostname and root HTTP(S) normalization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Exa/Browserbase -> MCP evidence pack -> client context | Untrusted provider text crosses into a client-visible structured payload and consumes the calling agent's context budget. |
| MCP client -> Account -> Exa | Untrusted domain text can affect provider queries, provider spend, and warning-log integrity unless rejected first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-04 | Denial of Service | `src/mcp_server/evidence.py::pack_from_context` | high | mitigate | Remove redundant nested citation text, keep per-field/count caps, reject indivisible over-limit URLs without truncation, and measure/reduce every final serialized payload until it is within `EVIDENCE_PACK_MAX_BYTES`. |
| T-10-03 | Spoofing / Injection | `src/models.py::Account._normalize_domain` | medium | mitigate | At the primary client boundary, parse only bare hosts or root HTTP(S) URLs, reject control characters and URL components, enforce total/label length plus ASCII DNS-label syntax, and prove no provider call occurs for malformed values. |
| T-10-08 | Tampering | warning logs and Exa query strings | medium | mitigate | Reject control characters, userinfo, path/query/fragment, and port syntax before the normalized hostname can be logged or passed to Exa. |
| T-10-09 | Denial of Service | strict hostname validator compatibility | low | accept | Arbitrary Unicode domains and non-root URL forms are intentionally rejected; this read-only company-domain tool accepts ASCII/punycode hostnames and root HTTP(S) URLs, which covers the documented input contract without hidden network normalization. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_evidence.py tests/unit/test_models.py tests/functional/test_mcp_server.py -q` passes.
- `make test` passes with smoke tests excluded.
- `make typecheck` passes under strict mypy with no new overrides.
- `make lint` passes under Ruff and Black.
- Adversarial long-URL, four-byte-Unicode, and JSON-escaping EvidencePack cases all serialize at or below `EVIDENCE_PACK_MAX_BYTES`, and all redundant nested citation text is absent.
- Retained citation URLs are unchanged and valid, dropped evidence is deterministically ordered, indices are sequential, and retrieval_status is recomputed from retained content.
- Every verifier-named malformed domain returns an in-memory MCP `isError` result before FakeExa records any request.
</verification>

<success_criteria>
- MCP-01 is no longer blocked: about text, justification summaries, news headlines/summaries, nested citation text, provenance URLs, JSON overhead, and multibyte encoding are controlled by an exact final serialized-byte check, with retained numbered citation URLs preserved unchanged.
- MCP-07 is no longer blocked: malformed paths, queries, URL authority components, control characters, invalid labels, and overlength hostnames are rejected before provider access.
- Existing real-client, stdio, tiering, annotation, error-sanitization, and smoke behavior remains unchanged.
- No dependency, wire-model schema, HTTP transport, rate-limit, resource, prompt, full-tier, hosting, or documentation scope is added.
</success_criteria>

<output>
Create `.planning/phases/10-stdio-mcp-server-thin-tier/10-04-SUMMARY.md` when done.
</output>

---
phase: 11-rate-limits-streamable-http-transport
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/mcp_server/wiring.py
  - src/mcp_server/server.py
  - tests/functional/test_mcp_server.py
autonomous: true
requirements: [HOST-04]

must_haves:
  truths:
    - "MCP_DEMO_MODE=1 activates the limiter and the Exa results clamp on ANY transport; without it the server is unlimited and unclamped (D-01, D-02, D-03)"
    - "Quota is consumed at exactly one point: after Account validation succeeds and before retrieval; invalid domains and refused calls consume nothing and never reach Exa (D-04)"
    - "Rationing refusals surface as isError:true tool results in plain-message style, each carrying its reset time, through the existing sanitizing ValueError path (D-06, MCP-07)"
    - "In demo mode, Exa 402/429 exhaustion surviving tenacity retries borrows the daily-cap wording so the public URL looks rationed, never broken; non-demo mode keeps the Phase 10 message (D-07)"
    - "Successful responses disclose no quota information; the EvidencePack wire format is byte-identical with and without the limiter (D-07)"
    - "Over the in-memory (stdio-equivalent) transport, demo-mode calls fall into the fail-closed shared bucket because no Request is attached (D-01, HOST-04)"
  artifacts:
    - "src/mcp_server/wiring.py with DemoClampedExa and ThinDeps.limiter"
    - "src/mcp_server/server.py with the limit gate and demo exhaustion wording in get_account_evidence"
    - "tests/functional/test_mcp_server.py extended with rationing, clamp, wiring, and exhaustion tests"
  key_links:
    - "get_account_evidence reads the limiter from ctx.request_context.lifespan_context (ThinDeps.limiter); limiter present if and only if demo mode"
    - "Client IP is read from ctx.request_context.request via resolve_client_ip; no middleware, no contextvars (RESEARCH.md core finding)"
    - "The exhaustion message reuses limits.DAILY_CAP_MESSAGE so both refusal paths share one string"
---

<objective>
Wire the plan 11-01 limiter into the tool path: demo-mode construction in `make_thin_lifespan` (limiter + Exa results clamp, D-01/D-02/D-03), the single check-and-consume gate inside `get_account_evidence` between Account validation and retrieval (D-04), rationing refusals through the existing sanitized isError path with reset times (D-06), and the demo-mode Exa-exhaustion masquerade (D-07). Verified end-to-end over the in-memory MCP session harness with injected clocks.

Purpose: after this plan, `MCP_DEMO_MODE=1` fully rations the tool on any transport; plan 11-03 only adds the HTTP transport surface on top.
Output: modified wiring.py and server.py, extended functional test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rate-limits-streamable-http-transport/11-CONTEXT.md
@.planning/phases/11-rate-limits-streamable-http-transport/11-RESEARCH.md
@.planning/phases/11-rate-limits-streamable-http-transport/11-PATTERNS.md
@.planning/phases/11-rate-limits-streamable-http-transport/11-01-SUMMARY.md
@src/mcp_server/server.py
@src/mcp_server/wiring.py
@src/mcp_server/limits.py
</context>

## Artifacts this phase produces

See the full phase-wide list in 11-01-PLAN.md. This plan creates:

- `DemoClampedExa` (class, src/mcp_server/wiring.py): ExaLike-satisfying wrapper clamping `num_results`
- `ThinDeps.limiter` field: `DemoLimiter | None = None` (frozen dataclass field with default, existing constructors unaffected)
- Limit gate + demo exhaustion wording inside `get_account_evidence` (src/mcp_server/server.py); tool `ctx` annotation widened to `Context[ServerSession, ThinDeps, Request]`
- New functional tests and fixtures in tests/functional/test_mcp_server.py (recording Exa fake with num_results capture, fake clock helper, limiter-bearing lifespan factory)

<tasks>

<task type="auto">
  <name>Task 1: Demo-mode wiring: DemoClampedExa and ThinDeps.limiter in make_thin_lifespan</name>
  <files>src/mcp_server/wiring.py</files>
  <read_first>
    - src/mcp_server/wiring.py (whole file; the ExaClient/BrowserbaseClient/NullBrowserbase branch inside the lifespan closure is the pattern the demo branch mirrors)
    - src/mcp_server/limits.py (the DemoLimiter constructor signature locked by plan 11-01)
    - src/clients/protocols.py (ExaLike protocol: search_about(domain, num_results=5) and search_news(domain, days=90, num_results=8) signatures DemoClampedExa must match exactly)
    - .planning/phases/11-rate-limits-streamable-http-transport/11-RESEARCH.md (Open Question 2: wrapper-at-the-wiring-layer is the chosen clamp shape)
  </read_first>
  <action>
    In src/mcp_server/wiring.py:

    1. Add `DemoClampedExa`, a small class satisfying the ExaLike protocol structurally (no explicit inheritance, matching the codebase's protocol convention). Constructor takes `inner: ExaLike` and `max_results: int`. `search_about` forwards to inner with `num_results=min(num_results, self._max_results)`; `search_news` forwards with the same clamp on num_results and passes `days` through unchanged. Signatures and defaults must match ExaLike exactly (num_results default 5 for about, 8 for news, days default 90) so it type-checks as ExaLike under strict mypy. Clamp semantics are min(), not replace: a caller asking for fewer than the clamp gets fewer (D-03: the clamp caps Exa spend, it does not inflate requests).

    2. Extend `ThinDeps` with a new field `limiter: DemoLimiter | None = None` (import DemoLimiter from src.mcp_server.limits). Default None keeps every existing constructor call site and test fixture valid; limiter-is-not-None is the single runtime signal for demo mode downstream (D-01/D-02: presence tracks settings.mcp_demo_mode, not the transport).

    3. In `make_thin_lifespan`'s lifespan closure, after the existing exa/browserbase construction: when `settings.mcp_demo_mode` is true, rebind `exa = DemoClampedExa(exa, max_results=settings.mcp_demo_exa_results)` (D-03: clamp applies in demo mode on any transport) and build `limiter = DemoLimiter(ip_limit=settings.mcp_demo_ip_limit, daily_cap=settings.mcp_demo_daily_cap)` with the default clock; otherwise `limiter = None` (D-02: non-demo is unlimited). Yield `ThinDeps(exa=exa, browserbase=bb, limiter=limiter)`. The lifespan runs once per process (Phase 10 verified), so the limiter is process-global by construction. Add a short WHY comment on the demo branch citing D-01/D-02/D-03. Note that demo mode already forces NullBrowserbase indirectly only when Browserbase keys are absent; do NOT change the existing browserbase branch this phase (Settings.mcp_tier() demo forcing is Phase 12's gate concern; CONTEXT.md locks ThinDeps-adjacent behavior otherwise unchanged).
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py tests/unit/test_evidence.py -x -q && uv run mypy src/mcp_server/wiring.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from src.mcp_server.wiring import DemoClampedExa, ThinDeps; import dataclasses; f = {x.name for x in dataclasses.fields(ThinDeps)}; assert f == {'exa', 'browserbase', 'limiter'}, f; print('ok')"` prints `ok`
    - Existing functional tests pass unmodified (ThinDeps construction with two args still valid thanks to the default)
    - `uv run mypy src/mcp_server/wiring.py` clean; DemoClampedExa is accepted where ExaLike is expected (mypy proves protocol conformance at the assignment inside the lifespan)
  </acceptance_criteria>
  <done>Demo mode wiring produces a clamped Exa and a live limiter; non-demo wiring is byte-for-byte the old behavior with limiter=None.</done>
</task>

<task type="auto">
  <name>Task 2: The limit gate and exhaustion wording in get_account_evidence</name>
  <files>src/mcp_server/server.py</files>
  <read_first>
    - src/mcp_server/server.py (whole file; the Account-validation block, the deps lookup, and the two-branch except chain around build_evidence_pack are the exact seams being extended)
    - src/mcp_server/limits.py (resolve_client_ip and check_and_consume signatures, DAILY_CAP_MESSAGE)
    - .planning/phases/11-rate-limits-streamable-http-transport/11-PATTERNS.md (server.py section: the raise-ValueError-from-None idiom and the locked insertion point)
    - .planning/phases/11-rate-limits-streamable-http-transport/11-RESEARCH.md (Pitfall 4: the third Context generic parameter; Summary point 2: why ctx.request_context.request is the only correct IP source)
  </read_first>
  <action>
    In src/mcp_server/server.py:

    1. Imports: add `from starlette.requests import Request` (a real import, not TYPE_CHECKING-guarded: FastMCP resolves the tool's type hints at registration time) and import `DAILY_CAP_MESSAGE` and `resolve_client_ip` from src.mcp_server.limits.

    2. Widen the tool signature annotation from Context[ServerSession, ThinDeps] to `Context[ServerSession, ThinDeps, Request]` so `ctx.request_context.request` types as `Request | None` under strict mypy instead of Any (RESEARCH.md Pitfall 4). No behavioral change from the annotation itself.

    3. Insert the limit gate between the existing Account-validation block and the retrieval try-block (D-04 locked insertion point: after validation passes, immediately before retrieval, so invalid domains cost nothing and refused calls never reach Exa). Concretely, after `deps = ctx.request_context.lifespan_context`: when `deps.limiter is not None`, compute `client_ip = resolve_client_ip(ctx.request_context.request)` (over stdio and the in-memory harness the request is None, so this fails closed into the shared bucket per D-01), then `decision = await deps.limiter.check_and_consume(client_ip)`, and when `decision.allowed` is false, `raise ValueError(decision.message) from None`. This MUST sit OUTSIDE the retrieval try-block so the refusal is not swallowed by the catch-all except branch, and it reuses the exact raise-ValueError-from-None idiom so refusals surface as sanitized isError:true tool results per D-06/MCP-07, never protocol errors. Do not add any new exception type. Do not log the client IP at INFO (if a debug log is wanted, log only the bucket kind, never raw header-derived values).

    4. D-07 exhaustion masquerade: inside the existing `except (httpx.HTTPError, BrowserbaseError)` branch, before the current raise, add: when `deps.limiter is not None` and the exception is an `httpx.HTTPStatusError` whose `response.status_code` is 402 or 429 (Exa credit exhaustion or provider rate-limit surviving ExaClient's tenacity retries, which reraise the original HTTPStatusError), raise `ValueError(DAILY_CAP_MESSAGE) from None` instead, keeping the existing WARNING log line first. All other cases (non-demo mode, other status codes, non-status HTTP errors, BrowserbaseError) keep the existing wording unchanged. Do not add a new except clause; extend the body of the existing one. Add a WHY comment: in demo mode credit exhaustion borrows the daily-cap wording so the public URL looks rationed, never broken (D-07).

    5. Do not touch EvidencePack, build_evidence_pack, or the success path in any way: D-07 locks the wire format, and successful results must carry zero rationing metadata.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -x -q && uv run mypy src/mcp_server/server.py</automated>
  </verify>
  <acceptance_criteria>
    - All pre-existing functional tests pass unmodified (limiter defaults to None, so non-demo behavior is unchanged, D-02)
    - `grep -c "Context\[ServerSession, ThinDeps, Request\]" src/mcp_server/server.py` returns 1
    - `grep -c "resolve_client_ip(ctx.request_context.request)" src/mcp_server/server.py` returns 1
    - `uv run mypy src/mcp_server/server.py` clean with no new ignore comments
    - The gate raise appears before the `try` that wraps build_evidence_pack (source order assertion; verify by reading the function)
  </acceptance_criteria>
  <done>get_account_evidence rations demo-mode calls at the D-04 point, refuses with D-06 messages through the sanitized path, and masks demo-mode Exa exhaustion per D-07, with no change to any non-demo code path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Functional tests for rationing, clamp, wiring, and exhaustion over the in-memory transport</name>
  <files>tests/functional/test_mcp_server.py</files>
  <read_first>
    - tests/functional/test_mcp_server.py (whole file; `_lifespan_factory`, `FakeExa`, `RaisingExa`, `_exa_about`/`_exa_news` fixtures, and the isError assertion idiom `result.content[0].text` are all reused)
    - src/mcp_server/limits.py (DemoLimiter constructor for injected-clock fixtures)
    - src/mcp_server/wiring.py (make_thin_lifespan and DemoClampedExa as modified by Task 1)
  </read_first>
  <behavior>
    All limiter-bearing tests inject a fake clock (roadmap criterion 3). Extend `_lifespan_factory` to accept an optional `limiter: DemoLimiter | None = None` parameter threaded into ThinDeps.

    - Test 1 (per-IP refusal over stdio-equivalent transport, D-01/D-06): lifespan with `DemoLimiter(ip_limit=2, daily_cap=100, clock=fake)` at t0; calls 1-2 to get_account_evidence return isError False; call 3 returns isError True with text containing `rate limit reached, resets at ` and the exact HH:MM for t0 + 1 hour; the in-memory session attaches no Request, so this also proves the fail-closed shared bucket (all calls share one bucket)
    - Test 2 (window rolls with the clock): continuing from exhaustion, advance the fake clock 61 minutes; the next call returns isError False
    - Test 3 (refusals burn no Exa credit, D-04): in Test 1's refused call, FakeExa.calls has NOT grown (assert length equal before/after the refused call)
    - Test 4 (global cap message, D-06): limiter with ip_limit=100, daily_cap=1; first call ok; second call isError True with text containing exactly `demo budget spent for today, resets at 00:00 UTC`
    - Test 5 (non-demo unlimited, D-02): lifespan with limiter None; ip_limit-plus-many (e.g. 8) consecutive calls all return isError False
    - Test 6 (no quota disclosure, D-07): run the same happy-path call twice, once with a fresh permissive limiter and once with limiter None, same FakeExa fixtures; assert the two structuredContent dicts are equal
    - Test 7 (clamp forwarding, D-03): a small RecordingExa fake in the test file that stores the num_results kwarg it receives; DemoClampedExa(recording, max_results=2): calling search_about with default num_results forwards 2 to the inner fake; search_news default forwards 2; requesting num_results=1 forwards 1 (min semantics)
    - Test 8 (lifespan wiring, D-01/D-02/D-03): entering make_thin_lifespan(Settings(_env_file=None, exa_api_key="x", mcp_demo_mode=True)) yields deps where limiter is not None and exa is an instance of DemoClampedExa; with mcp_demo_mode absent/false, limiter is None and exa is an ExaClient (construction only, no network)
    - Test 9 (exhaustion masquerade, D-07): RaisingExa raising an httpx.HTTPStatusError built with a 402 httpx.Response, lifespan WITH a permissive limiter: result isError True, text contains `demo budget spent for today, resets at 00:00 UTC`; parametrize 429 the same; with limiter None the same exception yields `retrieval unavailable, try again`; a 500 HTTPStatusError WITH limiter also yields `retrieval unavailable, try again`
    - Test 10 (mid-flight failure still counts, D-04): limiter ip_limit=1 with the 402 RaisingExa; first call returns the exhaustion wording (quota consumed before retrieval); second call returns the per-IP `rate limit reached` wording, proving consumption happened even though retrieval failed
  </behavior>
  <action>
    Write the tests first (they fail only if Tasks 1-2 were skipped; when executed sequentially in this plan they should pass immediately, which is acceptable: the behavior contract is authored before manual verification). Reuse the existing fixture vocabulary: extend `_lifespan_factory` with the optional limiter parameter instead of adding a parallel factory; build httpx.HTTPStatusError instances with an httpx.Request("POST", "https://api.exa.ai/search") and httpx.Response(status_code, request=that_request). Fake clocks: a tiny helper class holding a datetime with an advance(minutes) method, defined once in the test file. For Test 8, follow the existing Settings construction idiom `Settings(_env_file=None, exa_api_key="x")` with the call-arg ignore comment, adding `mcp_demo_mode=True` for the demo case; the lifespan factory's app parameter is typed FastMCP, so pass a throwaway `FastMCP("test")` instance when entering it with `async with`, then assert isinstance checks on the yielded ThinDeps fields (DemoClampedExa vs ExaClient, limiter presence).
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_mcp_server.py -q` passes with at least 10 new test functions beyond the pre-existing set
    - Every limiter-bearing test constructs DemoLimiter with an explicit clock argument (no wall-clock dependence; grep the test file for `DemoLimiter(` occurrences and confirm each passes clock=)
    - Test 4's assertion matches the full exact string `demo budget spent for today, resets at 00:00 UTC`
    - `uv run pytest -m "not smoke" -q` passes (full offline suite, no regressions)
    - `uv run mypy src evals` clean
  </acceptance_criteria>
  <done>Demo-mode rationing, clamping, wiring, exhaustion masquerade, and consume-semantics are all behavior-verified over the real in-memory MCP transport with injected clocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> get_account_evidence | Untrusted domain argument and untrusted (absent) request metadata cross here |
| Tool -> Exa | Each retrieval spends real credit from the shared demo pool |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Denial of Service (economic) | Exa credit pool via the tool path | high | mitigate | Gate placement per D-04: consume-before-retrieval, refusals never reach Exa (Task 2 step 3, Task 3 Tests 3/10); demo clamp caps per-call spend (Task 1) |
| T-11-06 | Information Disclosure | Rationing error payloads | medium | mitigate | Refusals routed through the existing sanitizing raise-ValueError path; fixed message templates carry only reset times; no stack traces, env names, or raw input (Task 2, Task 3 Tests 1/4/9) |
| T-11-07 | Tampering | Bypass of the limiter via error paths | medium | mitigate | Gate sits outside the retrieval try-block so no except branch can swallow a refusal; mid-flight failures still consume quota (Task 3 Test 10) |
| T-11-08 | Information Disclosure | Quota state leakage on success | low | mitigate | D-07: EvidencePack unchanged, equality test between limited and unlimited responses (Task 3 Test 6) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `uv run pytest -m "not smoke" -q` green
- `uv run mypy src evals` clean (strict, no new overrides)
- Manual spot check optional: `MCP_DEMO_MODE=1 make mcp` starts and the tier log still prints (limits are exercised by tests, not by hand, until 11-03 lands make mcp-demo)
</verification>

<success_criteria>
- Roadmap criterion 2 is TRUE end-to-end over a real MCP transport: demo mode enforces both caps with structured rationing errors carrying reset times
- D-01 through D-07 each verified by at least one named test
- Non-demo behavior is provably unchanged (existing tests untouched and green)
</success_criteria>

<output>
Create `.planning/phases/11-rate-limits-streamable-http-transport/11-02-SUMMARY.md` when done. Record the final ThinDeps shape and the gate placement so plan 11-03's HTTP tests build on them.
</output>

---
phase: 12-full-tier-tool-resources-prompt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline.py
  - tests/integration/test_pipeline_run_eval.py
autonomous: true
requirements: [MCP-05]
must_haves:
  truths:
    - "process_account(run_eval=False) returns eval_score=None with status clean or hook_suppressed, never judge_failed or low_groundedness (D-02)"
    - "process_account with all-empty hook paragraphs and run_eval=False returns hook_suppressed, not clean (Pitfall 3 canary)"
    - "on_stage callback fires in order enrich, score, contacts, outreach, eval on a clean run_eval=True run; eval is omitted when run_eval=False"
    - "All pre-existing pipeline tests pass with zero modifications (make run behavior unchanged)"
  artifacts:
    - "src/pipeline.py: Deps gains exa, browserbase, limiter fields; process_account gains run_eval and on_stage keyword-only parameters"
    - "tests/integration/test_pipeline_run_eval.py: new test file covering D-02 honesty semantics and on_stage ordering"
  key_links:
    - "The `elif not run_eval:` branch sits strictly between the hook_suppressed check and the `eval_score is None -> judge_failed` check in the status-precedence chain"
    - "build_deps populates Deps.exa/Deps.browserbase from its existing exa/browserbase parameters (single construction site, RESEARCH A2 verified)"
    - "src/pipeline.py contains no import from the MCP server package (dependency arrow stays one-directional)"
---

<objective>
Extend the sealed pipeline with the additive seams the full-tier MCP tool (Plan 12-03) will consume: `Deps` exposes the `exa`/`browserbase`/`limiter` attributes the evidence tool reads (RESEARCH Pattern 1), and `process_account` gains `run_eval` (D-02 honesty semantics) and `on_stage` (D-05 progress callback) keyword parameters, both defaulting to today's exact behavior so `make run`, the CLI path, and every existing test are untouched.

Purpose: MCP-05's `research_account_full(domain, run_eval)` is a thin wrapper around `process_account`; the wrapper must never re-compose stages (D-05) or recompute status (Phase 10 D-02 one-path principle), so the pipeline itself must carry the skip-the-judge and report-progress semantics.
Output: modified `src/pipeline.py`, new `tests/integration/test_pipeline_run_eval.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-CONTEXT.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md
@src/pipeline.py
@tests/integration/test_pipeline_failures.py
</context>

## Artifacts this phase produces

Symbols created or extended by this plan (the plan-review-convergence source-grounding pass reads this list):

- `src/pipeline.py::Deps.exa` — new field, type `ExaLike`
- `src/pipeline.py::Deps.browserbase` — new field, type `BrowserbaseLike`
- `src/pipeline.py::Deps.limiter` — new field, typed bare `None` with default `None`
- `src/pipeline.py::process_account` — new keyword-only parameters `run_eval: bool = True` and `on_stage: Callable[[str], Awaitable[None]] | None = None`
- `tests/integration/test_pipeline_run_eval.py` — new test file

(Other plans in this phase create: `EvidenceDeps`, `make_full_lifespan` (wiring.py); `_sanitized_validation_message`, `research_account_full`, `read_icp_rubric`, `read_eval_report`, `research_account`, `build_server` `tier` parameter (server.py); MCP surfaces `icp://rubric`, `icp://eval-report`, `research_account` prompt; `tests/unit/test_mcp_resources.py`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add run_eval and on_stage keyword parameters to process_account with D-02 status honesty</name>
  <files>src/pipeline.py, tests/integration/test_pipeline_run_eval.py</files>
  <read_first>
    - src/pipeline.py (current process_account body at ~line 131 and the D-03 precedence block at ~lines 209-227 — the exact chain being extended)
    - tests/integration/test_pipeline_failures.py (the FakeExa/FakeBrowserbase classes at lines 15-38 and the build_deps-with-fake-LLM happy-path construction around lines 40-150 — replicate this fake pattern, do not invent a new one)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-CONTEXT.md (D-02, D-03, D-05 exact semantics)
  </read_first>
  <behavior>
    - Test 1 (D-02 core): run_eval=False on a happy-path account (non-empty hooks) yields status == AccountStatus.clean, eval_score is None, and the judge fake's synthesize was never invoked (pass a distinct judge fake that counts or raises on call; writer and judge must be separate fake objects for this assertion)
    - Test 2 (Pitfall 3 canary): run_eval=False with a writer fake that produces empty-paragraph hooks yields AccountStatus.hook_suppressed, NOT clean
    - Test 3 (D-02 impossibility): run_eval=False can never yield judge_failed or low_groundedness — assert status in {clean, hook_suppressed} across both scenarios above
    - Test 4 (on_stage ordering): a collector callback appended to a list receives exactly ["enrich", "score", "contacts", "outreach", "eval"] in that order on a run_eval=True happy path, and exactly ["enrich", "score", "contacts", "outreach"] when run_eval=False
    - Test 5 (on_stage on early failure): an enrich-stage exception (FakeExa raising httpx.HTTPError) with on_stage set leaves the collector empty and returns the unchanged D-03 unscoreable result (status hook_suppressed, error text starts with "enrich failed")
    - Test 6 (default regression): calling process_account(account, deps) positionally with neither new argument still works (signature is backward compatible)
  </behavior>
  <action>
    Write the six tests above in a NEW file `tests/integration/test_pipeline_run_eval.py` FIRST (they must fail against current pipeline.py), then implement in `src/pipeline.py`:

    1. Extend the `collections.abc` import at the top of src/pipeline.py to include `Awaitable` and `Callable` (currently only `AsyncIterator` is imported from it).

    2. Change the `process_account` signature to: `async def process_account(account: Account, deps: Deps, *, run_eval: bool = True, on_stage: Callable[[str], Awaitable[None]] | None = None) -> ScoredAccount:` — keyword-only after `*` per the codebase convention (Phase 5 max_tokens precedent, per D-05's additive-parameter rule).

    3. Insert stage-boundary callbacks (D-05). Rule: fire immediately after each stage's try/except block completes without an early return from the except path, BEFORE any subsequent early-return check. Concretely, insert `if on_stage is not None: await on_stage("<name>")` at five points:
       - "enrich": after the enrich try/except, before the `if enrichment.is_empty:` check
       - "score": after the score try/except, before the `if score is None:` check
       - "contacts": after the contacts try/except
       - "outreach": after the hooks-building for-loop completes
       - "eval": inside the run_eval branch, after the eval try/except (see step 4)
       Do NOT add any try/except around on_stage calls (RESEARCH Open Question 2 recommendation: a disconnected client propagating is honest behavior; swallowing would be unrequested scope). Stage-name strings are exactly "enrich", "score", "contacts", "outreach", "eval" (Claude's discretion resolved: names match the pipeline module stages, not the spec's "personas" wording — document this in a brief WHY comment).

    4. Guard the eval stage (D-02): initialize `eval_score: EvalScore | None = None` before the block, and wrap the existing `try: eval_score = await deps.eval_rubric.evaluate_account(sa) ...` try/except inside `if run_eval:` so a deliberate skip never attempts (and therefore can never fail) the judge call. Import of EvalScore for the annotation: check whether `EvalScore` is already importable from `.models` in pipeline.py's import block; add it to the existing `.models` import line if absent, or annotate via the existing inferred type if the explicit annotation is unnecessary.

    5. Insert the D-02 precedence branch. The chain must read, in exactly this order (Pitfall 3): (a) `if eval_score is not None and eval_score.eval_failed:` -> judge_failed [existing]; (b) `elif all(h.paragraph == "" for h in hooks):` -> hook_suppressed [existing]; (c) NEW `elif not run_eval:` -> clean, with a WHY comment stating a deliberate judge skip must never read as a failure (D-02); (d) `elif eval_score is None:` -> judge_failed [existing, now unambiguous — only reachable when run_eval was True and the judge crashed]; (e) `elif eval_score.is_flagged:` -> low_groundedness [existing]; (f) `else:` clean [existing]. Update the existing D-03 precedence comment block above the chain to document the new branch's position.

    Do not touch `run_pipeline`, `open_deps`, `main`, or any other function in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_pipeline_run_eval.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_pipeline_run_eval.py -x` exits 0 with at least 6 tests collected
    - src/pipeline.py contains the literal `run_eval: bool = True` and the literal `on_stage: Callable[[str], Awaitable[None]] | None = None` inside the process_account signature
    - src/pipeline.py contains `elif not run_eval:` positioned after the `all(h.paragraph == ""` line and before the `elif eval_score is None:` line (verify: `grep -n 'elif not run_eval\|all(h.paragraph\|elif eval_score is None' src/pipeline.py` shows the three matches in that line order)
    - `uv run pytest tests/integration/test_pipeline_failures.py tests/integration/test_pipeline.py -x` exits 0 and `git diff --stat tests/integration/test_pipeline_failures.py tests/integration/test_pipeline.py` shows no changes (existing tests are the regression proof for the defaults, per RESEARCH test map D-04/D-05 row)
  </acceptance_criteria>
  <done>run_eval=False produces honest clean/hook_suppressed statuses with eval_score=None and zero judge calls; on_stage fires at the five stage boundaries in order; every pre-existing pipeline test passes unmodified</done>
</task>

<task type="auto">
  <name>Task 2: Extend Deps with exa/browserbase/limiter fields, keeping pipeline.py MCP-free</name>
  <files>src/pipeline.py</files>
  <read_first>
    - src/pipeline.py (Deps dataclass at ~line 45 and build_deps at ~line 54 — the single Deps construction site)
    - src/mcp_server/wiring.py (ThinDeps at lines 43-47 — the shape Deps must structurally mirror so one Protocol covers both; read-only, do not modify in this plan)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md (Pattern 1 and Pitfall 2 — the rationale for the field extension)
  </read_first>
  <action>
    In `src/pipeline.py`:

    1. Add three fields to the frozen `Deps` dataclass, after the existing five fields: `exa: ExaLike`, `browserbase: BrowserbaseLike`, `limiter: None = None`. The `limiter` field is deliberately typed as bare `None` (not an optional limiter type): the full tier is never demo mode (locked decision), and typing it `None` means this module never references the MCP server package, preserving the one-directional dependency arrow from CONTEXT.md's code_context section. Add a WHY comment on the limiter field stating exactly that rationale (full tier never rations; bare None keeps this module free of MCP-layer imports; Plan 12-03's read-only-property Protocol accepts it covariantly). `ExaLike`/`BrowserbaseLike` are already imported in this file from `.clients.protocols`.

    2. In `build_deps`, pass the two new fields through from the existing parameters: add `exa=exa, browserbase=browserbase,` to the `Deps(...)` constructor call. `limiter` stays at its default. This is the only `Deps(...)` construction site in the repo (verified by research audit A2; re-verify with the grep below).

    Note that dataclass field ordering requires the defaulted `limiter` field to come last. No other file changes in this task.
  </action>
  <verify>
    <automated>make test && make typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "exa: ExaLike" src/pipeline.py` matches inside the Deps dataclass body
    - `grep -rn "Deps(" src tests evals --include="*.py" | grep -v "ThinDeps\|open_deps\|build_deps\|EvidenceDeps"` shows construction only at the build_deps return site (single-construction-site audit, RESEARCH A2)
    - `grep -cE '^(from|import) src\.mcp_server' src/pipeline.py` outputs 0 (anchored import-statement pattern; the pipeline module stays free of MCP-package imports)
    - `make test` exits 0 (full offline suite, all pre-existing tests green)
    - `make typecheck` exits 0 (mypy strict, no new overrides)
  </acceptance_criteria>
  <done>Deps carries exa/browserbase/limiter so a full-tier lifespan context can serve both MCP tools; build_deps populates them from its existing parameters; strict mypy and the full offline suite stay green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> process_account (via Plan 12-03's tool) | run_eval/on_stage values originate from an untrusted MCP client; this plan defines their semantics |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Repudiation | process_account status precedence | medium | mitigate | D-02 branch ordering (Pitfall 3) prevents a deliberate eval skip from masquerading as judge_failed AND prevents skip from masking hook_suppressed; Task 1 tests 2-3 are the enforcement |
| T-12-02 | Tampering | run_eval parameter (client-controlled) | low | accept | run_eval only ever narrows work (skips the judge); it cannot bypass enrichment validation, citation validation, or hook suppression, all of which run before the eval stage |
| T-12-03 | Denial of Service | on_stage callback raising mid-run | low | accept | No try/except around on_stage by design (RESEARCH OQ2): a raise means the MCP session is torn down; the CLI path never passes a callback so make run is unaffected |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none introduced; mcp==1.28.1 already audited in Phase 10) |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_pipeline_run_eval.py -x` exits 0
- `make test` exits 0 (proves zero behavior change for all existing callers)
- `make typecheck` exits 0
- `git diff --stat tests/integration/test_pipeline_failures.py tests/integration/test_pipeline.py` is empty
</verification>

<success_criteria>
- process_account accepts run_eval/on_stage as keyword-only additive parameters with defaults reproducing today's exact behavior
- run_eval=False yields eval_score=None and only clean/hook_suppressed statuses (D-02 locked semantics)
- Deps exposes exa/browserbase/limiter without importing anything from the MCP server package
- Full offline suite + strict mypy green
</success_criteria>

<output>
Create `.planning/phases/12-full-tier-tool-resources-prompt/12-01-SUMMARY.md` when done
</output>

---
phase: 12-full-tier-tool-resources-prompt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_server/server.py
  - tests/unit/test_mcp_resources.py
  - tests/functional/test_mcp_server.py
autonomous: true
requirements: [MCP-02, MCP-03, MCP-04]
must_haves:
  truths:
    - "An MCP client reads the verbatim text of configs/icp.yaml via icp://rubric with mime type application/yaml (MCP-02)"
    - "An MCP client reads the verbatim text of evals/REPORT.md via icp://eval-report with mime type text/markdown (MCP-03)"
    - "A missing/unreadable resource file yields a sanitized non-empty 'resource unavailable' message to the client with the real cause logged to stderr, never a protocol error and never empty content (D-08)"
    - "The research_account prompt points at icp://rubric and get_account_evidence, mandates [N] citation indices with drop-uncited and never-fabricate rules, and never mentions the full-tier tool (D-09/D-10/D-11, MCP-04)"
  artifacts:
    - "src/mcp_server/server.py: read_icp_rubric, read_eval_report, research_account functions plus their registration in build_server"
    - "tests/unit/test_mcp_resources.py: D-08 sanitization tests calling the resource functions directly"
    - "tests/functional/test_mcp_server.py: read_resource/get_prompt round-trip tests over the in-memory session"
  key_links:
    - "Resource functions catch OSError INSIDE the function body — a raise becomes an unsanitized protocol-level ResourceError in the SDK dispatch layer (RESEARCH Pattern 5)"
    - "Paths come from the existing constants src/icp_config.py::DEFAULT_CONFIG_PATH and evals/report.py::REPORT_PATH, so resources are CWD-independent and share the single source of truth"
---

<objective>
Serve the two read-only resources (`icp://rubric` -> `configs/icp.yaml`, `icp://eval-report` -> `evals/REPORT.md`) and the static `research_account` prompt on every tier, registered unconditionally in `build_server`. Per-request disk reads (D-07), in-function error sanitization (D-08), and a prompt that teaches grounding-by-instruction (D-09/D-10/D-11).

Purpose: MCP-02/MCP-03 make the rubric and eval calibration report inspectable by any client on any tier; MCP-04's prompt is the grounding-by-instruction story the Phase 13 README will contrast with grounding-by-construction.
Output: modified `src/mcp_server/server.py`, new `tests/unit/test_mcp_resources.py`, extended `tests/functional/test_mcp_server.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-CONTEXT.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-PATTERNS.md
@src/mcp_server/server.py
@tests/functional/test_mcp_server.py
</context>

## Artifacts this phase produces

Symbols created by this plan (the plan-review-convergence source-grounding pass reads this list):

- `src/mcp_server/server.py::read_icp_rubric` — new module-level function, `() -> str`
- `src/mcp_server/server.py::read_eval_report` — new module-level function, `() -> str`
- `src/mcp_server/server.py::research_account` — new module-level prompt function, `(domain: str) -> str`
- MCP surfaces: `icp://rubric` resource, `icp://eval-report` resource, `research_account` prompt (registered in `build_server`)
- `tests/unit/test_mcp_resources.py` — new test file

(Other plans in this phase create: `Deps.exa`/`Deps.browserbase`/`Deps.limiter` and `process_account` `run_eval`/`on_stage` params (pipeline.py); `EvidenceDeps`, `make_full_lifespan` (wiring.py); `_sanitized_validation_message`, `research_account_full`, `build_server` `tier` parameter (server.py); `tests/integration/test_pipeline_run_eval.py`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serve icp://rubric and icp://eval-report with per-request reads and sanitized failures</name>
  <files>src/mcp_server/server.py, tests/unit/test_mcp_resources.py, tests/functional/test_mcp_server.py</files>
  <read_first>
    - src/mcp_server/server.py (current imports at lines 1-21, log usage, and build_server registration block at lines 148-151 — the file being extended)
    - src/icp_config.py (line 10: DEFAULT_CONFIG_PATH — the rubric path source of truth)
    - evals/report.py (line 40: REPORT_PATH — the report path source of truth; import only the constant, nothing else from that module)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md (Pattern 5: why resource functions must catch inside the body; Pitfall 5: MIME types)
    - tests/functional/test_mcp_server.py (the create_connected_server_and_client_session harness usage at lines 104-123 and _lifespan_factory at lines 23-31 — reuse both for the new functional tests)
  </read_first>
  <behavior>
    - Unit test 1: read_icp_rubric() returns exactly Path-read content of configs/icp.yaml (compare against DEFAULT_CONFIG_PATH.read_text(encoding="utf-8"))
    - Unit test 2: read_eval_report() returns exactly REPORT_PATH.read_text(encoding="utf-8")
    - Unit test 3 (D-08): with pathlib.Path.read_text monkeypatched to raise OSError("/leaky/absolute/path"), read_icp_rubric() returns a non-empty string containing "resource unavailable", NOT containing "/leaky/absolute/path", and does not raise; a caplog assertion at WARNING level shows the real cause was logged
    - Unit test 4 (D-08): same monkeypatched-OSError behavior for read_eval_report(), with a message that names the eval report (the two sanitized messages must be distinguishable from each other)
    - Functional test 5: over the in-memory session, client.read_resource(AnyUrl("icp://rubric")) yields contents whose first item has .text equal to the committed configs/icp.yaml text and .mimeType == "application/yaml"
    - Functional test 6: client.read_resource(AnyUrl("icp://eval-report")) yields .text equal to the committed evals/REPORT.md text and .mimeType == "text/markdown"
    - Functional test 7: client.list_resources() includes both URIs icp://rubric and icp://eval-report
  </behavior>
  <action>
    Write the tests FIRST (new file `tests/unit/test_mcp_resources.py` for tests 1-4 calling the resource functions directly, so sanitization is isolated from SDK error-wrapping; tests 5-7 appended to `tests/functional/test_mcp_server.py` using the existing harness with `_lifespan_factory(FakeExa(about=[], news=[]))`), then implement in `src/mcp_server/server.py`:

    1. Imports: add `from src.icp_config import DEFAULT_CONFIG_PATH` and `from evals.report import REPORT_PATH`. These constants are `Path(__file__)`-anchored, making resource reads independent of the process CWD — do NOT hardcode string paths.

    2. Two module-level functions, `read_icp_rubric() -> str` and `read_eval_report() -> str` (synchronous is fine; FastMCP accepts sync resource functions). Each: docstring explaining what the resource serves and that it reads from disk on every call so an edited file is visible without a server restart (D-07); body is a try/except where the try returns `<PATH>.read_text(encoding="utf-8")` and `except OSError as exc:` logs `log.warning(...)` with the resource name and the real exception using percent-placeholders, then returns a sanitized non-empty message. Suggested messages: "resource unavailable: the ICP rubric could not be read on the server." and "resource unavailable: the eval calibration report could not be read on the server." — the exact wording is discretionary but each MUST contain "resource unavailable", MUST name which resource failed, MUST NOT include the exception text or any filesystem path, and MUST NOT be empty (D-08: never fail startup, never serve empty content, never leak the cause). Catch INSIDE the function: a raising resource function becomes a protocol-level ResourceError carrying the unsanitized OSError message (RESEARCH Pattern 5, verified against installed SDK source).

    3. Register both in `build_server`, after the existing get_account_evidence registration, unconditionally (every tier serves them): `server.resource("icp://rubric", mime_type="application/yaml")(read_icp_rubric)` and `server.resource("icp://eval-report", mime_type="text/markdown")(read_eval_report)`. MIME choices per RESEARCH Pitfall 5 (RFC 9512 / RFC 7763; Claude's-discretion item resolved as the IANA-registered values).

    For functional tests: `read_resource` takes a pydantic `AnyUrl` — import it from pydantic and wrap the URI string. Result shape: `result.contents[0].text` and `result.contents[0].mimeType` (TextResourceContents).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mcp_resources.py tests/functional/test_mcp_server.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_mcp_resources.py -x` exits 0 with at least 4 tests collected
    - `uv run pytest tests/functional/test_mcp_server.py -k "rubric_resource or eval_report_resource or list_resources" -x` exits 0 with at least 3 tests collected
    - src/mcp_server/server.py contains `def read_icp_rubric() -> str` and `def read_eval_report() -> str`
    - src/mcp_server/server.py contains the registration literals `server.resource("icp://rubric", mime_type="application/yaml")` and `server.resource("icp://eval-report", mime_type="text/markdown")`
    - The unit tests assert the monkeypatched OSError sentinel text is absent from the returned string (leak-proof assertion executes and passes)
  </acceptance_criteria>
  <done>Any client on any tier reads the committed rubric and eval report verbatim with correct MIME types; a broken read degrades to a sanitized, resource-naming, non-empty message with the cause on stderr</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the static research_account prompt teaching rubric-based, citation-disciplined scoring</name>
  <files>src/mcp_server/server.py, tests/functional/test_mcp_server.py</files>
  <read_first>
    - src/mcp_server/server.py (get_account_evidence docstring at lines 47-53 — the citation-contract voice the prompt text mirrors, per D-10; and the build_server registration block being extended)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-PATTERNS.md (the research_account prompt core-pattern block — a verified starting shape for the prompt text)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-CONTEXT.md (D-09/D-10/D-11 exact constraints)
  </read_first>
  <behavior>
    - Functional test 1: client.get_prompt("research_account", {"domain": "notion.so"}) returns at least one message with role "user"; its text contains ALL of: "icp://rubric", "get_account_evidence", "notion.so", "[N]", the word "drop", and the word "fabricate"
    - Functional test 2 (D-11): the same prompt text does NOT contain the string "research_account_full" (one static prompt, no tier-conditional tool mention)
    - Functional test 3: client.list_prompts() includes a prompt named "research_account" taking a required "domain" argument
  </behavior>
  <action>
    Write the three functional tests FIRST in `tests/functional/test_mcp_server.py` (assertion style mirrors the existing test_annotations_and_description_over_the_wire docstring-contract test), then implement in `src/mcp_server/server.py`:

    1. Module-level function `research_account(domain: str) -> str` returning a single string (the SDK coerces a plain str return into one UserMessage — verified against installed mcp==1.28.1, RESEARCH "Prompt decorator" example; no Message object construction needed). Give it a one-line docstring (becomes the prompt description over the wire).

    2. Prompt content, per D-09 (point at the rubric, do not embed it) and D-10 (citation discipline as a HARD rule mirroring the get_account_evidence docstring contract). Required instruction elements, in a sensible paragraph order (exact phrasing is Claude's discretion): (a) read the `icp://rubric` resource for the current axis definitions and weights; (b) call `get_account_evidence` with the given domain to retrieve numbered justifications; (c) score each rubric axis 1-5 using the stated weights; (d) propose the top 3 buyer personas; (e) draft an outreach hook per persona; (f) HARD rule: every claim MUST cite an [N] index from justifications, and any claim without a matching index MUST be dropped; (g) if retrieval_status is "empty", state that the account cannot be researched — never fabricate. Interpolate the domain into the text (e.g. into the opening instruction and the get_account_evidence call example). Per D-11, do not reference the full-tier tool by name or allude to tier differences anywhere in the prompt text.

    3. Register unconditionally in build_server after the resource registrations: `server.prompt()(research_account)`.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -k "research_account_prompt or list_prompts" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_mcp_server.py -x` exits 0 (all pre-existing tests plus every new resource and prompt test)
    - src/mcp_server/server.py contains `def research_account(domain: str) -> str` and the registration literal `server.prompt()(research_account)`
    - The rendered prompt text over the wire contains "icp://rubric", "get_account_evidence", "[N]", "drop", "fabricate", and the interpolated domain (asserted by the passing functional test)
    - The rendered prompt text does not contain "research_account_full" (asserted by the passing D-11 functional test)
    - `make typecheck && make lint` exits 0
  </acceptance_criteria>
  <done>Invoking research_account guides an agent through rubric-based scoring with hard [N]-citation discipline, identical on every tier, teaching the resource surface without embedding the rubric</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> resource read | Untrusted clients request resource URIs; responses must not leak server filesystem details |
| MCP client -> prompt render | Client-supplied domain string is interpolated into returned prompt text |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Information Disclosure | read_icp_rubric / read_eval_report failure path | high | mitigate | Catch OSError inside the function and return a sanitized "resource unavailable" message; real cause to stderr only (D-08, HOST-05 discipline); unit tests assert the leak-proof property with a sentinel path |
| T-12-05 | Tampering | Prompt text integrity (client-supplied domain interpolation) | low | accept | The prompt is advisory text executed by the CLIENT's own agent; an adversarial domain string only pollutes the caller's own prompt, no server-side execution surface exists |
| T-12-06 | Information Disclosure | Served resource content | low | accept | configs/icp.yaml and evals/REPORT.md are committed public-repo artifacts already scrubbed by the Phase 7 public-repo audit; serving them verbatim discloses nothing non-public |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none introduced) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_mcp_resources.py tests/functional/test_mcp_server.py -x` exits 0
- `make typecheck` and `make lint` exit 0
- Resource round-trip texts byte-match the committed files (functional tests 5-6)
</verification>

<success_criteria>
- MCP-02: icp://rubric serves configs/icp.yaml verbatim, per-request, application/yaml
- MCP-03: icp://eval-report serves evals/REPORT.md verbatim, per-request, text/markdown
- MCP-04: research_account prompt enforces [N]-citation discipline with drop-uncited and never-fabricate rules, points at the rubric resource, never mentions the full-tier tool
- D-08: read failures degrade to sanitized non-empty messages, never protocol errors, never empty content
</success_criteria>

<output>
Create `.planning/phases/12-full-tier-tool-resources-prompt/12-02-SUMMARY.md` when done
</output>

---
phase: 12-full-tier-tool-resources-prompt
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - src/mcp_server/wiring.py
  - src/mcp_server/server.py
  - src/mcp_server/__main__.py
  - tests/functional/test_mcp_server.py
autonomous: true
requirements: [MCP-05]
must_haves:
  truths:
    - "A full-tier server registers both get_account_evidence and research_account_full; a thin-tier server registers only get_account_evidence"
    - "MCP_DEMO_MODE=1 with all four full-tier keys present resolves to thin tier and research_account_full is absent from list_tools (roadmap success criterion 2, proven by a functional test)"
    - "research_account_full carries readOnlyHint=True/destructiveHint=False annotations and a description containing the 30-60 second runtime warning and the run_eval=False latency hint (D-06, MCP-07)"
    - "The full-tier lifespan is a thin wrapper around open_deps, so research_account_full and get_account_evidence share one httpx pool and both inherit replay/record branching"
  artifacts:
    - "src/mcp_server/wiring.py: EvidenceDeps protocol and make_full_lifespan"
    - "src/mcp_server/server.py: _sanitized_validation_message helper, research_account_full tool, tier parameter on build_server"
    - "src/mcp_server/__main__.py: tier captured from resolve_and_log_tier and threaded into lifespan selection and build_server"
  key_links:
    - "tier passed to build_server ALWAYS originates from settings.mcp_tier() via resolve_and_log_tier, never re-derived from key presence (RESEARCH Pitfall 4)"
    - "tier gating does NOT use the settings=None parameter, which is None on the stdio path by unrelated design (RESEARCH Pitfall 1)"
    - "EvidenceDeps uses read-only property members so both ThinDeps and the extended Deps (limiter typed bare None) satisfy it structurally under strict mypy (RESEARCH Pitfall 2)"
    - "The MCP wrapper forwards progress via a closure over ctx passed as process_account's on_stage; it never re-composes pipeline stages (D-05)"
---

<objective>
Wire the full tier: an `EvidenceDeps` structural protocol so one `get_account_evidence` type-checks against both deps shapes, a `make_full_lifespan` that delegates entirely to `open_deps` (one httpx pool, replay inherited for free), the `research_account_full` tool with per-stage progress reporting (D-04/D-05) and D-06 description, tier-gated registration in `build_server` (hidden, not visible-but-refusing), and tier threading in `__main__.py`.

Purpose: MCP-05's delivery mechanism — BYOK users get the complete grounded pipeline as a gated MCP tool, with the demo-hides-full-tool guarantee (roadmap criterion 2) proven by a functional test.
Output: modified `src/mcp_server/wiring.py`, `src/mcp_server/server.py`, `src/mcp_server/__main__.py`; registration-level functional tests in `tests/functional/test_mcp_server.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-CONTEXT.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-PATTERNS.md
@src/mcp_server/server.py
@src/mcp_server/wiring.py
@src/mcp_server/__main__.py
@src/pipeline.py
@src/config.py
</context>

## Artifacts this phase produces

Symbols created by this plan (the plan-review-convergence source-grounding pass reads this list):

- `src/mcp_server/wiring.py::EvidenceDeps` — new `typing.Protocol` with read-only properties `exa`, `browserbase`, `limiter`
- `src/mcp_server/wiring.py::make_full_lifespan` — new lifespan factory, `(settings: Settings) -> Callable[[FastMCP], AbstractAsyncContextManager[Deps]]`
- `src/mcp_server/server.py::_sanitized_validation_message` — new module-private helper, `(exc: ValidationError) -> str`
- `src/mcp_server/server.py::research_account_full` — new tool function, `(domain: str, ctx: Context[ServerSession, Deps, Request], run_eval: bool = True) -> ScoredAccount`
- `src/mcp_server/server.py::build_server` — new `tier: Literal["thin", "full"] = "thin"` parameter
- MCP surface: `research_account_full` tool (full tier only)

(Consumes from Plan 12-01: `Deps.exa`/`Deps.browserbase`/`Deps.limiter`, `process_account(run_eval=, on_stage=)`. Plan 12-02 created: `read_icp_rubric`, `read_eval_report`, `research_account`, resource/prompt registrations. Plan 12-04 creates the behavioral full-tool tests.)

<tasks>

<task type="auto">
  <name>Task 1: EvidenceDeps protocol and make_full_lifespan wrapping open_deps</name>
  <files>src/mcp_server/wiring.py, src/mcp_server/server.py, tests/functional/test_mcp_server.py</files>
  <read_first>
    - src/mcp_server/wiring.py (ThinDeps at lines 43-47 and make_thin_lifespan at lines 50-96 — the factory shape make_full_lifespan mirrors)
    - src/pipeline.py (open_deps at ~line 69 — the seam make_full_lifespan delegates to; and the Deps fields Plan 12-01 added)
    - src/mcp_server/server.py (get_account_evidence annotation at line 45 and build_server lifespan annotation at line 98 — the two annotations being widened)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md (Pattern 1 rationale and Pitfall 2 mypy analysis)
  </read_first>
  <action>
    1. In `src/mcp_server/wiring.py`, define `class EvidenceDeps(Protocol)` with three READ-ONLY property members: `exa` returning `ExaLike`, `browserbase` returning `BrowserbaseLike`, `limiter` returning `DemoLimiter | None` (each as `@property def name(self) -> Type: ...`). Property members (not plain attributes) are load-bearing: property return types are covariant, so the frozen `ThinDeps` (limiter: DemoLimiter | None) and the extended `Deps` from Plan 12-01 (limiter typed bare None, keeping pipeline.py MCP-free) both satisfy the protocol structurally under strict mypy without inheritance — this is the codebase's *Like protocol convention applied to the lifespan-context shape (RESEARCH Pitfall 2). Add `Protocol` to the typing import. Include a docstring stating the protocol names exactly the attributes get_account_evidence reads.

    2. In the same file, add `make_full_lifespan(settings: Settings) -> Callable[[FastMCP], AbstractAsyncContextManager[Deps]]`: an `@asynccontextmanager async def lifespan(_app: FastMCP) -> AsyncIterator[Deps]` whose entire body is `async with open_deps(settings) as deps: yield deps`, mirroring make_thin_lifespan's factory shape. Import `Deps` and `open_deps` from `src.pipeline`. Docstring must note: full tier is never demo mode so no limiter/clamp/NullBrowserbase wiring belongs here, and delegating to open_deps means one shared httpx pool and inherited replay/record branching (a second client stack would silently skip replay fixtures for get_account_evidence — RESEARCH "Alternatives Considered").

    3. In `src/mcp_server/server.py`, widen two annotations: get_account_evidence's ctx becomes `Context[ServerSession, EvidenceDeps, Request]`, and build_server's lifespan parameter becomes `Callable[[FastMCP], AbstractAsyncContextManager[EvidenceDeps]]`. Update the wiring import line to bring in `EvidenceDeps` (keep `ThinDeps` in the import only if still referenced; if ruff flags it unused after the widening, remove it). The function bodies are unchanged.

    4. Append one functional test to `tests/functional/test_mcp_server.py`: build `Settings(_env_file=None, exa_api_key="x", deepseek_api_key="y", browserbase_api_key="z", browserbase_project_id="p")`, enter `make_full_lifespan(settings)(FastMCP("test"))` via `async with`, and assert the yielded object is an instance of `Deps` with `deps.limiter is None` and `deps.exa is not None` and `deps.enricher is not None`. Client construction with dummy keys performs no network I/O; open_deps's finally-block closes the LLM pools on exit. Mirrors the existing test_lifespan_non_demo_mode_builds_plain_exa_and_no_limiter shape.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -x && make typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/mcp_server/wiring.py contains `class EvidenceDeps(Protocol)` with `@property` declarations for exa, browserbase, and limiter
    - src/mcp_server/wiring.py contains `def make_full_lifespan(` and the body delegates to `open_deps(settings)` (grep: `grep -n "open_deps" src/mcp_server/wiring.py` matches)
    - src/mcp_server/server.py annotates get_account_evidence's ctx with `EvidenceDeps` (grep: `grep -n "Context\[ServerSession, EvidenceDeps, Request\]" src/mcp_server/server.py` matches)
    - `uv run pytest tests/functional/test_mcp_server.py -x` exits 0 (every pre-existing thin-tier test still passes against the widened annotations)
    - `make typecheck` exits 0 (strict mypy accepts ThinDeps and Deps against EvidenceDeps — the Pitfall 2 gate)
  </acceptance_criteria>
  <done>One protocol covers both deps shapes under strict mypy; the full-tier lifespan is a delegation to open_deps with no duplicate client construction</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: research_account_full tool, tier-gated registration, and tier threading in __main__</name>
  <files>src/mcp_server/server.py, src/mcp_server/__main__.py, tests/functional/test_mcp_server.py</files>
  <read_first>
    - src/mcp_server/server.py (get_account_evidence validation block at lines 55-62 to extract into the shared helper; its docstring-contract style; build_server registration block; the existing test_annotations_and_description_over_the_wire assertion style)
    - src/mcp_server/__main__.py (main() at lines 24-54 — the tier threading site)
    - src/config.py (mcp_tier at lines 201-219 — demo-mode-before-keys ordering the gating must inherit, RESEARCH Pitfall 4)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-PATTERNS.md (progress-closure and tier-gated registration pattern blocks — verified concrete shapes)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-CONTEXT.md (D-01, D-04, D-05, D-06 constraints)
  </read_first>
  <behavior>
    - Test 1 (roadmap criterion 2): Settings with _env_file=None, exa_api_key="x", deepseek_api_key="y", browserbase_api_key="z", browserbase_project_id="p", AND mcp_demo_mode=True resolves settings.mcp_tier() == "thin"; build_server(lifespan=_lifespan_factory(FakeExa(about=[], news=[])), tier=settings.mcp_tier()) then list_tools over the in-memory session yields exactly one tool named get_account_evidence — research_account_full is hidden (not registered), not visible-but-refusing
    - Test 2 (full tier registers both, stdio-style): same Settings WITHOUT mcp_demo_mode resolves mcp_tier() == "full"; build_server(lifespan=..., tier="full") with settings left at its None default (the stdio path — Pitfall 1 warning-sign test) lists both tools; the research_account_full entry has annotations.readOnlyHint True, annotations.destructiveHint False, and a description containing "30-60", "run_eval=False", and "[N]"
    - Test 3 (default stays thin): build_server(lifespan=...) with no tier argument lists only get_account_evidence (every pre-existing call site keeps its exact behavior)
  </behavior>
  <action>
    Write the three functional tests FIRST in `tests/functional/test_mcp_server.py`, then implement:

    In `src/mcp_server/server.py`:

    1. Extract the duplicated validation-message logic (DRY): module-private `_sanitized_validation_message(exc: ValidationError) -> str` containing the existing lines 58-62 body (take errors()[0]["msg"], strip the "Value error, " prefix). Rewrite get_account_evidence's except branch to `raise ValueError(_sanitized_validation_message(exc)) from None`.

    2. Add `async def research_account_full(domain: str, ctx: Context[ServerSession, Deps, Request], run_eval: bool = True) -> ScoredAccount`. Imports needed: `ScoredAccount` added to the existing `src.models` import line; `Deps` and `process_account` from `src.pipeline`. Body:
       (a) validate via `Account(domain=domain)` with `except ValidationError as exc: raise ValueError(_sanitized_validation_message(exc)) from None` — the full tool validates through Account exactly like the thin tool (locked);
       (b) `deps = ctx.request_context.lifespan_context`;
       (c) build the progress closure (D-04/D-05): compute `total = 5 if run_eval else 4` (stages enrich/score/contacts/outreach plus eval only when run_eval), keep an incrementing counter (nonlocal int or a one-key dict), and define `async def on_stage(stage: str) -> None` that awaits `ctx.report_progress(<count>, total, message=f"{stage} complete")` — report_progress is a verified no-op when the client attached no progressToken, so ignoring clients lose nothing;
       (d) `return await process_account(account, deps, run_eval=run_eval, on_stage=on_stage)` wrapped in a defensive `except Exception as exc:` that logs `log.warning(..., exc_info=True)` and raises `ValueError("internal error, try again") from None` — mirroring get_account_evidence's catch-all (HOST-05 discipline). This is the ONLY try/except around the pipeline call: process_account already isolates per-stage domain failures internally and returns degraded-but-successful results (D-03), so unscoreable/hook_suppressed/low_groundedness outcomes flow through as successful structured results, never isError. Return the ScoredAccount INSTANCE directly — FastMCP's structured-output path serializes it, and a manual dump call would be the second serialization path D-01 forbids (the EvidencePack tool is the proven precedent). Do not re-compose stages or add per-stage handling in this wrapper (D-05).
       (e) Docstring (the citation contract lives in tool descriptions — milestone research pitfall 6): state that it runs the complete grounded pipeline returning a ScoredAccount whose hooks cite [N] indices resolving into enrichment.justifications, and what the four status values mean.

    3. Add `tier: Literal["thin", "full"] = "thin"` as a new build_server parameter (Literal already imported). Docstring note: tier gating is intentionally independent of the `settings` parameter, which is None on the stdio path for transport-kwarg reasons only (RESEARCH Pitfall 1). After the existing unconditional registrations (thin tool, resources, prompt from Plan 12-02), append: `if tier == "full":` register research_account_full via `server.tool(annotations=ToolAnnotations(readOnlyHint=True, destructiveHint=False), description=...)(research_account_full)`. The description string MUST contain the substrings "30-60", "run_eval=False", and "[N]" — required elements: full-pipeline summary (enrich, score, personas, cited outreach, judge eval), the 30-60 second runtime warning, the hint that run_eval=False skips the judge for roughly half the latency (D-06), and that outreach claims cite [N] justification indices. Exact phrasing is discretionary; PATTERNS.md's registration block is a valid starting point.

    In `src/mcp_server/__main__.py`:

    4. Capture the already-computed return value: `tier = resolve_and_log_tier(settings)`. Select the lifespan: `make_full_lifespan(settings)` when tier == "full", else `make_thin_lifespan(settings)`. Pass `tier=tier` to build_server unconditionally (independent of transport). Extend the wiring import to include make_full_lifespan. No key validation is needed here: mcp_tier() == "full" already implies writer/judge/Browserbase keys are present, and open_deps defers key checks by design.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -x && make typecheck && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_mcp_server.py -k "demo_hides_full_tool" -x` exits 0 and the test constructs Settings with all four full-tier keys plus mcp_demo_mode=True, derives tier via settings.mcp_tier(), and asserts research_account_full absent from list_tools (roadmap success criterion 2)
    - `uv run pytest tests/functional/test_mcp_server.py -x` exits 0 (all tests, including Plans 12-01/12-02 additions and every pre-existing test)
    - src/mcp_server/server.py contains `def _sanitized_validation_message(` and `async def research_account_full(` with `run_eval: bool = True` in its signature and `-> ScoredAccount` as its return annotation
    - src/mcp_server/server.py contains `tier: Literal["thin", "full"] = "thin"` in build_server's signature and an `if tier == "full":` registration branch
    - src/mcp_server/__main__.py contains `tier = resolve_and_log_tier(settings)` and passes `tier=tier` to build_server; `grep -n "make_full_lifespan" src/mcp_server/__main__.py` matches
    - `make typecheck` and `make lint` exit 0
  </acceptance_criteria>
  <done>A full-tier server (stdio or HTTP) registers and describes research_account_full with progress reporting wired through process_account's on_stage; demo mode with full keys provably hides the tool; tier always originates from settings.mcp_tier()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> research_account_full | Untrusted domain string and run_eval flag enter the full pipeline |
| Environment -> tier resolution | Env misconfiguration (keys present on a demo instance) must not expose the full tool |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Elevation of Privilege | Full-tool registration in demo mode | high | mitigate | tier passed to build_server always derives from settings.mcp_tier() (demo check precedes key check, src/config.py:213); never re-derived from key presence (Pitfall 4); Task 2 Test 1 is the enforcement with full keys present |
| T-12-08 | Information Disclosure | research_account_full error payloads | high | mitigate | Account-validator message sanitization via _sanitized_validation_message plus catch-all `raise ValueError("internal error, try again") from None` with exc_info to stderr only — the exact HOST-05 pattern already proven on get_account_evidence |
| T-12-09 | Spoofing / Input Validation | domain argument | medium | mitigate | Validated through the same Account normalizer as the thin tool (ASVS V5), before any provider access; invalid domains never reach deps |
| T-12-10 | Denial of Service | 30-60s full-pipeline call as exhaustion vector | low | accept | Full tool is BYOK/full-tier only and never registered in demo mode; the hosted demo (Phase 13) never exposes it (RESEARCH Security Domain) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none introduced) |
</threat_model>

<verification>
- `uv run pytest tests/functional/test_mcp_server.py -x` exits 0
- `make typecheck` and `make lint` exit 0
- Demo-hides-full-tool functional test passes with all four full-tier keys present (roadmap criterion 2)
</verification>

<success_criteria>
- Full tier registers research_account_full (annotated, described per D-06); thin tier and demo mode do not
- Tier gating flows exclusively from settings.mcp_tier() through build_server's explicit tier parameter, independent of transport and of the settings kwarg
- Full-tier lifespan delegates to open_deps; progress reporting reaches the client via the on_stage closure without re-composing stages (D-04/D-05)
- Strict mypy green across both deps shapes via EvidenceDeps
</success_criteria>

<output>
Create `.planning/phases/12-full-tier-tool-resources-prompt/12-03-SUMMARY.md` when done
</output>

---
phase: 12-full-tier-tool-resources-prompt
plan: 04
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - tests/functional/test_mcp_server.py
autonomous: true
requirements: [MCP-05]
must_haves:
  truths:
    - "A stubbed BYOK call to research_account_full returns isError=False with the complete ScoredAccount JSON: account, status, enrichment (with numbered justifications), score, contacts, hooks, eval_score, error (roadmap success criterion 1)"
    - "run_eval=False over the wire yields eval_score null, a non-failure status, and zero judge invocations (D-02)"
    - "Empty retrieval through the full tool is a SUCCESSFUL result carrying error text and hook_suppressed status, never isError (D-03)"
    - "Hook cited_indices in the returned payload resolve within the payload's own enrichment.justifications indices (D-01)"
    - "make test, make typecheck, and make lint all exit 0 on the merged phase changeset"
  artifacts:
    - "tests/functional/test_mcp_server.py: _full_lifespan_factory plus behavioral full-tool tests (happy path, run_eval=False, empty retrieval, invalid domain, progress)"
  key_links:
    - "The test lifespan yields a Deps built via build_deps with fake writer/judge LLMs and FakeExa/NullBrowserbase — stubs at the API boundary, matching the CLAUDE.md functional-test layer"
    - "structuredContent is FastMCP's own serialization of the returned ScoredAccount instance — the tests prove the single-serialization-path survives the JSON round-trip (D-01)"
---

<objective>
Prove MCP-05's behavioral contract end-to-end over the in-memory MCP transport with stubbed dependencies: the complete `ScoredAccount` JSON round-trip including `AccountStatus`, the D-02 run_eval honesty semantics at the wire level, D-03 degraded-stage mirroring, sanitized invalid-domain errors, and per-stage progress notifications. Close the phase with the full offline gate.

Purpose: Plans 12-01/12-03 built the seam and the wiring; this plan is the spec-mandated functional test surface (RESEARCH Validation Architecture rows MCP-05 criterion 1, D-02, D-04/D-05) and the phase gate.
Output: extended `tests/functional/test_mcp_server.py`; green `make test && make typecheck && make lint`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-CONTEXT.md
@.planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md
@tests/functional/test_mcp_server.py
@tests/integration/test_pipeline_failures.py
@src/mcp_server/server.py
@src/pipeline.py
</context>

## Artifacts this phase produces

Symbols created by this plan (the plan-review-convergence source-grounding pass reads this list):

- `tests/functional/test_mcp_server.py::_full_lifespan_factory` — new test lifespan factory yielding a prepared `Deps`
- New test functions in `tests/functional/test_mcp_server.py` covering the full tool's happy path, run_eval=False, empty retrieval, invalid domain, and progress notifications

(Consumes symbols created by Plans 12-01/12-02/12-03: `Deps.exa`/`Deps.browserbase`/`Deps.limiter`, `process_account(run_eval=, on_stage=)`, `EvidenceDeps`, `make_full_lifespan`, `research_account_full`, `build_server(tier=)`, `read_icp_rubric`, `read_eval_report`, `research_account`.)

<tasks>

<task type="auto">
  <name>Task 1: Behavioral functional tests for research_account_full over the in-memory transport</name>
  <files>tests/functional/test_mcp_server.py</files>
  <read_first>
    - tests/functional/test_mcp_server.py (the _lifespan_factory shape at lines 23-31, harness usage, and the assertion style of test_happy_path_returns_structured_evidence_pack — the new tests extend all three)
    - tests/integration/test_pipeline_failures.py (the FakeExa/FakeBrowserbase classes and, critically, the happy-path fake-LLM `synthesize` implementations plus `build_deps(writer=..., judge=..., exa=..., browserbase=...)` construction around lines 40-150 — replicate these JSON payload shapes exactly; the writer fake must produce valid firmographics, score, contacts, and outreach JSON, and the judge fake valid eval JSON)
    - src/mcp_server/server.py (research_account_full body from Plan 12-03 — what the tests exercise)
    - .venv/lib/python3.12/site-packages/mcp/client/session.py (line ~391: call_tool's progress_callback parameter and the ProgressFnT signature — verify the exact callback shape against the installed SDK before writing the progress test)
  </read_first>
  <action>
    Add to `tests/functional/test_mcp_server.py`:

    1. `_full_lifespan_factory(deps: Deps)` — an asynccontextmanager factory mirroring `_lifespan_factory` but typed `AbstractAsyncContextManager[Deps]` and yielding a caller-prepared `Deps`. Build test deps via `build_deps(writer=<fake writer LLM>, judge=<fake judge LLM>, exa=<FakeExa>, browserbase=<fake/Null browserbase>)` imported from `src.pipeline` — build_deps populates the new exa/browserbase fields itself (Plan 12-01), so the same Deps serves both tools. Reuse or adapt the fake-LLM synthesize payloads from tests/integration/test_pipeline_failures.py's happy path; keep writer and judge as SEPARATE fake objects so judge invocations are countable. Servers for these tests: `build_server(lifespan=_full_lifespan_factory(deps), tier="full")`.

    2. Test full happy path (roadmap criterion 1, D-01): `call_tool("research_account_full", {"domain": "notion.so"})` returns isError False and structuredContent containing every ScoredAccount field name: "account", "status", "enrichment", "score", "contacts", "hooks", "eval_score", "error". Assert status is one of "clean"/"low_groundedness"/"hook_suppressed"/"judge_failed" (AccountStatus StrEnum values) and matches what the fakes imply; assert enrichment.justifications is non-empty with 1-based index fields; assert every hook's cited_indices values are a subset of the justification index set — the D-01 guarantee that [N] citations resolve within the payload itself.

    3. Test run_eval=False honesty (D-02): `call_tool("research_account_full", {"domain": "notion.so", "run_eval": False})` returns isError False, structuredContent["eval_score"] is None, structuredContent["status"] == "clean", and the judge fake recorded zero synthesize calls.

    4. Test empty retrieval mirrors the sheet (D-03): a Deps whose FakeExa returns no about and no news yields isError False (never promoted to isError — empty retrieval is honest data), structuredContent["status"] == "hook_suppressed", structuredContent["error"] == "empty enrichment", structuredContent["score"] is None, hooks empty.

    5. Test invalid domain sanitized: `call_tool("research_account_full", {"domain": "example.com/path"})` returns isError True with "invalid domain" in the text content, no "Traceback" and no "/Users/" in the text, and the FakeExa recorded no calls (validation precedes provider access, matching the thin tool's parametrized test assertions).

    6. Test progress notifications (D-04): pass an async `progress_callback` collector to call_tool (signature per ProgressFnT verified in read_first; collect (progress, total, message) tuples). On a run_eval=True happy path assert exactly 5 notifications, progress values 1.0 through 5.0 with total 5.0, and messages ending in "complete" covering the five stage names enrich/score/contacts/outreach/eval in order. If the installed harness does not propagate progress notifications end-to-end (verify first — the in-memory session shares the real Session machinery so it should), fall back to asserting at least the count and monotonically increasing progress values; do not weaken further without noting it in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_mcp_server.py -k "full_tool" -x` exits 0 with at least 5 new tests collected
    - The happy-path test asserts all eight ScoredAccount field names in structuredContent and the cited_indices-subset-of-justification-indices property
    - The run_eval=False test asserts eval_score is None AND status == "clean" AND judge call count == 0
    - The empty-retrieval test asserts isError is False AND status == "hook_suppressed" AND error == "empty enrichment"
    - The progress test asserts 5 stage notifications with total 5.0 on a run_eval=True call
  </acceptance_criteria>
  <done>The full tool's wire contract — complete ScoredAccount JSON, honesty semantics, degraded-stage mirroring, sanitized errors, per-stage progress — is locked in by in-memory functional tests using stubs at the API boundary</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full offline suite, strict mypy, lint</name>
  <files>tests/functional/test_mcp_server.py</files>
  <read_first>
    - Makefile (test/typecheck/lint targets — the exact gate commands)
    - .planning/phases/12-full-tier-tool-resources-prompt/12-RESEARCH.md (Sampling Rate section — the phase-gate precedent from Phases 10/11)
  </read_first>
  <action>
    Run the merged-changeset gate: `make test` (full offline suite, smoke excluded), `make typecheck` (mypy strict over src and evals, no new overrides permitted), `make lint` (ruff + black --check). Fix any failure surfaced by the merged Wave 1-3 changeset — permitted fixes are limited to formatting, import ordering, unused imports, type annotations, and test adjustments; any behavioral fix to src/ must be the minimal change preserving every locked decision (D-01 through D-11) and must be recorded in the SUMMARY with its rationale. If all three gates pass on the first run with no changes, record that (Phase 9's 09-04 precedent: a clean no-op gate is a valid outcome).
  </action>
  <verify>
    <automated>make test && make typecheck && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0
    - `make typecheck` exits 0 with no new mypy overrides added to pyproject.toml (git diff on pyproject.toml is empty)
    - `make lint` exits 0
    - `git diff --stat tests/integration/test_pipeline_failures.py tests/integration/test_pipeline.py` is empty (pre-existing pipeline tests were never modified across the phase)
  </acceptance_criteria>
  <done>The complete phase changeset passes the same offline gate every prior MCP phase shipped under; strict mypy stays strict</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> research_account_full (tested surface) | These tests are the enforcement artifacts for the phase's client-facing guarantees |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11 | Information Disclosure | Full-tool error payloads (regression surface) | high | mitigate | Task 1 test 5 asserts sanitized invalid-domain text with no traceback or filesystem-path fragments, extending the thin tool's existing leak-proof assertions to the new tool (HOST-05 discipline) |
| T-12-12 | Repudiation | run_eval skip mislabeled as failure at the wire level | medium | mitigate | Task 1 test 3 pins eval_score null + clean status + zero judge calls over the actual JSON-RPC round-trip, not just the pipeline unit level (D-02) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none introduced) |
</threat_model>

<verification>
- `uv run pytest tests/functional/test_mcp_server.py -x` exits 0
- `make test && make typecheck && make lint` exits 0
- Roadmap success criteria 1 (full ScoredAccount JSON incl. AccountStatus) and 2 (demo hides full tool, from Plan 12-03) both hold under the merged changeset
</verification>

<success_criteria>
- MCP-05 criterion 1 proven functionally: complete grounded ScoredAccount JSON including AccountStatus over the in-memory MCP transport
- D-01/D-02/D-03 wire-level behaviors locked by tests
- Phase gate green: full offline suite, strict mypy with no new overrides, lint
</success_criteria>

<output>
Create `.planning/phases/12-full-tier-tool-resources-prompt/12-04-SUMMARY.md` when done
</output>

---
phase: 13-hosted-deploy-docs-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/mcp_server/server.py
  - src/mcp_server/__main__.py
  - tests/functional/test_mcp_http_transport.py
  - tests/unit/test_mcp_main_guard.py
  - .env.example
autonomous: true
requirements: [HOST-06, TEST-01]
must_haves:
  truths:
    - "A client sending the public Fly hostname as its Host header is accepted by the HTTP transport even when the server binds 0.0.0.0"
    - "A client sending the bind address 0.0.0.0:8000 as its Host header is rejected with 421"
    - "Starting the HTTP transport on a non-loopback bind without MCP_PUBLIC_HOSTNAME refuses to start with an actionable message"
    - "Local loopback workflows (make mcp-http, make mcp-demo) behave exactly as before"
  artifacts:
    - path: src/config.py
      provides: "mcp_public_hostname Settings field, empty default, WHY comment citing D-05/HOST-06"
      contains: "mcp_public_hostname"
    - path: src/mcp_server/server.py
      provides: "public-hostname-parametrized TransportSecuritySettings allowlist with wildcard-bind exclusion"
    - path: src/mcp_server/__main__.py
      provides: "guard_non_loopback_requires_public_hostname fail-fast startup guard"
      contains: "guard_non_loopback_requires_public_hostname"
    - path: tests/functional/test_mcp_http_transport.py
      provides: "D-07 bind-vs-Host integration test"
      contains: "test_public_hostname_allowed_but_bare_bind_address_is_not"
    - path: tests/unit/test_mcp_main_guard.py
      provides: "D-06 guard unit tests (refuse, allow-with-hostname, loopback-unaffected, stdio-unaffected)"
  key_links:
    - from: src/config.py
      to: src/mcp_server/server.py
      via: "build_server reads settings.mcp_public_hostname when composing allowed_hosts/allowed_origins"
      pattern: "mcp_public_hostname"
    - from: src/mcp_server/__main__.py
      to: src/config.py
      via: "guard reads settings.mcp_http_host and settings.mcp_public_hostname in main() before build_server"
      pattern: "guard_non_loopback_requires_public_hostname"
---

<objective>
Close the HOST-06 transport-hardening gap: add the `mcp_public_hostname` setting (D-05), parametrize `build_server`'s existing `TransportSecuritySettings` allowlist with it so a real client's Fly-hostname Host header is accepted while the `0.0.0.0` bind address is never allowlisted (D-07), and add the D-06 fail-fast startup guard that refuses a non-loopback HTTP bind with no public hostname configured. All behavior is proven by new unit and functional tests (TEST-01 partial).

Purpose: 12-SECURITY's scope note is explicit that without this change, every real client request to the hosted URL would 421 against the loopback-only allowlist, and the 12-REVIEW WR-03 observation that `0.0.0.0:{port}` was itself allowlisted stays open.

Output: modified `src/config.py`, `src/mcp_server/server.py`, `src/mcp_server/__main__.py`; new tests in `tests/functional/test_mcp_http_transport.py` and `tests/unit/test_mcp_main_guard.py`; `.env.example` documentation for `MCP_PUBLIC_HOSTNAME`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-hosted-deploy-docs-close/13-CONTEXT.md
@.planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md
@src/config.py
@src/mcp_server/server.py
@src/mcp_server/__main__.py
</context>

## Artifacts this phase produces (plan 01 slice)

- `Settings.mcp_public_hostname: str = ""` field in `src/config.py`
- `MCP_PUBLIC_HOSTNAME` env var (consumed via pydantic-settings)
- Extended `allowed_hosts`/`allowed_origins` composition inside `build_server` in `src/mcp_server/server.py` (local list variables, conditional appends; no new function)
- `guard_non_loopback_requires_public_hostname(settings, transport)` function in `src/mcp_server/__main__.py` plus its call site in `main()`
- Test `test_public_hostname_allowed_but_bare_bind_address_is_not` in `tests/functional/test_mcp_http_transport.py`
- Tests `test_refuses_non_loopback_http_bind_without_public_hostname`, `test_allows_non_loopback_http_bind_with_public_hostname`, `test_loopback_http_bind_is_unaffected_by_hostname_guard`, `test_stdio_is_unaffected_by_hostname_guard` in `tests/unit/test_mcp_main_guard.py`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mcp_public_hostname setting plus additive allowlist with wildcard-bind exclusion (D-05, D-07)</name>
  <files>src/config.py, src/mcp_server/server.py, tests/functional/test_mcp_http_transport.py, .env.example</files>
  <read_first>
    - src/config.py (current Settings fields; the mcp_http_host/mcp_http_port block at lines 136-147 is the insertion point and comment-style analog)
    - src/mcp_server/server.py (build_server lines 234-314; the settings-not-None branch builds the TransportSecuritySettings this task parametrizes)
    - tests/functional/test_mcp_http_transport.py (whole file; test_dns_rebinding_rejected_for_foreign_host and test_configured_non_loopback_host_is_allowed are the analogs the new test copies)
    - .planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md (sections for src/config.py and src/mcp_server/server.py carry the exact code excerpts; note the deviation below)
  </read_first>
  <behavior>
    - Test test_public_hostname_allowed_but_bare_bind_address_is_not (async, in tests/functional/test_mcp_http_transport.py): build Settings with _env_file=None, exa_api_key="x", mcp_http_host="0.0.0.0", mcp_public_hostname="poc-scraper-mcp.fly.dev"; build_server with _lifespan_factory(FakeExa(about=[_exa_about()], news=[_exa_news()])) and those settings; enter asgi_app.router.lifespan_context; POST the same initialize JSON-RPC body and Accept/Content-Type headers used by test_dns_rebinding_rejected_for_foreign_host to /mcp twice via httpx.ASGITransport: once with base_url "https://poc-scraper-mcp.fly.dev" (assert status_code != 421) and once with base_url "http://0.0.0.0:8000" (assert status_code == 421).
    - All five pre-existing tests in tests/functional/test_mcp_http_transport.py keep passing unchanged, including test_configured_non_loopback_host_is_allowed (mcp_http_host="app.internal" must still be accepted).
  </behavior>
  <action>
    Write the failing test first (RED), then implement (GREEN).

    In src/config.py, add mcp_public_hostname: str = "" immediately after mcp_http_host, with a comment-first block in the established style explaining WHY per D-05/HOST-06: this is the externally-visible hostname a real client's Host header carries (Fly's edge-forwarded hostname), deliberately distinct from mcp_http_host which stays a pure bind address (11-SECURITY scope correction); when set it sources additional TransportSecuritySettings allowlist entries in build_server; empty default keeps local make mcp-http and make mcp-demo unaffected. Copy the comment shape from 13-PATTERNS.md's src/config.py section.

    In src/mcp_server/server.py build_server, inside the settings-not-None branch, build allowed_hosts and allowed_origins as local list variables before the FastMCP constructor call, then pass them into TransportSecuritySettings(enable_dns_rebinding_protection=True, allowed_hosts=allowed_hosts, allowed_origins=allowed_origins). Composition rules (this is the one deliberate DEVIATION from the 13-PATTERNS.md excerpt, which keeps the bind-derived entry unconditionally):
    1. Always include the two loopback pairs: "127.0.0.1:{mcp_http_port}" and "localhost:{mcp_http_port}" in allowed_hosts, and their "http://" forms in allowed_origins.
    2. Append the bind-derived pair "{mcp_http_host}:{mcp_http_port}" (and "http://{mcp_http_host}:{mcp_http_port}" origin) ONLY when settings.mcp_http_host is not a wildcard bind, i.e. not in the set {"0.0.0.0", "::"}. Rationale (roadmap success criterion 2 / D-07): a wildcard bind is a listener address, not a hostname any legitimate client sends, so allowlisting it re-opens the 12-REVIEW WR-03 hole. This exclusion is required for the new test to pass while test_configured_non_loopback_host_is_allowed (host "app.internal") keeps passing.
    3. When settings.mcp_public_hostname is truthy, append the bare hostname (primary entry; HTTPS on default port 443 omits the port suffix from the Host header, RESEARCH Pitfall 3), then "{mcp_public_hostname}:443" as defense-in-depth, to allowed_hosts; append "https://{mcp_public_hostname}" to allowed_origins.
    Update the long comment above the FastMCP HTTP construction (currently saying a 0.0.0.0 bind "still needs Phase 13's separate public-hostname setting") to describe the implemented state: public hostname sourced from mcp_public_hostname, wildcard binds never allowlisted. Also update the build_server docstring sentence that anticipated this change.

    Add the new functional test exactly per the behavior block, importing nothing new beyond what the file already imports.

    Document MCP_PUBLIC_HOSTNAME in .env.example next to the existing MCP_ entries (default empty, one-line comment: externally-visible hostname for the hosted HTTP deploy, distinct from the MCP_HTTP_HOST bind address). If tool permissions deny reading or editing .env.example, do not fight it: note the omission explicitly in the SUMMARY and ensure docs/DEPLOY.md (plan 13-02) carries the variable documentation instead.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_http_transport.py -q && uv run mypy src evals</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_mcp_http_transport.py -q` passes with 6 tests collected (5 pre-existing plus the new one)
    - The new test asserts both directions: Host "poc-scraper-mcp.fly.dev" accepted (!= 421) and Host "0.0.0.0:8000" rejected (== 421) against the same 0.0.0.0-bound server
    - `src/config.py` contains a field named exactly `mcp_public_hostname` with default `""` and a preceding comment that cites D-05
    - `uv run mypy src evals` exits 0 with no new overrides added to pyproject.toml
  </acceptance_criteria>
  <done>The HTTP transport accepts the public Fly hostname and rejects the bind address as a Host header when bound to 0.0.0.0 with mcp_public_hostname set, proven by a passing functional test; existing transport tests unchanged and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-06 fail-fast guard for non-loopback binds without a public hostname</name>
  <files>src/mcp_server/__main__.py, tests/unit/test_mcp_main_guard.py</files>
  <read_first>
    - src/mcp_server/__main__.py (whole file; guard_full_tier_http_exposure at lines 24-44 is the shape to copy verbatim, and main() lines 47-63 contain the call site)
    - tests/unit/test_mcp_main_guard.py (whole file; every new test copies the crafted-Settings + pytest.raises(SystemExit) pattern)
    - .planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md (sections for __main__.py and test_mcp_main_guard.py carry drafted excerpts)
  </read_first>
  <behavior>
    - test_refuses_non_loopback_http_bind_without_public_hostname: Settings(_env_file=None, exa_api_key="x", mcp_http_host="0.0.0.0") plus transport="http" raises SystemExit; str(exc_info.value) contains "MCP_PUBLIC_HOSTNAME"
    - test_allows_non_loopback_http_bind_with_public_hostname: same Settings plus mcp_public_hostname="poc-scraper-mcp.fly.dev" does not raise
    - test_loopback_http_bind_is_unaffected_by_hostname_guard: default mcp_http_host ("127.0.0.1") with no hostname and transport="http" does not raise
    - test_stdio_is_unaffected_by_hostname_guard: mcp_http_host="0.0.0.0", no hostname, transport="stdio" does not raise
  </behavior>
  <action>
    Write the four failing unit tests first (RED), then implement (GREEN).

    In src/mcp_server/__main__.py, add guard_non_loopback_requires_public_hostname(settings: Settings, transport: str) -> None directly below guard_full_tier_http_exposure, copying its shape exactly: docstring explaining the D-06 rationale (a non-loopback bind with no MCP_PUBLIC_HOSTNAME means real clients' Host headers would 421 against the DNS-rebinding allowlist, or the allowlist gets silently misconfigured; loopback binds keep Phase 11's localhost allowlist untouched, so local workflows are unaffected; small named function so it unit-tests without a server or argv). Condition: transport == "http" and settings.mcp_http_host not in the loopback set {"127.0.0.1", "localhost", "::1"} and not settings.mcp_public_hostname; on match raise SystemExit with a message that names the offending bind value via repr, names MCP_PUBLIC_HOSTNAME as the fix, and offers the loopback-bind alternative for local development (wording per the 13-PATTERNS.md draft; exact phrasing is discretionary but must contain the literal MCP_PUBLIC_HOSTNAME).

    Call site: in main(), immediately after the existing guard_full_tier_http_exposure(settings, args.transport, tier) line, add guard_non_loopback_requires_public_hostname(settings, args.transport). Both guards must run before build_server and before any asyncio.run.

    Add guard_non_loopback_requires_public_hostname to the existing import line in tests/unit/test_mcp_main_guard.py and append the four tests per the behavior block, matching the file's existing naming and type-annotation style (all test functions annotated -> None).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mcp_main_guard.py -q && uv run mypy src evals</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_mcp_main_guard.py -q` passes with 9 tests collected (5 pre-existing plus 4 new)
    - `src/mcp_server/__main__.py` defines `guard_non_loopback_requires_public_hostname` and `main()` calls it on the line after the `guard_full_tier_http_exposure` call
    - The SystemExit message for a 0.0.0.0 bind contains the substring "MCP_PUBLIC_HOSTNAME" (asserted in the unit test)
    - `uv run mypy src evals` exits 0
  </acceptance_criteria>
  <done>A misconfigured container (non-loopback bind, no public hostname) dies visibly at startup with an actionable message; loopback and stdio paths are provably unaffected; all guard unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet client -> Fly edge -> app HTTP transport | Untrusted Host/Origin headers cross here; DNS-rebinding surface |
| Operator env -> Settings | MCP_PUBLIC_HOSTNAME / MCP_HTTP_HOST configuration errors become security posture errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Spoofing | TransportSecuritySettings allowlist in build_server | high | mitigate | Public hostname sourced from a dedicated setting; wildcard binds (0.0.0.0, ::) excluded from allowed_hosts; D-07 functional test asserts acceptance and rejection |
| T-13-02 | Tampering | Startup configuration path in __main__.py | medium | mitigate | D-06 fail-fast guard: non-loopback bind with no public hostname raises SystemExit before serving; four unit tests cover refuse/allow/loopback/stdio |
| T-13-03 | Information Disclosure | Guard SystemExit message | low | accept | Message names the bind value and the env var name of the fix (operator-facing stderr at startup, never a tool payload; consistent with the shipped WR-03 guard) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_mcp_main_guard.py tests/functional/test_mcp_http_transport.py -q` fully green
- `uv run mypy src evals` exits 0; `git diff pyproject.toml` shows no mypy-section changes
- `uv run ruff check src tests` and `uv run black --check src tests` clean on touched files
</verification>

<success_criteria>
- HOST-06 allowlist half satisfied: public hostname setting exists, sources the allowlist, bind address never allowlisted for wildcard binds, proven by the D-07 test
- D-06 guard ships in the WR-01/WR-03 fail-loud pattern with 4 unit tests
- Zero behavior change for stdio, loopback HTTP, and every pre-existing test
</success_criteria>

<output>
Create `.planning/phases/13-hosted-deploy-docs-close/13-01-SUMMARY.md` when done.
</output>

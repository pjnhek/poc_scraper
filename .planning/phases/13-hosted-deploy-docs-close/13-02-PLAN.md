---
phase: 13-hosted-deploy-docs-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - fly.toml
  - Makefile
  - docs/DEPLOY.md
autonomous: true
requirements: [HOST-03, HOST-06]
user_setup:
  - service: fly.io
    why: "Hosted MCP endpoint (HOST-03); the fly launch dry run registers the app under the operator's Fly account"
    dashboard_config:
      - task: "Create a Fly.io account and authenticate the CLI (fly auth login)"
        location: "fly.io signup, then local terminal"
must_haves:
  truths:
    - "The roadmap-mandated fly launch dry run happens before the Dockerfile and fly.toml are finalized"
    - "Dockerfile and fly.toml are committed with zero secret values"
    - "fly.toml encodes suspend-on-idle single-machine demo-mode hosting (internal_port 8000, force_https, suspend)"
    - "make deploy wraps the confirmed fly deploy invocation as a thin one-line target"
    - "docs/DEPLOY.md documents the full operator sequence including the fly scale count 1 single-machine pin"
  artifacts:
    - path: Dockerfile
      provides: "multi-stage uv build running python -m src.mcp_server --transport http with MCP_DEMO_MODE=1 and MCP_HTTP_HOST=0.0.0.0"
      contains: "MCP_DEMO_MODE=1"
    - path: fly.toml
      provides: "app identity, MCP_PUBLIC_HOSTNAME env, http_service with suspend-on-idle, vm sizing"
      contains: "auto_stop_machines"
    - path: docs/DEPLOY.md
      provides: "operator runbook per D-15: dry-run notes, secrets, deploy, pin, cost, teardown"
    - path: Makefile
      provides: "deploy target wrapping fly deploy"
      contains: "deploy:"
  key_links:
    - from: fly.toml
      to: src/config.py
      via: "MCP_PUBLIC_HOSTNAME in [env] feeds Settings.mcp_public_hostname; internal_port matches mcp_http_port default 8000"
      pattern: "MCP_PUBLIC_HOSTNAME"
    - from: Dockerfile
      to: src/mcp_server/__main__.py
      via: "CMD python -m src.mcp_server --transport http; MCP_HTTP_HOST=0.0.0.0 ENV makes the D-06 guard require the fly.toml hostname"
      pattern: "src.mcp_server"
---

<objective>
Produce the deploy artifacts for the hosted demo endpoint (HOST-03) with the roadmap-mandated early `fly launch` dry run done first: a committed multi-stage uv Dockerfile, a `fly.toml` encoding the D-01 suspend-on-idle and D-02 cost posture with the D-04 app identity, a thin `make deploy` target (D-13), and the `docs/DEPLOY.md` operator runbook (D-15) that carries the D-03 single-machine pin discipline (HOST-06 fly.toml half).

Purpose: no confirmed reference exists for Python + streamable HTTP on Fly (roadmap research flag), so the dry run falsifies RESEARCH assumptions A1-A3 cheaply before the artifacts are finalized.

Output: `Dockerfile`, `.dockerignore`, `fly.toml`, `Makefile` deploy target, `docs/DEPLOY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-hosted-deploy-docs-close/13-CONTEXT.md
@.planning/phases/13-hosted-deploy-docs-close/13-RESEARCH.md
@.planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md
@Makefile
@pyproject.toml
</context>

## Artifacts this phase produces (plan 02 slice)

- `Dockerfile` (new file, repo root): multi-stage uv build, builder + runtime stages
- `.dockerignore` (new file, repo root)
- `fly.toml` (new file, repo root): app name, `[env] MCP_PUBLIC_HOSTNAME`, `[http_service]` block, `[[vm]]` block
- `Makefile` target `deploy` (plus `.PHONY` entry)
- `docs/DEPLOY.md` (new file): operator runbook

<tasks>

<task type="auto">
  <name>Task 1: fly CLI preflight and fly launch dry run (D-04, roadmap research flag)</name>
  <files>docs/DEPLOY.md</files>
  <read_first>
    - .planning/phases/13-hosted-deploy-docs-close/13-RESEARCH.md (Assumptions Log A1-A3, Open Questions 1-2, Common Pitfalls 1-2; these are exactly what the dry run must confirm or falsify)
    - Makefile (mcp-demo target is what the container will run; no edit in this task)
  </read_first>
  <action>
    Preflight: check for the CLI with command -v fly (also try flyctl). If missing, install it (brew install flyctl on macOS). Run fly version. Run fly auth whoami; if unauthenticated, pause with an authentication gate checkpoint asking the operator to run fly auth login, then retry (standard GSD auth-gate flow, not a pre-planned checkpoint).

    Dry run (must precede Task 2's hand-authored artifacts, per the roadmap note): run fly launch --no-deploy --name poc-scraper-mcp with a single region (accept the suggested nearest region or use iad) and record:
    1. Whether the app name poc-scraper-mcp is available (D-04). If taken, pick the closest repo-plus-surface variant (for example poc-scraper-mcp-server) and use it consistently in every later artifact; record the final name for plans 13-04 and 13-05.
    2. The generated fly.toml content (keep a copy in the scratchpad; Task 2 hand-authors the final file and may delete or overwrite the generated one).
    3. Whether fly launch --help and fly deploy --help list a smoke-checks flag (RESEARCH Pitfall 1 recommends disabling smoke checks for MCP JSON-RPC endpoints); record the exact flag name and which subcommand accepts it.
    4. Whether the generated fly.toml contains any machine-count key (falsifies RESEARCH A2; expected answer: no, the pin is fly scale count 1 post-deploy).
    5. How many machines fly status reports for the freshly launched (undeployed) app.

    If fly launch cannot run at all (no account, CLI install impossible in this environment), degrade gracefully: author Task 2's artifacts from the RESEARCH patterns as-is, create docs/DEPLOY.md with a clearly labeled "dry run pending" note in the launch section, and record in the SUMMARY that D-04 name confirmation moved to plan 13-04's deploy preflight. Do not silently skip: the SUMMARY must state which of the five observations were confirmed.

    Start docs/DEPLOY.md now with only a heading and a "Dry run findings" section capturing observations 1-5 (Task 3 completes the runbook). Use plain hyphens; never use em-dash characters in this file (published markdown rule).
  </action>
  <verify>
    <automated>test -f docs/DEPLOY.md && grep -qi "dry run" docs/DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `fly version` output (or the explicit degraded-mode note) is recorded in docs/DEPLOY.md's dry-run findings section
    - The confirmed app name (poc-scraper-mcp or the fallback variant) is stated in docs/DEPLOY.md
    - The confirmed smoke-checks flag spelling (or its confirmed absence) is stated in docs/DEPLOY.md
    - docs/DEPLOY.md exists and `grep -c '—' docs/DEPLOY.md` returns 0
  </acceptance_criteria>
  <done>The dry run has confirmed or falsified A1-A3-relevant mechanics (name availability, fly.toml shape, smoke-checks flag, machine-count behavior) before any deploy artifact is finalized, and the findings are written down.</done>
</task>

<task type="auto">
  <name>Task 2: author Dockerfile, .dockerignore, and fly.toml (D-01, D-03, D-04, D-13)</name>
  <files>Dockerfile, .dockerignore, fly.toml</files>
  <read_first>
    - .planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md (Dockerfile and fly.toml sections carry the verbatim starting-point excerpts from RESEARCH Patterns 3 and 4)
    - pyproject.toml (requires-python floor, hatchling build config; determines whether the in-container uv sync needs README.md or other files copied for the project build)
    - docs/DEPLOY.md (Task 1's dry-run findings; use the confirmed app name and region)
  </read_first>
  <action>
    Author Dockerfile from the 13-PATTERNS.md excerpt (multi-stage uv build): builder stage FROM python:3.11-slim, uv binaries copied from ghcr.io/astral-sh/uv:latest, COPY pyproject.toml uv.lock first then uv sync --frozen --no-install-project --no-dev with the uv cache mount, then COPY src, evals, and configs directories, then final uv sync --frozen --no-dev; runtime stage FROM python:3.11-slim copying /app from builder, WORKDIR /app, ENV PATH prepending /app/.venv/bin plus MCP_DEMO_MODE=1 and MCP_HTTP_HOST=0.0.0.0, EXPOSE 8000, CMD python -m src.mcp_server --transport http (exec form). If the project build inside uv sync fails on a missing file referenced by hatchling (for example README.md), COPY that file too and note it in the SUMMARY. Never put MCP_PUBLIC_HOSTNAME or EXA_API_KEY in the Dockerfile: the hostname is fly.toml [env] config, the key is a Fly secret (D-13).

    Author .dockerignore excluding at minimum: .git, .venv, .planning, .pytest_cache, .mypy_cache, .ruff_cache, tests, images, runs, fixtures, htmlcov, tmp, docs, inputs, credentials.json, .env (RESEARCH Pitfall 4 layer-cache hygiene plus keeping any secret-bearing local files out of the build context).

    Author fly.toml from the 13-PATTERNS.md excerpt with the Task-1-confirmed app name: app = "poc-scraper-mcp" (or fallback), primary_region from the dry run, [env] MCP_PUBLIC_HOSTNAME = "<app-name>.fly.dev", [http_service] with internal_port = 8000 (matches Settings.mcp_http_port default), force_https = true (RESEARCH Pitfall 5), auto_stop_machines = "suspend" (D-01: suspend preserves in-memory DemoLimiter counters across idle), auto_start_machines = true, min_machines_running = 0 (D-02 cost posture; RESEARCH Pitfall 2 says 0 is correct and 1 would bill continuously), [[vm]] size = "shared-cpu-1x", memory = "256mb" (D-02). Do not add any [[http_service.checks]] block (RESEARCH Open Question 2 recommendation: none for first deploy). Machine-count pinning is deliberately NOT in this file; it is the fly scale count 1 step documented in Task 3 (D-03, RESEARCH Pitfall 2).

    Validate: run fly config validate (if the CLI is available) and docker build . (if Docker is available; if not, note the fly deploy --remote-only fallback in the SUMMARY and rely on plan 13-04's deploy to exercise the build). Both files must pass the pre-commit public-discipline guard unmodified (they contain no vendor or secret strings by construction).
  </action>
  <verify>
    <automated>test -f Dockerfile && test -f fly.toml && grep -q 'MCP_DEMO_MODE=1' Dockerfile && grep -q 'auto_stop_machines = "suspend"' fly.toml && grep -q 'internal_port = 8000' fly.toml && grep -q 'force_https = true' fly.toml && ! grep -qi 'EXA_API_KEY' Dockerfile && ! grep -qi 'EXA_API_KEY' fly.toml</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile ENV carries MCP_DEMO_MODE=1 and MCP_HTTP_HOST=0.0.0.0; CMD runs python -m src.mcp_server --transport http
    - fly.toml contains the [env] MCP_PUBLIC_HOSTNAME entry set to the confirmed app hostname, internal_port = 8000, force_https = true, auto_stop_machines = "suspend", min_machines_running = 0, and a [[vm]] block with shared-cpu-1x / 256mb
    - Neither Dockerfile nor fly.toml contains the string EXA_API_KEY or any secret value (grep gate above)
    - fly.toml has no [[http_service.checks]] block
    - If Docker is available locally: docker build . exits 0
  </acceptance_criteria>
  <done>Committed, secret-free deploy artifacts exist that encode the locked lifecycle decisions (suspend, one region, demo mode, 0.0.0.0 bind with the hostname supplied via fly.toml env) and build cleanly where tooling allows.</done>
</task>

<task type="auto">
  <name>Task 3: make deploy target and docs/DEPLOY.md runbook (D-02, D-03, D-13, D-15)</name>
  <files>Makefile, docs/DEPLOY.md</files>
  <read_first>
    - Makefile (whole file; every target is a one-to-three-line thin wrapper and the .PHONY list on line 1 must gain the new name)
    - docs/DEPLOY.md (Task 1's dry-run findings section; this task completes the file)
    - .planning/phases/13-hosted-deploy-docs-close/13-CONTEXT.md (D-15 runbook contents list, D-02 cost expectations)
  </read_first>
  <action>
    Add a deploy target to the Makefile: a one-line wrapper around fly deploy using the smoke-checks-disabling flag exactly as confirmed in Task 1 (expected shape: fly deploy --smoke-checks=false; if the dry run showed the flag lives only on fly launch, use plain fly deploy and state that in DEPLOY.md). Add deploy to the .PHONY list on line 1. No conditional logic in the target (thin-wrapper convention).

    Complete docs/DEPLOY.md as the D-15 operator runbook with these sections, in order, using numbered steps and one fenced code block per command (match README.md's setup-section tone; no marketing language, no em-dash characters, no emojis):
    1. Prerequisites: flyctl installed (brew install flyctl), fly auth login, Docker Engine recommended for local builds with fly deploy --remote-only as the fallback.
    2. Dry run findings (already present from Task 1; keep).
    3. First deploy: fly launch notes (app already created by the dry run), then fly secrets set EXA_API_KEY=<your-exa-key> (the only secret; never in the image or fly.toml, per D-13), then make deploy.
    4. Single-machine pin (D-03, HOST-06): run fly scale count 1 once after the first deploy, verify with fly status and fly scale show that exactly one machine exists; warn explicitly that running fly scale count with any higher value or adding a second region silently fragments the in-memory rate-limit counters into per-machine limits and must never be done.
    5. Idle behavior and cost (D-01, D-02): auto-suspend freezes VM memory so DemoLimiter counters survive quiet periods; counters reset only on deploys or crashes (accepted); shared-cpu-1x with 256MB and suspend-on-idle is expected to cost under 1-2 USD per month with a ceiling of about 5 USD.
    6. Verifying the deploy: curl -sI http://<app-hostname>/mcp should redirect to HTTPS (force_https); a JSON-RPC initialize POST to https://<app-hostname>/mcp with Accept "application/json, text/event-stream" and Content-Type application/json should return a non-421 response.
    7. Teardown: fly apps destroy <app-name>.
    Also document the MCP_PUBLIC_HOSTNAME environment variable here (what it is, that fly.toml [env] supplies it in production, that the server refuses non-loopback binds without it) so the variable is documented even if .env.example could not be edited in plan 13-01.
  </action>
  <verify>
    <automated>grep -q '^deploy:' Makefile && grep -q 'deploy' <(head -1 Makefile) && grep -q 'fly scale count 1' docs/DEPLOY.md && grep -q 'fly secrets set EXA_API_KEY' docs/DEPLOY.md && grep -q 'fly apps destroy' docs/DEPLOY.md && grep -q 'MCP_PUBLIC_HOSTNAME' docs/DEPLOY.md && [ "$(grep -c '—' docs/DEPLOY.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `make -n deploy` prints a fly deploy invocation and nothing else
    - .PHONY line 1 of the Makefile includes deploy
    - docs/DEPLOY.md contains all seven sections listed in the action, including the fly scale count 1 step, the never-scale-above-1 warning, the cost expectation, and the teardown command
    - `grep -c '—' docs/DEPLOY.md` returns 0 (no em-dash characters in published markdown)
  </acceptance_criteria>
  <done>An operator can take the repo from zero to a deployed, pinned, suspend-on-idle single-machine app and back to teardown using only docs/DEPLOY.md and make deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public repo -> deploy artifacts | Dockerfile and fly.toml are committed publicly; any secret placed in them leaks |
| Fly platform -> container runtime | Secrets delivered as runtime env vars via fly secrets, never baked into layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Information Disclosure | Dockerfile / fly.toml / .dockerignore | high | mitigate | EXA_API_KEY only ever via fly secrets set (D-13); grep gate asserts neither committed artifact contains the key name; .dockerignore excludes .env and credentials.json from the build context; existing pre-commit public-discipline guard runs on both files |
| T-13-05 | Denial of Service (economic) | fly.toml machine scaling | high | mitigate | Single region, min_machines_running = 0, suspend-on-idle (D-01/D-02); DEPLOY.md documents fly scale count 1 and forbids higher counts (D-03, counter fragmentation) |
| T-13-06 | Denial of Service (self-inflicted) | Fly smoke/health checks vs MCP JSON-RPC endpoint | medium | mitigate | Smoke-checks flag confirmed in dry run and carried into make deploy; no [[http_service.checks]] block in first deploy (RESEARCH Pitfall 1, Open Question 2) |
</threat_model>

<verification>
- All Task 2 and Task 3 grep gates pass
- `git status` shows Dockerfile, .dockerignore, fly.toml, Makefile, docs/DEPLOY.md staged with no other tracked-file collateral
- Pre-commit hooks (public-discipline guard included) pass on the commit
</verification>

<success_criteria>
- Dry run findings recorded before artifacts were finalized (roadmap research flag honored)
- Deploy artifacts are committed, secret-free, and encode D-01/D-02/D-03/D-04/D-13 exactly
- docs/DEPLOY.md is a complete D-15 runbook a stranger-operator could follow
</success_criteria>

<output>
Create `.planning/phases/13-hosted-deploy-docs-close/13-02-SUMMARY.md` when done.
</output>

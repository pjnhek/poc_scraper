---
phase: 13-hosted-deploy-docs-close
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - tests/integration/test_mcp_error_sanitization.py
autonomous: true
requirements: [HOST-05, TEST-01]
must_haves:
  truths:
    - "A deliberately triggered internal failure returns isError true with a payload containing no stack trace, no env var name, and no key fragment"
    - "Provider failures and invalid domains likewise surface only sanitized plain messages"
    - "The full offline suite is green and strict mypy is clean on the merged Wave 1 changeset with no new overrides"
  artifacts:
    - path: tests/integration/test_mcp_error_sanitization.py
      provides: "cross-cutting banned-substring error-payload tests (HOST-05)"
      contains: "BANNED_SUBSTRINGS"
  key_links:
    - from: tests/integration/test_mcp_error_sanitization.py
      to: src/mcp_server/server.py
      via: "in-memory MCP client calling get_account_evidence against raising stubs; asserts the sanitized ValueError wording reaches the client and nothing else does"
      pattern: "get_account_evidence"
---

<objective>
Prove HOST-05 offline with a dedicated integration test file: deliberately trigger the three tool-error paths of `get_account_evidence` (invalid domain, provider failure, unexpected internal exception carrying poisoned secret-like content) and assert the client-visible payload never contains stack traces, env var names, or key fragments. Then run the full offline gate (TEST-01) across the merged Wave 1 changeset.

Purpose: the D-14 live checkpoint (plan 13-04) is a one-time gate; this file is the repeatable regression coverage the roadmap's success criterion 3 requires.

Output: new `tests/integration/test_mcp_error_sanitization.py`; green `make test`, `make typecheck`, `make lint`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-hosted-deploy-docs-close/13-CONTEXT.md
@.planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md
@src/mcp_server/server.py
@tests/functional/test_mcp_server.py
</context>

## Artifacts this phase produces (plan 03 slice)

- `tests/integration/test_mcp_error_sanitization.py` (new file)
- `BANNED_SUBSTRINGS` module constant in that file
- Test functions: `test_invalid_domain_error_is_sanitized`, `test_provider_failure_error_is_sanitized`, `test_unexpected_internal_error_is_sanitized`
- Local raising stub classes in that file (an Exa-shaped stub whose search methods raise; names at executor discretion, suggest `RaisingExa` and `PoisonedExa`)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: banned-substring error-payload integration tests (HOST-05)</name>
  <files>tests/integration/test_mcp_error_sanitization.py</files>
  <read_first>
    - src/mcp_server/server.py (get_account_evidence lines 61-107: the three error paths under test and their exact sanitized wordings)
    - tests/functional/test_mcp_server.py (the _lifespan_factory / FakeClock / _exa_about / _exa_news harness and the existing isError assertion style; reuse via import, do not reinvent)
    - tests/integration/test_pipeline_failures.py (integration-layer conventions: module docstring, stub naming, annotations)
    - .planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md (test_mcp_error_sanitization.py section carries the drafted shape)
  </read_first>
  <behavior>
    - BANNED_SUBSTRINGS is a module-level tuple containing at least: "Traceback", "EXA_API_KEY", "DEEPSEEK_API_KEY", "NVIDIA_API_KEY", "BROWSERBASE_API_KEY", "site-packages", and the poisoned sentinel "sk-test-secret-fragment"
    - test_invalid_domain_error_is_sanitized: call get_account_evidence with domain "not-a-domain" via create_connected_server_and_client_session over a build_server built with _lifespan_factory and a normal FakeExa; assert result.isError is True and no banned substring appears in result.content[0].text
    - test_provider_failure_error_is_sanitized: an Exa-shaped stub whose search_about and search_news raise httpx.HTTPError("boom"); assert isError is True, the text contains "retrieval unavailable, try again", and no banned substring appears
    - test_unexpected_internal_error_is_sanitized: an Exa-shaped stub whose search_about raises RuntimeError with a message deliberately containing "EXA_API_KEY=sk-test-secret-fragment" and a fake traceback-like line; assert isError is True, the text contains "internal error, try again", and no banned substring appears (proves the catch-all path drops the poisoned message rather than echoing it)
  </behavior>
  <action>
    Create tests/integration/test_mcp_error_sanitization.py. Module docstring: HOST-05 regression coverage, cross-cutting banned-substring assertion over the tool error paths, offline counterpart to the phase's live D-14 check.

    Import the shared harness from tests.functional.test_mcp_server (_lifespan_factory and the fixture helpers) and create_connected_server_and_client_session from mcp.shared.memory, mirroring the import pattern at the top of tests/functional/test_mcp_http_transport.py. Build local Exa-shaped stubs (structural ExaLike: async search_about and search_news methods matching the FakeExa signatures in tests/functional/test_enrich.py) that raise per the behavior block. Every test builds its server via build_server(lifespan=_lifespan_factory(stub)) with no settings kwarg (stdio-shape in-memory session; the sanitization code is transport-independent).

    Assert banned substrings with a loop over BANNED_SUBSTRINGS against result.content[0].text for each scenario. Keep all functions annotated -> None and pass mypy strict style; pytest-asyncio auto mode means no asyncio marks are needed.

    Note on RED: the invalid-domain and provider-failure wordings already ship, so those tests should pass immediately; the value is the banned-substring net plus the poisoned-internal-error case. Run the file once before writing assertions loosely: if any assertion fails, that is a real HOST-05 gap to fix in src/mcp_server/server.py (report it, do not weaken the test).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_mcp_error_sanitization.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_mcp_error_sanitization.py -q` passes with 3 tests
    - The file defines BANNED_SUBSTRINGS including the sentinel "sk-test-secret-fragment" and each test loops over it against the client-visible payload text
    - The poisoned-internal-error test's stub message contains "sk-test-secret-fragment" and the asserted client payload does not
  </acceptance_criteria>
  <done>Repeatable offline proof exists that tool error payloads never leak stack traces, env names, or key fragments across all three error paths of the public tool surface.</done>
</task>

<task type="auto">
  <name>Task 2: full offline phase gate on the merged Wave 1 changeset (TEST-01)</name>
  <files>tests/integration/test_mcp_error_sanitization.py</files>
  <read_first>
    - .planning/phases/13-hosted-deploy-docs-close/13-RESEARCH.md (Validation Architecture section: sampling commands and the TEST-01 requirement-to-test map)
  </read_first>
  <action>
    Run the three project gates against the merged Wave 1 plus Task 1 changeset: make test (full offline suite, smoke excluded), make typecheck (uv run mypy src evals, strict), make lint (ruff check plus black --check). Fix any cross-plan integration fallout revealed here (for example import collisions between plans 13-01 and 13-02 merges, formatting drift in new files). If a failure traces to production code shipped by an earlier phase and is unrelated to this phase's changes, stop and report it in the SUMMARY as a blocking discovery rather than patching unrelated code silently.

    Confirm no new mypy overrides: the [tool.mypy] section of pyproject.toml must be byte-identical to its state before Phase 13 (the only allowed override remains the Google API ignore_missing_imports block).
  </action>
  <verify>
    <automated>make test && make typecheck && make lint && ! grep -q 'ignore_errors' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 (full offline suite green, smoke skipped)
    - `make typecheck` exits 0
    - `make lint` exits 0
    - The [tool.mypy] block in pyproject.toml is unchanged this phase (no new overrides, TEST-01)
  </acceptance_criteria>
  <done>TEST-01's offline half is closed: unit, functional (in-memory MCP client), and integration coverage over tiering, limits with the injected clock, evidence construction, and error paths is green in one run, with strict mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server internals -> MCP client payload | Exception content crosses from trusted process internals to untrusted clients; sanitization is the control under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Information Disclosure | get_account_evidence error paths | high | mitigate | Banned-substring integration tests over all three error paths including a poisoned internal exception; existing sanitized-ValueError-from-None pattern is the mechanism, this plan adds the regression net |
| T-13-08 | Repudiation | Server-side logging of real errors | low | accept | Full detail (exc_info) stays on stderr logs for the operator; only sanitized text crosses the boundary; already the shipped Phase 10-12 posture |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_mcp_error_sanitization.py -q` green
- `make test`, `make typecheck`, `make lint` all exit 0
</verification>

<success_criteria>
- HOST-05's offline verification exists as a dedicated, repeatable test file
- TEST-01 gate passes across the merged phase changeset with no new mypy overrides
</success_criteria>

<output>
Create `.planning/phases/13-hosted-deploy-docs-close/13-03-SUMMARY.md` when done.
</output>

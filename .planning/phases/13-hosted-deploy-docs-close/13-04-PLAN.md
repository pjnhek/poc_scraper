---
phase: 13-hosted-deploy-docs-close
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - docs/DEPLOY.md
autonomous: false
requirements: [HOST-03, HOST-05, HOST-06]
user_setup:
  - service: fly.io
    why: "Live deploy target; the EXA_API_KEY secret value is unreadable to the executor by permission policy, so the operator must set it"
    env_vars:
      - name: EXA_API_KEY
        source: "Operator's own .env (or exa.ai dashboard); set via: fly secrets set EXA_API_KEY=<value>"
    dashboard_config:
      - task: "Authenticated fly CLI session (fly auth login)"
        location: "Local terminal"
must_haves:
  truths:
    - "The app is deployed and reachable at the public Fly URL over HTTPS"
    - "Exactly one machine exists after the deploy (fly status / fly scale show)"
    - "A JSON-RPC initialize POST to the public URL with the Fly hostname as Host is not rejected with 421"
    - "A real MCP client retrieved cited evidence for one domain through the hosted URL with zero setup"
    - "A deliberately triggered invalid-domain failure over the live URL returned a sanitized error payload"
  artifacts:
    - path: docs/DEPLOY.md
      provides: "confirmed (not guessed) deploy command sequence after the real deploy"
  key_links:
    - from: fly.toml
      to: "https://<app-name>.fly.dev/mcp"
      via: "fly deploy ships the Dockerfile image; Fly edge terminates TLS and forwards the client Host header to the allowlist added in 13-01"
      pattern: "MCP_PUBLIC_HOSTNAME"
---

<objective>
Deploy the demo-mode MCP server to Fly.io and close HOST-03 and the live halves of HOST-05/HOST-06: operator sets the EXA_API_KEY secret (D-13), the executor runs the deploy, pins the machine count to exactly one (D-03), verifies transport acceptance live (falsifying RESEARCH assumption A1), and a human performs the D-14 real-client checkpoint (retrieve cited evidence, then trigger and inspect a failure payload).

Purpose: this is the milestone's public artifact; everything before it is preparation.

Output: live app at the confirmed public URL; docs/DEPLOY.md updated with the confirmed command sequence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-hosted-deploy-docs-close/13-CONTEXT.md
@.planning/phases/13-hosted-deploy-docs-close/13-RESEARCH.md
@.planning/phases/13-hosted-deploy-docs-close/13-02-SUMMARY.md
@docs/DEPLOY.md
@fly.toml
</context>

## Artifacts this phase produces (plan 04 slice)

- Live Fly.io app (external artifact, not a repo file): the confirmed app at `https://<app-name>.fly.dev/mcp`
- Fly secret `EXA_API_KEY` on that app (operator-set, never committed)
- Confirmed-command updates to `docs/DEPLOY.md` (only if the real sequence deviated from the plan 13-02 draft)

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: operator sets the EXA_API_KEY Fly secret (D-13)</name>
  <action>Pause and present the steps below. The executor cannot read the operator's .env by permission policy, so the secret value must come from the operator's own terminal. Before pausing, print the app name read from fly.toml so the operator can paste it into the commands.</action>
  <what-built>Deploy artifacts (Dockerfile, fly.toml, make deploy) are committed and the Fly app exists from the plan 13-02 dry run. The hosted server needs exactly one secret before its first deploy: EXA_API_KEY (Settings.mcp_tier raises at startup without it, so deploying first would crash-loop).</what-built>
  <how-to-verify>
    1. Confirm the CLI is authenticated: fly auth whoami
    2. Set the secret (value from your local .env or the exa.ai dashboard; the executor cannot read it by permission policy): fly secrets set EXA_API_KEY=<your-exa-key> --app <app-name from fly.toml>
    3. Confirm it is staged: fly secrets list --app <app-name> shows EXA_API_KEY (digest only, no value)
  </how-to-verify>
  <resume-signal>Type "secret set" when fly secrets list shows EXA_API_KEY</resume-signal>
</task>

<task type="auto">
  <name>Task 2: deploy, pin to one machine, and verify the live transport (D-03, D-13, HOST-06)</name>
  <files>docs/DEPLOY.md</files>
  <read_first>
    - docs/DEPLOY.md (the drafted runbook and dry-run findings, including the confirmed app name and smoke-checks flag)
    - fly.toml (the app name and internal_port this deploy ships)
    - .planning/phases/13-hosted-deploy-docs-close/13-RESEARCH.md (Assumptions Log A1-A3; this task is where A1 is falsified or confirmed live)
  </read_first>
  <action>
    Preflight: if the plan 13-02 dry run ran in degraded mode (no app created), create the app now with fly launch --no-deploy using the D-04 name rules, reconcile the app name and MCP_PUBLIC_HOSTNAME value in fly.toml if the name changed, and ask the operator to re-run the secret step for the new app before continuing.

    Deploy: run make deploy (which wraps fly deploy with the confirmed smoke-checks flag). If the deploy fails on smoke or health checking despite the flag, consult fly deploy --help for the current flag spelling, fix the Makefile target, and record the correction in docs/DEPLOY.md.

    Pin: run fly scale count 1 --yes (D-03), then verify with fly status and fly scale show that exactly one machine exists in one region. If more than one machine exists, scale down before proceeding; the in-memory rate-limit counters are only global on exactly one machine (HOST-06).

    Live transport verification (burns zero Exa quota; initialize is not a tool call):
    1. curl -sI http://<app-hostname>/mcp and confirm an HTTPS redirect status (force_https working, RESEARCH Pitfall 5).
    2. POST a JSON-RPC initialize body (jsonrpc 2.0, method initialize, protocolVersion, capabilities, clientInfo, same shape as the functional tests) to https://<app-hostname>/mcp with headers Accept "application/json, text/event-stream" and Content-Type "application/json"; confirm the response status is not 421 and not a 5xx. A 421 here means the Host header composition differs from RESEARCH assumption A1: log the actual Host expectation once via fly logs, fix the allowlist composition in src/mcp_server/server.py accordingly (never by wildcarding allowed_hosts or disabling DNS-rebinding protection), and redeploy.
    3. Check fly logs for the startup lines: the resolved tier line must say thin, and the demo-limits line must show the 5/25/5 defaults.

    Update docs/DEPLOY.md wherever the real sequence deviated from the draft (exact flag spellings, machine-count behavior on first deploy, region). Keep zero secret values out of the file and keep it free of em-dash characters.
  </action>
  <verify>
    <automated>APP=$(grep '^app' fly.toml | sed 's/.*= *"\(.*\)"/\1/'); curl -s -o /dev/null -w '%{http_code}' -X POST "https://$APP.fly.dev/mcp" -H 'Accept: application/json, text/event-stream' -H 'Content-Type: application/json' -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"deploy-verify","version":"0.1"}}}' | grep -vqx '421'</automated>
  </verify>
  <acceptance_criteria>
    - `fly status` output shows exactly one machine, state running or suspended, in one region
    - The initialize POST to the public URL returns a non-421, non-5xx status (A1 confirmed live)
    - `curl -sI http://<app-hostname>/mcp` returns a 3xx redirect to https
    - `fly logs` contains the tier-resolved line with "thin" and the demo-limits line
    - docs/DEPLOY.md matches the commands actually run (no guessed flags remain)
  </acceptance_criteria>
  <done>The hosted endpoint is live, HTTPS-only, single-machine, demo-tier, and accepts real-client Host headers; the runbook reflects reality.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-14 live real-client verification of HOST-03 and HOST-05</name>
  <action>Pause and present the verification steps below with the confirmed hostname substituted into every URL. Do not script this check (D-14 explicitly forbids a scripted live-smoke target); the human performs it in a real client.</action>
  <what-built>The hosted MCP server is live at https://<app-name>.fly.dev/mcp in demo mode (thin tier, rate limits on). Offline tests already cover the error paths; this is the one-time D-14 live gate for HOST-03 and HOST-05, expected to burn 1-2 of the day's 25 quota calls.</what-built>
  <how-to-verify>
    1. Connect a real client with zero setup, either:
       - Claude Code: claude mcp add --transport http poc-scraper https://<app-name>.fly.dev/mcp
       - or the bridge: npx mcp-remote https://<app-name>.fly.dev/mcp
    2. Call get_account_evidence for one real domain (for example notion.so). Confirm the result contains numbered justifications with citation URLs and a retrieval_status field (HOST-03: cited evidence, zero setup).
    3. Trigger a failure: call get_account_evidence with domain "not-a-domain". Inspect the full error payload text and confirm it is a plain one-line message with no stack trace, no env var names, no key fragments, and no file paths (HOST-05 live check).
    4. Optional: repeat the valid call 5+ times from the same network to see the rationing message with a reset time (HOST-04 already shipped; this just confirms it live).
  </how-to-verify>
  <resume-signal>Type "approved" if both checks pass, or describe exactly what the payload showed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> public Fly URL | Fully untrusted traffic reaches the demo endpoint; rate limits and DNS-rebinding allowlist are the only gates (by design, AR-12-05) |
| Operator terminal -> Fly secrets store | EXA_API_KEY crosses here once; never enters the repo or the executor's context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Information Disclosure | EXA_API_KEY handling during deploy | high | mitigate | Secret set only by the operator via fly secrets set (checkpoint task); executor never sees the value; fly secrets list shows digests only |
| T-13-10 | Denial of Service (economic) | Public unauthenticated endpoint spending Exa credits | high | mitigate | MCP_DEMO_MODE=1 baked into the image; live log check confirms thin tier and 5/25/5 limits before the phase closes; single-machine pin keeps caps truly global |
| T-13-11 | Spoofing | Live Host-header handling at the Fly edge | medium | mitigate | Live initialize POST confirms the allowlist accepts the real forwarded Host; any 421 fixed by correcting entry composition, never by wildcarding or disabling protection |
| T-13-12 | Information Disclosure | Live tool error payloads | high | mitigate | D-14 checkpoint: human triggers an invalid-domain failure and inspects the payload for leaks before approving |
</threat_model>

<verification>
- Automated initialize POST gate passes against the live URL
- Human checkpoint approved with cited evidence retrieved and a clean failure payload
- `fly status` single-machine evidence recorded in the SUMMARY
</verification>

<success_criteria>
- HOST-03 satisfied: a stranger-equivalent client connected with zero setup and retrieved cited evidence
- HOST-05 live half satisfied: deliberately triggered failure leaked nothing
- HOST-06 operational half satisfied: exactly one machine, hostname allowlist working against real edge-forwarded headers
</success_criteria>

<output>
Create `.planning/phases/13-hosted-deploy-docs-close/13-04-SUMMARY.md` when done.
</output>

---
phase: 13-hosted-deploy-docs-close
plan: 05
type: execute
wave: 3
depends_on: ["13-04"]
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - "A hiring reader hits a Try it live hook with the working hosted URL near the top of the README"
    - "The README MCP section carries three client config snippets, the grounding-by-instruction vs grounding-by-construction contrast, and a BYOK full-tier subsection"
    - "CLAUDE.md's locked stack lists mcp>=1.28,<2.0 and its scope, file layout, and failure modes reflect the MCP surface"
    - "The pinned Loom keeps its commit pin and gains a one-line scope note"
  artifacts:
    - path: README.md
      provides: "Try it live hook, MCP section with snippets, grounding contrast, BYOK subsection, DEPLOY.md link, Loom scope note"
      contains: "mcp-remote"
    - path: CLAUDE.md
      provides: "charter sync per D-11: stack pin, scope note, src/mcp_server layout, Makefile targets, MCP failure modes"
      contains: "mcp>=1.28,<2.0"
  key_links:
    - from: README.md
      to: docs/DEPLOY.md
      via: "MCP section links to the operator runbook"
      pattern: "docs/DEPLOY.md"
    - from: README.md
      to: "https://<app-name>.fly.dev/mcp"
      via: "Try it live hook and all three client snippets use the live URL confirmed in plan 13-04"
      pattern: "fly.dev"
---

<objective>
Close the milestone's documentation surface: README gains the D-08 "Try it live" hook and MCP section (three client snippets, D-10 grounding contrast as centerpiece, D-09 BYOK subsection, link to docs/DEPLOY.md) plus the D-12 Loom scope note (DOCS-02); CLAUDE.md gets the D-11 full charter sync (DOCS-01).

Purpose: the working URL beats the video for a hiring reader (CONTEXT specifics), and a stale CLAUDE.md misleads every future agent session.

Output: updated README.md and CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-hosted-deploy-docs-close/13-CONTEXT.md
@.planning/phases/13-hosted-deploy-docs-close/13-RESEARCH.md
@.planning/phases/13-hosted-deploy-docs-close/13-04-SUMMARY.md
@README.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 05 slice)

- README.md sections: "Try it live" hook (near top), "MCP server" section containing "Connect a client" snippets, "Grounding by instruction vs grounding by construction" contrast, "Full tier (BYOK)" subsection, docs/DEPLOY.md link, Loom scope note line under the Demo section
- CLAUDE.md additions: `mcp>=1.28,<2.0` stack line, MCP-surface scope sentence, `src/mcp_server/` file-layout entries (`evidence.py`, `limits.py`, `server.py`, `wiring.py`, `__main__.py`), Makefile target mentions (`mcp`, `mcp-http`, `mcp-demo`, `smoke-mcp`, `deploy`), MCP failure-mode bullets (rationing, fail-closed client IP, sanitized tool errors)

<tasks>

<task type="auto">
  <name>Task 1: README Try-it-live hook, MCP section, and Loom scope note (D-08, D-09, D-10, D-12)</name>
  <files>README.md</files>
  <read_first>
    - README.md (whole file: current section order, heading level conventions, voice; the hook lands between the Proof bullet block and the Demo section, the MCP section lands after "Run it")
    - .planning/phases/13-hosted-deploy-docs-close/13-RESEARCH.md (Code Examples section: the three verified client-config snippets to copy, including the note on Claude Desktop native-url vs mcp-remote portability and Open Question 3's lead-with-mcp-remote recommendation)
    - .planning/phases/13-hosted-deploy-docs-close/13-04-SUMMARY.md (the confirmed live app hostname; use it verbatim in every URL)
    - src/mcp_server/server.py (research_account prompt text and research_account_full docstring: source material for describing what each tier actually returns; do not paraphrase beyond what the code does)
  </read_first>
  <action>
    All edits scoped and additive; do not restructure existing sections. Use the confirmed hostname from 13-04-SUMMARY everywhere a URL appears (placeholder below: the confirmed app URL). No em-dash characters anywhere in this file; no emojis.

    1. Try it live hook (D-08), placed directly after the Proof bullet / hero image block and before the Demo section: a short section (heading "Try it live") stating that the pipeline's evidence retrieval is hosted as a public MCP server anyone can hit with zero setup, giving the one-liner npx mcp-remote <confirmed-url>/mcp, noting it is demo-tier (rate-limited, evidence-only) and pointing to the MCP section below for full client configs.

    2. Demo section (D-12): keep the Loom embed and its commit pin untouched; add one line after the existing pin note saying the video covers the v1.0 pipeline and the MCP server surface is newer than the recording.

    3. New "MCP server" section after "Run it" (before "Eval"), containing in order:
       a. One-paragraph framing: the same grounded retrieval the pipeline uses, exposed over MCP; hosted demo is thin-tier and rationed (5 calls/IP/hour, 25/day global), full tier is BYOK via stdio.
       b. The D-10 grounding contrast as the centerpiece (subheading), sourced from the design intent: the thin tier practices grounding by instruction (the research_account prompt and the get_account_evidence tool instruct the calling agent to cite [N] justification indices and refuse to fabricate), while the full tier is grounding by construction (the pipeline itself validates citations in code and drops unciteable claims before anything ships). Write 2-3 sentences per side, concrete, naming the actual tool/prompt/resource identifiers: get_account_evidence, research_account, icp://rubric, icp://eval-report, research_account_full.
       c. "Connect a client" with the three snippets from RESEARCH Code Examples, in this order: npx mcp-remote <confirmed-url>/mcp (lead, most portable), Claude Code (claude mcp add --transport http poc-scraper <confirmed-url>/mcp), Claude Desktop JSON (mcpServers block using command npx with args mcp-remote and the URL, plus a one-line aside that clients with native remote-HTTP support can use a url field directly).
       d. "Full tier (BYOK)" subsection (D-09): clone the repo, make mcp over stdio; listing which keys unlock the full tier (a writer/judge provider key, DEEPSEEK_API_KEY or NVIDIA_API_KEY, plus BROWSERBASE_API_KEY and BROWSERBASE_PROJECT_ID on top of EXA_API_KEY); stating what research_account_full(domain, run_eval) returns: the complete grounded ScoredAccount JSON including the ICP score breakdown, personas, cited outreach hooks, and the AccountStatus honesty field.
       e. A closing line linking operators to docs/DEPLOY.md for hosting their own instance.

    4. Optionally extend the Tests table's functional row wording to mention the in-memory MCP client only if it reads naturally; skip otherwise (not a requirement).
  </action>
  <verify>
    <automated>grep -q "Try it live" README.md && grep -q "mcp-remote" README.md && grep -q "claude mcp add --transport http" README.md && grep -q "mcpServers" README.md && grep -qi "grounding by construction" README.md && grep -q "docs/DEPLOY.md" README.md && grep -q "research_account_full" README.md && [ "$(grep -c '—' README.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - README contains a "Try it live" heading positioned before the "Demo" heading (grep -n confirms line order)
    - All three client snippets are present and every URL in them uses the confirmed live hostname (grep for "fly.dev" returns at least 3 matches)
    - The MCP section names get_account_evidence, research_account, and research_account_full, and contrasts grounding by instruction with grounding by construction
    - The BYOK subsection lists the exact env var names DEEPSEEK_API_KEY, NVIDIA_API_KEY, BROWSERBASE_API_KEY, BROWSERBASE_PROJECT_ID
    - The Demo section retains the existing commit-pin line and gains the one-line v1.0 scope note
    - `grep -c '—' README.md` returns 0
  </acceptance_criteria>
  <done>DOCS-02 satisfied: a reader can go from the README to a working MCP connection in one copy-paste, understands the two grounding models, and can find the BYOK path and the operator runbook.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md charter sync (D-11)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (whole file; edit only the hand-authored sections: "What this project is", "Stack (locked, don't drift)", "File layout", "Failure modes (first-class, don't let them slide)"; never touch the GSD-managed sections such as Project, Technology Stack, Conventions, Architecture, GSD Workflow Enforcement, Developer Profile)
    - .planning/phases/13-hosted-deploy-docs-close/13-PATTERNS.md (CLAUDE.md section lists the exact additive edits)
  </read_first>
  <action>
    Additive edits only, in the hand-authored sections:

    1. "Stack (locked, don't drift)": add a bullet locking mcp>=1.28,<2.0 as the MCP server SDK (FastMCP, stdio plus streamable HTTP transports).
    2. "What this project is": add a sentence noting the pipeline is also exposed as an MCP server, a hosted rationed thin tier (evidence retrieval only) plus a BYOK full tier over stdio, with the rubric and eval report served as MCP resources.
    3. "File layout" tree: add src/mcp_server/ with evidence.py, limits.py, server.py, wiring.py, __main__.py (one-line purpose comments in the existing tree style); also add Dockerfile, fly.toml, and docs/DEPLOY.md entries at their tree positions.
    4. "Failure modes": add three bullets in the existing one-bullet-per-mode style: rationing (demo limits return structured refusals with reset times, and Exa credit exhaustion masquerades as the daily cap so the public URL looks rationed, never broken), client IP resolution (Fly-Client-IP only, fails closed into one shared bucket, no X-Forwarded-For), sanitized tool errors (every tool failure surfaces as an isError result with a plain message, never a stack trace, env name, or key fragment).
    5. Mention the new Makefile targets where the file discusses running things (the "Testing strategy" section's make run/make smoke paragraph is the natural place): mcp, mcp-http, mcp-demo, smoke-mcp, deploy.
    Keep new text free of em-dash characters and emojis. Do not reflow or rewrite unrelated lines (small reviewable diff norm).
  </action>
  <verify>
    <automated>grep -q 'mcp>=1.28,<2.0' CLAUDE.md && grep -q 'src/mcp_server' CLAUDE.md && grep -q 'Fly-Client-IP' CLAUDE.md && grep -q 'mcp-demo' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The "Stack (locked, don't drift)" section contains the literal mcp>=1.28,<2.0
    - The File layout tree lists src/mcp_server/ with all five module files plus Dockerfile, fly.toml, and docs/DEPLOY.md
    - The Failure modes section has the three new MCP bullets (rationing, fail-closed IP, sanitized errors)
    - git diff CLAUDE.md shows only additive hunks in hand-authored sections (no GSD-managed section touched)
  </acceptance_criteria>
  <done>DOCS-01 satisfied and the D-11 full charter sync is complete: future agent sessions see the MCP surface, its files, its targets, and its failure modes as first-class charter content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public repo README -> the internet | Published markdown; must not leak secrets or violate public-repo discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-13 | Information Disclosure | README/CLAUDE.md edits | medium | mitigate | Docs contain only the public hostname and public commands; no keys, no vendor names (seller stays abstract per public-repo discipline); pre-commit public-discipline guard runs on the commit |
| T-13-14 | Spoofing (social) | Published client snippets | low | accept | Snippets point only at the project's own confirmed fly.dev hostname; no third-party URLs beyond npx mcp-remote, already legitimacy-audited in RESEARCH |
</threat_model>

<verification>
- Both grep gates pass; `make lint` unaffected (markdown only)
- Pre-commit public-discipline guard passes on the commit
</verification>

<success_criteria>
- DOCS-01 and DOCS-02 fully satisfied per D-08 through D-12
- README URLs match the live deployment exactly
</success_criteria>

<output>
Create `.planning/phases/13-hosted-deploy-docs-close/13-05-SUMMARY.md` when done.
</output>

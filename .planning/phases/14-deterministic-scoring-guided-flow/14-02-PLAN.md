---
phase: 14-deterministic-scoring-guided-flow
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/enrich.py
  - src/mcp_server/evidence.py
  - src/mcp_server/server.py
  - tests/unit/test_evidence.py
  - tests/functional/test_mcp_server.py
autonomous: true
requirements: [EVID-01, PROMPT-01, TEST-04]

must_haves:
  truths:
    - "Passing news_days=1000 to get_account_evidence results in exa.search_news receiving days=365; news_days=6 results in days=7; the call still succeeds (silent clamp)"
    - "Omitting news_days preserves the existing 90-day default all the way to exa.search_news"
    - "The research_account prompt walks the agent through a numbered sequence ending in a score_account call and verdict presentation, then personas and hooks"
    - "The prompt forbids calling score_account when retrieval_status is empty and instructs reporting the account as unscoreable"
    - "The full pipeline path (Enricher.enrich) is behaviorally unchanged: it never passes days and keeps the 90-day default"
  artifacts:
    - path: "src/mcp_server/evidence.py"
      provides: "clamp_news_days plus NEWS_DAYS_MIN/MAX/DEFAULT constants and news_days threading in build_evidence_pack"
      exports: ["clamp_news_days", "NEWS_DAYS_MIN", "NEWS_DAYS_MAX", "NEWS_DAYS_DEFAULT"]
    - path: "src/enrich.py"
      provides: "collect_context keyword-only days parameter forwarded to exa.search_news"
      contains: "days: int = 90"
    - path: "src/mcp_server/server.py"
      provides: "news_days parameter on get_account_evidence and the rewritten research_account guided-flow prompt"
      contains: "news_days"
  key_links:
    - from: "src/mcp_server/server.py"
      to: "src/mcp_server/evidence.py"
      via: "get_account_evidence forwards news_days into build_evidence_pack"
      pattern: "news_days=news_days"
    - from: "src/mcp_server/evidence.py"
      to: "src/enrich.py"
      via: "build_evidence_pack passes days=clamp_news_days(news_days) into collect_context"
      pattern: "clamp_news_days"
    - from: "src/enrich.py"
      to: "ExaLike.search_news"
      via: "collect_context passes days kwarg through"
      pattern: "search_news\\(account\\.domain, days=days\\)"
---

<objective>
Thread a clamped `news_days` tuning parameter from the MCP boundary down to `ExaClient.search_news` (EVID-01, TEST-04), and rewrite the `research_account` prompt as the explicit numbered guided flow that orchestrates evidence retrieval, rubric reading, agent-side axis scoring with [N] citations, the `score_account` call, verdict presentation, and personas/hooks (PROMPT-01).

Implements locked decisions D-08 (numbered step sequence), D-09 (personas and hooks stay as closing steps), D-10 (empty-evidence skip rule mirroring the pipeline's unscoreable semantics), D-11 (news_days one-line aside, not a numbered step), D-12 (clamp 7-365, default 90), D-13 (silent clamp, range documented in the tool text, no new EvidencePack field).

Purpose: the guided flow is what makes the new scoring capability discoverable and correctly used by connecting agents; news_days makes recency tunable without changing the default behavior anywhere.
Output: modified src/enrich.py, src/mcp_server/evidence.py, src/mcp_server/server.py; extended tests/unit/test_evidence.py and tests/functional/test_mcp_server.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-CONTEXT.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-RESEARCH.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-PATTERNS.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-01-SUMMARY.md
@src/mcp_server/server.py
@src/mcp_server/evidence.py
@src/enrich.py
</context>

## Artifacts this phase produces (plan 02 portion)

- `clamp_news_days(value: int | None) -> int` — new function in `src/mcp_server/evidence.py`
- `NEWS_DAYS_MIN`, `NEWS_DAYS_MAX`, `NEWS_DAYS_DEFAULT` — new module constants in `src/mcp_server/evidence.py` (7, 365, 90)
- `news_days: int | None = None` — new parameter on `build_evidence_pack` (`src/mcp_server/evidence.py`) and on the `get_account_evidence` tool (`src/mcp_server/server.py`)
- `days: int = 90` — new keyword-only parameter on `collect_context` (`src/enrich.py`)
- Rewritten `research_account` prompt body in `src/mcp_server/server.py` (signature unchanged: `(domain: str) -> str`)
- `RecordingExa.news_days_received` — new recorded attribute on the existing `RecordingExa` fake in `tests/functional/test_mcp_server.py`
- New unit tests for `clamp_news_days` in `tests/unit/test_evidence.py`; new functional tests for news_days threading and the rewritten prompt in `tests/functional/test_mcp_server.py`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread clamped news_days from get_account_evidence to exa.search_news, with boundary tests</name>
  <files>src/enrich.py, src/mcp_server/evidence.py, src/mcp_server/server.py, tests/unit/test_evidence.py, tests/functional/test_mcp_server.py</files>
  <read_first>
    - .planning/phases/14-deterministic-scoring-guided-flow/14-RESEARCH.md (Common Pitfalls #2: the exact four-hop threading chain and why two hops currently drop the value; Code Examples: proposed clamp_news_days and signature changes, adopt as written)
    - src/enrich.py (collect_context at lines 34-56 currently calls exa.search_news(account.domain) with no days kwarg; Enricher.enrich at line 83 calls collect_context and must remain untouched)
    - src/mcp_server/evidence.py (build_evidence_pack at lines 164-168; the existing *_MCP_CAP constants block at lines 7-13 is where the NEWS_DAYS_* constants belong)
    - src/mcp_server/server.py (get_account_evidence at lines 61-107: signature, docstring style, the build_evidence_pack call at line 90)
    - src/mcp_server/wiring.py (DemoClampedExa.search_news already forwards days to the inner client; no wiring.py change is needed, do not modify it)
    - tests/functional/test_mcp_server.py (RecordingExa at lines 68-83 records num_results; extend the same way for days; _lifespan_factory at lines 29-37)
    - tests/unit/test_evidence.py (import block at lines 1-13; add clamp_news_days tests in this file's existing style)
  </read_first>
  <behavior>
    - clamp_news_days(None) == 90; clamp_news_days(6) == 7; clamp_news_days(7) == 7; clamp_news_days(90) == 90; clamp_news_days(365) == 365; clamp_news_days(366) == 365; clamp_news_days(1000) == 365; clamp_news_days(-5) == 7
    - call_tool("get_account_evidence", {"domain": "notion.so", "news_days": 1000}) succeeds (isError False) and the RecordingExa-style fake observed days == 365
    - news_days 6 -> observed days 7; news_days 30 -> observed days 30 (in-range passthrough); omitted news_days -> observed days 90
    - No new field appears on EvidencePack (D-13); src/models.py is untouched
  </behavior>
  <action>
    Three-hop threading per RESEARCH Pitfall 2, adopting the Code Examples signatures verbatim. In src/enrich.py change collect_context's signature to `collect_context(account: Account, *, exa: ExaLike, browserbase: BrowserbaseLike, days: int = 90) -> RawContext` and change the news call to `exa.search_news(account.domain, days=days)`. Leave Enricher.enrich's call site exactly as is (it passes no days, so the pipeline keeps the 90-day default; D-13 means no pipeline-wide behavior change). ExaClient.search_news, the ExaLike protocol, DemoClampedExa, and the FakeExa/RecordingExa fakes already accept days; do not modify src/clients/ or src/mcp_server/wiring.py.

    In src/mcp_server/evidence.py add, alongside the existing MCP-cap constants, `NEWS_DAYS_MIN = 7`, `NEWS_DAYS_MAX = 365`, `NEWS_DAYS_DEFAULT = 90`, and `clamp_news_days(value: int | None) -> int` returning NEWS_DAYS_DEFAULT for None and otherwise `max(NEWS_DAYS_MIN, min(NEWS_DAYS_MAX, value))` (D-12; matches the DemoClampedExa clamp idiom, no new abstraction). Change build_evidence_pack's signature to add keyword-only `news_days: int | None = None` and pass `days=clamp_news_days(news_days)` into collect_context.

    In src/mcp_server/server.py add `news_days: int | None = None` to get_account_evidence's signature (after ctx; FastMCP resolves ctx by annotation so ordering is safe) and forward `news_days=news_days` in the build_evidence_pack call. Extend the docstring with one sentence documenting the parameter: tunes the news lookback window in days, clamped silently to 7-365, default 90 (D-13: the clamp is silent, out-of-range calls proceed).

    Tests. In tests/unit/test_evidence.py import clamp_news_days plus the three constants and add boundary tests covering all eight clamp cases in behavior. In tests/functional/test_mcp_server.py extend RecordingExa's search_news to also record the received days into a `news_days_received: int | None` attribute (mirroring how it records num_results), then add functional tests calling get_account_evidence through the in-memory client with news_days 1000, 6, 30, and omitted, asserting the recorded days value (365, 7, 30, 90 respectively) and isError False for each. Assert on the recorded kwarg, never on EvidencePack output shape, per RESEARCH Pitfall 2's warning that output-shape assertions pass even when threading is broken.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_evidence.py tests/functional/test_mcp_server.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_evidence.py -k clamp_news_days -q` collects at least 4 tests, all passing
    - `uv run pytest tests/functional/test_mcp_server.py -k news_days -q` collects at least 3 tests, all passing
    - `uv run python -c "from src.mcp_server.evidence import clamp_news_days; print(clamp_news_days(None), clamp_news_days(1000), clamp_news_days(6))"` prints `90 365 7`
    - `grep -c "days=days" src/enrich.py` outputs 1 (collect_context forwards the kwarg to search_news)
    - `git diff --name-only` includes neither src/models.py nor src/mcp_server/wiring.py nor src/clients/protocols.py
    - `uv run pytest tests/functional/test_enrich.py -q` exits 0 (pipeline call sites unaffected by the default-90 keyword)
  </acceptance_criteria>
  <done>news_days flows get_account_evidence -> build_evidence_pack -> collect_context -> exa.search_news with silent 7-365 clamping and an untouched 90-day default on every pre-existing call site.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite research_account as the numbered guided flow, with prompt-content tests</name>
  <files>src/mcp_server/server.py, tests/functional/test_mcp_server.py</files>
  <read_first>
    - src/mcp_server/server.py (research_account at lines 136-149: current single-paragraph prose, f-string domain interpolation, return type str; keep the signature and registration untouched)
    - .planning/phases/14-deterministic-scoring-guided-flow/14-CONTEXT.md (D-08 through D-11: the exact six-step sequence, personas/hooks retained, empty-evidence skip rule, news_days one-line aside)
    - tests/functional/test_mcp_server.py (test_research_account_prompt_contains_required_elements at lines 572-592 with its required-substring list; test_research_account_prompt_never_mentions_full_tier_tool at lines 595-604; test_list_prompts_includes_research_account_with_required_domain_arg at lines 607-618)
  </read_first>
  <behavior>
    - get_prompt("research_account", {"domain": "notion.so"}) returns text containing all of the pre-existing required substrings: "icp://rubric", "get_account_evidence", "notion.so", "[N]", "drop", "fabricate"
    - The text additionally contains "score_account", "news_days", "unscoreable", and the six numbered step markers "1." through "6."
    - The text does not contain the full-tier tool name (existing test test_research_account_prompt_never_mentions_full_tier_tool stays green)
    - The prompt still registers with a required domain argument (existing list_prompts test stays green)
  </behavior>
  <action>
    Rewrite research_account's returned string (signature, docstring role, and `server.prompt()(research_account)` registration all unchanged) as an explicit numbered sequence per D-08, keeping the f-string interpolation of domain. Step content: 1. call get_account_evidence for the domain, with the one-line parenthetical aside that news_days can widen or narrow the news lookback when recency matters (D-11: an aside inside step 1 or immediately after it, never its own numbered step); 2. read the icp://rubric resource for axis definitions and weights; 3. score each rubric axis 1-5 yourself, citing an [N] justification index from the evidence for every axis judgment, dropping any claim without a matching index and never fabricating; 4. call score_account with the four integer scores, the per-axis reason strings carrying those [N] citations, and the domain; 5. present the returned verdict, weighted total, and per-axis breakdown to the user; 6. propose the top 3 buyer personas and draft one cited outreach hook per persona (D-09: personas and hooks stay as the closing step).

    Add the D-10 skip rule as an explicit instruction tied to step 1's result: if retrieval_status is "empty", the account cannot be researched; do NOT call score_account, report the account as unscoreable instead, and never fabricate. Word the rule so the literal strings "unscoreable", "drop", and "fabricate" appear in the final text (the first mirrors the pipeline's ScoredAccount semantics; the latter two keep the pre-existing required-substring test green). Do not name the full-tier tool anywhere in the text.

    Tests: extend test_research_account_prompt_contains_required_elements's required tuple (or add a sibling test in the same style) so the assertion set now also includes "score_account", "news_days", "unscoreable", and each of the six step markers "1." through "6.". Leave test_research_account_prompt_never_mentions_full_tier_tool and test_list_prompts_includes_research_account_with_required_domain_arg untouched; they must pass against the rewrite as-is.
  </action>
  <verify>
    <automated>uv run pytest tests/functional/test_mcp_server.py -k prompt -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/functional/test_mcp_server.py -k prompt -q` exits 0 with at least 3 tests collected
    - `uv run python -c "from src.mcp_server.server import research_account; t = research_account('notion.so'); assert all(s in t for s in ('icp://rubric', 'get_account_evidence', 'notion.so', '[N]', 'drop', 'fabricate', 'score_account', 'news_days', 'unscoreable', '1.', '6.')); print('ok')"` prints `ok`
    - `uv run pytest tests/functional/test_mcp_server.py tests/integration/test_mcp_error_sanitization.py -q` exits 0 (full functional + integration files green after both tasks)
    - `uv run mypy src` reports no errors in the three touched src files
  </acceptance_criteria>
  <done>The research_account prompt orchestrates the six-step guided flow end to end, carries the empty-evidence skip rule and the news_days aside, and all prompt-content tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> get_account_evidence news_days | Untrusted caller-supplied integer controls the Exa news query window |
| Prompt text -> connecting agent | Instructions cross to an agent the server cannot control (grounding-by-instruction) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Denial of Service | news_days -> ExaClient.search_news | high | mitigate | Server-side clamp_news_days to [7, 365] applied in build_evidence_pack before any external call, on every tier including full; asserted via recorded-kwarg tests |
| T-14-05 | Tampering (integrity of the honesty guarantee) | Agent fabricating scores on empty evidence | medium | mitigate | D-10 prompt-level skip rule (report unscoreable, never call score_account on retrieval_status empty); residual risk is inherent to grounding-by-instruction and is documented honestly in plan 14-03's DOCS-04 framing, never claimed as server-enforced |
| T-14-06 | Information Disclosure | SDK type-validation of a non-integer news_days | low | accept | Same SDK arg-model path already proven free of banned substrings by plan 14-01's integration tests; pydantic bounds its own input repr |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_evidence.py tests/functional/test_mcp_server.py tests/integration/test_mcp_error_sanitization.py -q` green
- `uv run mypy src` clean
- src/models.py, src/mcp_server/wiring.py, src/clients/ untouched
</verification>

<success_criteria>
- EVID-01: news_days threads through all four hops with silent 7-365 clamping and an unchanged 90-day default
- TEST-04: clamp boundaries and default covered at both the unit layer (clamp_news_days) and the MCP boundary (recorded days kwarg)
- PROMPT-01: the prompt orchestrates get_account_evidence, icp://rubric, agent-side 1-5 scoring with [N] citations, score_account, verdict presentation, then personas and hooks
</success_criteria>

<output>
Create `.planning/phases/14-deterministic-scoring-guided-flow/14-02-SUMMARY.md` when done
</output>

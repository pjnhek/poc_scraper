---
phase: 14-deterministic-scoring-guided-flow
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - README.md
  - deploy/oracle/setup.sh
autonomous: true
requirements: [DOCS-03, DOCS-04, TEST-03]

must_haves:
  truths:
    - "The README MCP section documents score_account with the hybrid framing: agent judgment is grounding-by-instruction, scoring math is grounding-by-construction"
    - "The README no longer describes the demo tier as scoring-free; both stale claims (top connect blurb and MCP section intro) are corrected"
    - "The Oracle landing-page heredoc mentions the agent-driven scoring capability alongside evidence retrieval"
    - "make test, make typecheck, make lint, and make verify-public-repo all exit 0 with no new mypy overrides"
  artifacts:
    - path: "README.md"
      provides: "hybrid-grounding documentation of score_account and the guided flow in the MCP section"
      contains: "score_account"
    - path: "deploy/oracle/setup.sh"
      provides: "landing-page index.html heredoc mentioning deterministic ICP scoring"
      contains: "score_account"
  key_links:
    - from: "README.md"
      to: "src/mcp_server/server.py"
      via: "documented tool name and behavior must match the shipped score_account tool"
      pattern: "score_account"
    - from: "deploy/oracle/setup.sh"
      to: "src/mcp_server/server.py"
      via: "landing page names the same two thin-tier tools the server registers"
      pattern: "score_account"
---

<objective>
Update the two public documentation surfaces for the new capability (DOCS-03, DOCS-04) with the honest hybrid-grounding framing, then run the full offline gate that closes TEST-03: entire offline test suite, strict mypy with no new overrides, ruff, black, and the public-repo discipline check.

The framing to convey (from REQUIREMENTS.md and D-03): the connecting agent's judgment stays grounding-by-instruction (it must cite [N] evidence per axis, and nothing server-side verifies those citations); the scoring math becomes grounding-by-construction (the server cannot return an arithmetically wrong total, and the response echoes the weights and verdict thresholds used, per D-06, so every score is reproducible by inspection).

Purpose: the docs are the demo's storefront; an inaccurate "evidence-only" claim would make the shipped capability invisible and the honesty framing hollow.
Output: updated README.md and deploy/oracle/setup.sh; green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-CONTEXT.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-RESEARCH.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-01-SUMMARY.md
@.planning/phases/14-deterministic-scoring-guided-flow/14-02-SUMMARY.md
@README.md
@deploy/oracle/setup.sh
</context>

## Artifacts this phase produces (plan 03 portion)

- No new code symbols. This plan produces documentation text only:
  - README.md: updated demo-tier blurb (near line 24), updated "MCP server" section intro (near line 154), extended "Grounding by instruction vs grounding by construction" section (lines 156-160) with the hybrid third paragraph documenting `score_account` and `news_days`
  - deploy/oracle/setup.sh: updated landing-page heredoc demo-tier paragraph (near line 183) and connected-client note (near line 208) mentioning `score_account`

<tasks>

<!-- planner-discipline-allow: evidence-only, no scoring -->
<!-- planner-discipline-allow: Evidence retrieval only -->

<task type="auto">
  <name>Task 1: Update README MCP section and Oracle landing-page heredoc with the hybrid framing</name>
  <files>README.md, deploy/oracle/setup.sh</files>
  <read_first>
    - README.md (line 24: the connect blurb currently claims the demo tier is "evidence-only, no scoring, no personas, no outreach hooks"; line 154: MCP section intro currently says "evidence retrieval only"; lines 156-160: the two-paragraph grounding-by-instruction vs grounding-by-construction section this task extends)
    - deploy/oracle/setup.sh (lines 146-217: the unquoted index.html heredoc; line 183: the demo-tier paragraph currently saying "Evidence retrieval only, rationed to 5 calls per IP per hour and 25 per day"; line 208: the note telling a connected client to call get_account_evidence)
    - src/mcp_server/server.py (the shipped score_account docstring and the rewritten research_account prompt, so documented behavior matches reality: integer 1-5 axes, echoed weights and thresholds, unrationed, news_days 7-365 default 90)
    - .planning/REQUIREMENTS.md (the "Milestone frame: Hybrid grounding" paragraph is the wording anchor for DOCS-04)
  </read_first>
  <action>
    README.md changes (DOCS-04). First, fix the two stale claims: rewrite the line-24 demo blurb so it stays honest, keeping the rate-limit numbers for get_account_evidence but stating the demo tier now also includes deterministic ICP scoring via score_account (unrationed, pure arithmetic), while personas and outreach hooks remain full-tier; rewrite the line-154 MCP section intro the same way ("evidence retrieval only" becomes evidence retrieval plus deterministic scoring). Second, extend the "Grounding by instruction vs grounding by construction" section with a third paragraph (or short subsection) documenting the hybrid: the thin tier's score_account takes the agent's four 1-5 integer axis scores (whose per-axis reasons are supposed to cite [N] evidence indices, a discipline the prompt instructs but the stateless server cannot verify, per D-03) and returns the weighted total and verdict computed server-side, echoing the axis weights and verdict thresholds used (D-06) so the result is reproducible by inspection; the judgment is grounding-by-instruction, the arithmetic is grounding-by-construction. Mention in the same section or the intro that get_account_evidence accepts an optional news_days parameter (clamped 7-365, default 90) to tune news recency. Keep the guided flow visible: one sentence noting the research_account prompt walks an agent through evidence, rubric, scoring, verdict, personas, and hooks. Style constraints: no em-dash characters anywhere in the additions (use commas or parentheses), no emojis, keep the existing section ordering.

    deploy/oracle/setup.sh changes (DOCS-03), editing only the index.html heredoc body: rewrite the demo-tier paragraph (line 183) so "Evidence retrieval only" becomes a statement that the demo serves cited evidence retrieval plus deterministic agent-driven ICP scoring (the agent scores each axis from the cited evidence, the server computes the weighted total and verdict), keeping the existing ration numbers for retrieval; update the connected-client note (line 208) to mention calling get_account_evidence with a domain and then score_account with the four 1-5 axis scores. Preserve the heredoc mechanics exactly: the heredoc is unquoted, so introduce no new dollar signs, backticks, or backslashes in the added prose (the existing ${CADDY_HOSTNAME} interpolations must keep working); keep HTML structure valid (plain p and code tags in the existing style). Scope note resolving RESEARCH Open Question 1: this task edits and commits the setup.sh source only; a live redeploy of the Oracle host via make deploy-oracle is an optional operator follow-up, not a phase gate, and must not be attempted by the executor.
  </action>
  <verify>
    <automated>grep -c "score_account" README.md deploy/oracle/setup.sh && bash -n deploy/oracle/setup.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "score_account" README.md` outputs at least 2; `grep -c "score_account" deploy/oracle/setup.sh` outputs at least 2
    - `grep -c "news_days" README.md` outputs at least 1
    - `grep -c "evidence-only, no scoring" README.md` outputs 0 and `grep -c "Evidence retrieval only" deploy/oracle/setup.sh` outputs 0 (both stale claims rewritten)
    - `grep -c $'—' README.md` outputs 0 (no em-dash characters, matching the project's published-markdown rule)
    - `bash -n deploy/oracle/setup.sh` exits 0 (heredoc and script still parse)
    - `grep -c 'CADDY_HOSTNAME' deploy/oracle/setup.sh` output is unchanged from before the edit (interpolations preserved)
  </acceptance_criteria>
  <done>Both public surfaces name score_account, describe the hybrid grounding split honestly, no longer claim the demo tier is scoring-free, and the deploy script still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full offline phase gate and confirm no new mypy overrides</name>
  <files></files>
  <read_first>
    - Makefile (test = pytest -m "not smoke"; typecheck = mypy src evals; lint = ruff check + black --check over src tests evals; verify-public-repo = python -m scripts.verify_public_repo)
    - pyproject.toml (the single existing [[tool.mypy.overrides]] block for the google API modules is the only override allowed to exist)
  </read_first>
  <action>
    Run the four gates in sequence: `make test`, `make typecheck`, `make lint`, `make verify-public-repo`. If lint reports auto-fixable formatting, run `make format` and re-run the gates. Fix any failure at its source (the phase's own new code or tests), never by loosening a gate: no new `[[tool.mypy.overrides]]` entries, no new per-file ruff ignores, no test deselection, no `# type: ignore` additions beyond what strict mypy genuinely requires with a specific error code (prefer fixing types over ignoring; ask via a blocker note if an ignore seems unavoidable). This closes TEST-03's gate clause: full offline suite plus strict mypy plus ruff plus black plus verify-public-repo, green, with no new mypy overrides.
  </action>
  <verify>
    <automated>make test && make typecheck && make lint && make verify-public-repo</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 (full offline suite, smoke excluded)
    - `make typecheck` exits 0
    - `make lint` exits 0
    - `make verify-public-repo` exits 0
    - `grep -c "tool.mypy.overrides" pyproject.toml` outputs 1 (only the pre-existing google-modules override block)
    - `git diff --name-only pyproject.toml` is empty (no tooling-config loosening)
  </acceptance_criteria>
  <done>All four offline gates green over the fully merged phase changeset with the mypy override set unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> reader | README and landing page are the public claims about what the server enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Repudiation (overclaiming) | README/landing-page framing | low | mitigate | Hybrid framing explicitly states citation discipline is instruction-level and not verified server-side (D-03); only the arithmetic is claimed as server-guaranteed |
| T-14-08 | Information Disclosure | Public docs additions | low | mitigate | make verify-public-repo (denylist guard) runs as part of this plan's gate before commit |
| T-14-SC | Tampering | package installs | low | accept | Zero new packages this phase; no install tasks exist |
</threat_model>

<verification>
- `grep score_account README.md deploy/oracle/setup.sh` shows hits in both files
- `make test && make typecheck && make lint && make verify-public-repo` all exit 0
- pyproject.toml unchanged
</verification>

<success_criteria>
- DOCS-03: the landing-page heredoc mentions agent-driven scoring alongside evidence retrieval and still parses
- DOCS-04: the README MCP section documents score_account with the honest hybrid framing and no stale evidence-only claims remain
- TEST-03 (gate clause): full offline suite, strict mypy (no new overrides), ruff, black, and verify-public-repo all green
</success_criteria>

<output>
Create `.planning/phases/14-deterministic-scoring-guided-flow/14-03-SUMMARY.md` when done
</output>

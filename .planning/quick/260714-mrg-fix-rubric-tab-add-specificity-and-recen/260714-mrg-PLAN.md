---
quick_id: 260714-mrg
type: quick
created: 2026-07-14
files_modified:
  - src/sheets.py
must_haves:
  truths:
    - "build_rubric_rows() renders all five judge dimensions (groundedness, icp_relevance, personalization, specificity, recency) in the Rubric tab"
    - "specificity and recency descriptions match the judge prompt wording in evals/rubric.py (the writer and judge stay in sync per the icp.yaml single-source principle)"
    - "Existing sheets tests still pass"
---

<objective>
The judge scores five eval dimensions (EvalScore in src/models.py:139-144: groundedness, icp_relevance, personalization, specificity, recency) and the Results tab renders all five columns (src/sheets.py:40-44). But the human-readable Rubric tab, generated by build_rubric_rows(), documents only three (groundedness, icp_relevance, personalization). specificity and recency were never added. This makes the Rubric tab contradict the Results columns two tabs over. Add the two missing rows so the Rubric tab is complete and self-consistent.

Purpose: the README demo Loom walks the Rubric tab on camera; it must not narrate a rubric that omits two of the five dimensions the sheet actually reports.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add specificity and recency rows to the judge rubric block in build_rubric_rows</name>
  <files>src/sheets.py</files>
  <action>
    In build_rubric_rows() (src/sheets.py), immediately after the `personalization` row block (the `rows.append([...])` ending around line 265, before `rows.append([])`), append two new rows following the exact same list-of-two-strings shape used by the groundedness/icp_relevance/personalization rows:

      rows.append(
          [
              "specificity",
              "1-5 categorical: 1 = generic claim that could apply to any "
              "company, 5 = highly specific facts unique to this account.",
          ]
      )
      rows.append(
          [
              "recency",
              "1-5 categorical: 1 = no recent evidence cited, 5 = multiple "
              "recent-news citations.",
          ]
      )

    The wording mirrors the judge prompt in evals/rubric.py:39-42 so the Rubric tab and the judge stay in sync. Do not change the groundedness/icp_relevance/personalization rows or any other part of the function.
  </action>
  <verify>
    - `uv run pytest tests -k sheets -q` passes (update any test that asserts the exact judge-rubric row set to include the two new rows, if such a test exists).
    - `uv run ruff check src/sheets.py` and `uv run black --check src/sheets.py` are clean.
    - `uv run mypy src/sheets.py` passes (strict).
    - Sanity: `uv run python -c "from src.sheets import build_rubric_rows; from src.icp_config import get_config; rows=build_rubric_rows(get_config()); flat=[c for r in rows for c in r]; assert 'specificity' in flat and 'recency' in flat, flat"` exits 0.
  </verify>
  <done>build_rubric_rows renders all five judge dimensions and the full test/lint/type suite for src/sheets.py is green.</done>
</task>

</tasks>

<success_criteria>
- The Rubric tab (build_rubric_rows) documents groundedness, icp_relevance, personalization, specificity, and recency.
- specificity/recency descriptions match evals/rubric.py judge-prompt wording.
- pytest (sheets), ruff, black, and mypy all pass.
- No em-dashes, no emojis, no vendor names introduced (public-repo discipline).
</success_criteria>

<output>
Create 260714-mrg-SUMMARY.md with status: complete recording the edit, the test result, and the commit SHA.
</output>
